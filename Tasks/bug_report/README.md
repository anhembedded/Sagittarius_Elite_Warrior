# 🐞 Bug Board — Sagittarius Elite Warrior

Bảng theo dõi **mọi lỗi đã báo cáo** của app. Tách riêng khỏi
[`../ROADMAP.md`](../ROADMAP.md) có lý do: ROADMAP là bảng *task* — bug đã sửa
thì xuất hiện ở mục Completed của nó, nhưng bug **đang mở** thì trước đây
không có chỗ nào hiển thị cả. Muốn biết hệ thống đang gánh lỗi gì, phải mở
từng file lên đọc. Bảng này là câu trả lời cho câu hỏi đó.

- **Bố cục thư mục** (song song với `Tasks/backlog|completed/` của task):
  - `incomplete/` — bug **chưa sửa xong**. Bug mới luôn tạo ở đây.
  - `completed/` — bug đã sửa, kèm ảnh chụp/bằng chứng của chính nó.
- Đặt tên `BUG-XXX_mô_tả.md`, số kế tiếp số lớn nhất đang tồn tại **ở cả hai
  thư mục**.
- ⚠️ **Đánh số tay đã hỏng một lần ở đây, 2026-08-26.** Hai phiên cùng lấy "số
  kế tiếp" từ bảng này, và `BUG-051`/`BUG-052` mỗi số được dùng cho **hai**
  lỗi khác nhau; chỉ lộ ra lúc merge. Hai bug của phiên sau đã đổi thành
  `BUG-057`/`BUG-058`.

  ✅ **Đã có guard ở repo này từ 2026-09-08:**
  `tests/unit/test_task_board_is_consistent.py` — chặn trùng ID trên **mọi**
  pool (`backlog`/`completed`/`in_progress`/`cancelled`/`proposal`/
  `bug_report/*`/từng epic), và bắt link gãy trên `ROADMAP.md` + bảng này.
  Nó tìm ra 4 va chạm còn tồn: `BOT-095B`, `BOT-095H` (bản sao cũ sót lại),
  `BOT-120`, và `BUG-058` — chính số sinh ra từ lần đổi ghi ở đoạn dưới, va
  lần thứ hai. Bug UI-freeze đã đổi sang `BUG-108`.

  Repo Engine có test tương đương
  (`tests/test_bug_board_is_consistent.py`: không trùng số, mọi file có dòng
  trên bảng, mọi dòng trỏ tới file có thật, số Open khớp `incomplete/`).
  **Bảng này chưa có** — port sang cần xử lý 10 file cũ đặt tên `BUG-00X.md`
  không theo quy ước `BUG-XXX_mô_tả.md`, nên là một task riêng chứ không phải
  một dòng thêm vào. Cho tới lúc đó: kiểm tay trước khi lấy số.
- **Khi sửa xong:** `git mv incomplete/BUG-XXX_*.md completed/` (kèm ảnh của
  nó), cập nhật `Status` trong file, rồi chuyển dòng tương ứng ở bảng dưới từ
  mục "Đang mở" sang mục "Đã sửa".
- Quy trình xử lý bug: [`.claude/rules/fix-bug-rule.md`](../../.claude/rules/fix-bug-rule.md)
  — root cause trước, regression test **fail đúng lý do** trước khi sửa, giữ
  test vĩnh viễn, ghi hồ sơ.
- Bug **không** được tính vào các con số task ở `ROADMAP.md`.

> **Cập nhật 2026-09-08 (2):** `BUG-109` báo và sửa cùng ngày — Realtime/tick mode + preset "365
> ngày qua" treo "Chạy Backtest" không thanh tiến trình. Bug Board vẫn 0 đang mở.
>
> **Cập nhật 2026-09-08: Bug Board về 0 bug đang mở.** `BUG-068` đóng dạng *không tái hiện được từ
> môi trường hiện có* (tiền lệ `BUG-058`/`BUG-060`) — không dòng code nào đổi. `BUG-034` đóng cùng
> lúc theo dạng đó rồi **mở lại và sửa thật** ngay trong ngày. Con số `0` nghĩa là *không còn hồ sơ
> nào đang điều tra*, không phải *app hết lỗi*.
>
> Cập nhật: 2026-09-01. Hai đợt số trùng nhau cùng ngày, xử lý riêng:
> `BUG-078` (phantom shard) và `BUG-079` (trend-zone, đổi 2 lần từ `BUG-077`
> gốc) đã đóng, đổi số lúc merge — xem hồ sơ §8 của từng file. Độc lập, cùng
> lúc: khảo sát cho `EPIC-021` mở 3 bug mới, đánh số **sau khi** merge nhánh
> trên nên tránh được va chạm với hai số đó — `BUG-080`/`BUG-081`/`BUG-082`.

---

## 📊 Tổng quan

| Trạng thái | Số lượng |
| :--- | :--- |
| 🔴 **Đang mở** | 0 |
| ✅ **Đã sửa / đã đóng** | 133 |
| 📈 **Tổng** | **133** |

---

## 🔴 Đang mở (Open)

| ID | Tiêu đề | Mức độ | Ngày báo |
| :--- | :--- | :---: | :---: |
| _(không có bug đang mở)_ | | | |

> Hai hồ sơ cuối đóng cùng ngày theo hai đường khác hẳn nhau, và cặp đó đáng nhớ:
> `BUG-068` đóng dạng **không tái hiện được từ môi trường hiện có** (cảnh báo Qt chỉ tồn tại trên
> nền tảng có cửa sổ thật — không sửa dòng code nào). `BUG-034` đóng cùng lúc *cũng* dạng đó, rồi
> **mở lại và sửa thật** ngay trong ngày: lượt điều tra thứ 5 phát hiện 4 lượt trước đo
> `viewRange()` trước khi pyqtgraph kịp chốt auto-range. Bài học: "không tái hiện được" đôi khi chỉ
> cách bằng chứng đúng một lượt đo cho đúng thời điểm — nên đóng dạng đó là **tạm dừng**, không
> phải kết luận. `BUG-110` (ngay hôm sau) là ví dụ tiếp theo: cùng dòng cảnh báo `BUG-034` cấy vào
> code để bắt lại triệu chứng, bắt được thật — nhưng log lần này không nêu tên thủ phạm, nên nhiều
> khả năng là một cơ chế khác đứng sau cùng một triệu chứng, không phải `BUG-034` tái phát.

---

## ✅ Đã sửa (Fixed)

| ID | Tiêu đề | Mức độ | Ngày báo | Sửa ở |
| :--- | :--- | :---: | :---: | :--- |
| **[BUG-135](completed/BUG-135_metatrader_csv_import_drops_time_and_rejects_dotted_dates.md)** | Import CSV MetaTrader (`BOT-112D`) không hoạt động thật: cột `Date`+`Time` tách riêng chưa bao giờ được ghép — `_ALIASES["open_time"]` khớp `date` trước rồi bỏ qua `time`, xoá sạch giờ trong ngày mỗi dòng; ngày kiểu MT4/5 thật (`2024.01.15`, chấm phân cách) khiến `datetime.fromisoformat()` raise `ValueError` mọi dòng. Phát hiện bởi phiên review độc lập của `PR #257`. Đã sửa: ghép `Date`+`Time` khi cả hai cột cùng tồn tại, chuẩn hoá dấu chấm sang gạch ngang chỉ ở phần ngày (không đụng phần giây lẻ) | 🟡 P2 | 2026-09-23 | ✅ 2026-09-23 |
| **[BUG-134](completed/BUG-134_dev_board_strategy_params_dialog_crashes_on_open.md)** | Nút "Strategy Parameters…" trên Dev Board crash `TypeError` ngay khi bấm — `_open_strategy_params_dialog()` truyền `self` (một `QObject` từ `EPIC-025` PR 1.4c-3, không còn là `QWidget`) làm Qt parent cho `StrategyParamsDialog`, trong khi `_dialog_parent()` (đã có sẵn trên đúng class này cho 2 dialog khác) mới là cách đúng. Phát hiện khi xây `BOT-063`'s dialog thông số indicator script, tái dùng đúng class này. Đã sửa: đổi sang `self._dialog_parent()` | 🔴 P1 | 2026-09-23 | ✅ 2026-09-23 |
| **[BUG-133](completed/BUG-133_tick_backtest_never_checks_intrabar_stops.md)** | Historical Tick Backtest (`BOT-076`) không bao giờ gọi `PaperExchange.check_intrabar_stops()` — Stop Loss, Take Profit, thanh lý (`BOT-049`) và MAE/MFE (`BOT-106B`) đều bị vô hiệu hoá âm thầm ở chế độ này. Đã sửa: gọi `check_intrabar_stops()` mỗi tick (dùng high/low của chính tick đó, không phải bar đã gộp) ngay trước khi tick đó được đánh giá — giữ đúng độ phân giải tick, khác với Static (mỗi bar). MAE/MFE ở chế độ tick cũng tự động hoạt động lại theo đó, không cần sửa gì thêm | 🟡 P2 | 2026-09-22 | ✅ 2026-09-22 |
| **[BUG-132](completed/BUG-132_chart_subplots_zoom_divergence_and_missing_zoom_limits.md)** | Khi lăn chuột zoom ở chart volume / equity / subplots, chart con zoom quá mức hoặc lệch hoàn toàn khỏi chart nến và không có giới hạn zoom tối thiểu (`minXRange`). Đã sửa: áp dụng giới hạn `xMin`/`xMax`/`minXRange`/`maxXRange` đồng nhất cho toàn bộ subplots trong `plot_layout`, kế thừa ngay khi khởi tạo subplot, và mở rộng `CachedFrameInteractionController.begin_zoom` + scale clamping để hỗ trợ zoom trên mọi plot | 🟡 P2 | 2026-09-21 | ✅ 2026-09-21 |
| **[BUG-110](completed/BUG-110_chart_range_warning_tai_xuat_hien_sau_khi_BUG-034_da_sua.md)** | **Đóng không tái hiện được, cùng tiền lệ `BUG-068`/`BUG-121` — không sửa dòng code nào.** Cảnh báo `[chart-range]` (nến bị ép dẹp còn 18.77% trục Y) tái xuất hiện trên Trading (ETHUSDT) sau khi `BUG-034` đã sửa; log không nêu tên item thủ phạm nào ngoài nến, nên chắc chắn là cơ chế khác. 3 lượt điều tra: round 1 loại trừ cơ chế `BUG-034` bằng đọc code; round 2 loại trừ bằng toán học rằng padding của pyqtgraph không thể tạo dải rộng như log, thử 4 bộ dữ liệu tổng hợp headless không tái hiện, tăng cường log vĩnh viễn (`windowed price band`, `live candle forming`); round 3 phát hiện `Xvfb`+`xcb` (compositor X11 thật) có sẵn trong container, dùng nó **loại trừ sống** cả 2 giả thuyết cụ thể còn lại (Y auto-range theo toàn bộ lịch sử thay vì cửa sổ hiển thị; nến live glitch bị đóng băng vào trục) — cả hai đều hoạt động đúng. Giả thuyết cuối cùng chưa thử (hành vi đặc thù Wayland — nền tảng log gốc, khác X11 đã thử) không có compositor Wayland trong container để kiểm. Log chẩn đoán tăng cường vẫn còn trong code, sẵn sàng bắt đủ bằng chứng nếu triệu chứng tái xuất | 🟡 P3 | 2026-09-09 | ✅ 2026-09-19 |
| **[BUG-121](completed/BUG-121_the_ui_integration_tier_hangs_or_aborts_when_run_serially.md)** | **Closed not-reproducible, same precedent as `BUG-068`/`BUG-034` — no code fix, not a conclusion.** The 47 tests in `tests/integration/presentation/ui/` intermittently hung or aborted when run together (~50% on 2026-09-15); root cause (a `ThreadManager` worker or a `QWidget` surviving into the next test) was never confirmed. Hardened 2026-09-19 regardless of reproduction: `pytest_collection_modifyitems` marks this tier `@pytest.mark.timeout(60, method="thread")` so a future hang fails loudly with a full thread dump instead of silently eating the CI timeout budget (`pytest-timeout`'s `signal` method never fires while the main thread is stuck in Qt's C++ event loop — the gap `BUG-119` left); both `thread_manager.shutdown(wait=True)` call sites now assert `stats().in_flight == 0` right after the drain, positive proof it actually covered every submitted task. **15/15 reproduction attempts clean** (10 sequential, 3 xdist+`pytest-randomly`, 2 full-gate CI runs) — user decided to stop chasing it further and close. If it recurs, the hardening now catches it under a new bug number rather than silently again | ⚪ (was 🟠 P2) | 2026-09-15 | ✅ 2026-09-19 |
| **[BUG-131](completed/BUG-131_indicator_script_convention_guard_scans_a_deleted_directory.md)** | `test_every_script_is_registered_in_the_module` and `test_domain_layer_never_imports_a_ui_toolkit` (`tests/unit/support/indicators/test_indicator_script_conventions.py`) scanned `src/domain/indicator_scripts` — deleted since `EPIC-025` PR 1.6g (`c3a949af`, 2026-09-16) when indicator scripts moved to `src/support/indicators/indicator_scripts/`. Both passed vacuously ever since. **Correction found during the fix:** the second test was not fully redundant with `test_module_domain_is_qt_free.py` as first suspected — it is `architecture-rule.md` §3's sole enforcement of the Shared Kernel allow-list (no bare `sagittarius_engine` import) for indicator scripts, so that allow-list was unenforced there too, not merely duplicated. Retargeted both constants to `src/support/indicators/indicator_scripts`; added a non-vacuous-scan regression test; reconciled `scanned_roots_registry.py`'s stale second root. Mutation-verified by temporarily injecting `import PySide6` into a real script and watching the guard catch it | 🟢 P3 | 2026-09-19 | ✅ 2026-09-19 |
| **[BUG-130](completed/BUG-130_dev_board_recheck_edge_fires_on_a_deleted_chart_card.md)** | **Retracted root cause, real one found by an independent PR review.** File này ban đầu kết luận "có sẵn từ trước, không phải lỗi của PR #226" bằng cách so `master-warrior` với PR branch — nhưng phép so đó chạy `PYTHONPATH=..` từ checkout đang ở PR branch, nên "tái hiện trên `master-warrior`" thực ra chạy file test cũ của `master-warrior` **cùng với `src/` của chính PR branch** — cùng một code, hai lần. Review độc lập (PR #226) chạy đúng cách và tìm ra thật: `6678d456` (chính PR này) thêm tham số bắt buộc `config_store` vào `ArmStrategyCommandHandler.__init__`, và `tests/integration/presentation/ui/conftest.py:290`'s `mock_dispatch` fixture quên cập nhật — mọi lần "arm strategy" thật đều raise `TypeError`, bị `StrategyArmingCoordinator.on_arm_clicked()` nuốt thành một dòng log thay vì lỗi hiện rõ, nên triệu chứng là timeout. `ERROR` log `_recheck_edge` cũng biến mất sau khi sửa — chỉ là hệ quả phụ của luồng arm bị hỏng, không phải cơ chế riêng như giả thuyết ban đầu. Sửa: thêm `config_store` vào lệnh gọi trong fixture (1 dòng) | 🟠 P2 | 2026-09-17 | ✅ 2026-09-17 |
| **[BUG-129](completed/BUG-129_the_checker_that_answered_about_my_disk.md)** | **The check answered about the developer's disk, and GitHub CI was red for three merges.** `scripts/check_skill_prompt_references.py` resolved every cited path with `Path.exists()` — the filesystem, not the repository. A working tree carries more than the repository does: `EPIC-025`'s moves deleted two legacy directories and every tree kept them alive as shells holding nothing but `__pycache__`, which nothing deletes. So two agent briefings citing those paths passed on every machine and failed in CI, which clones fresh — and because this script runs **before** the tests, every later CI step was skipped: `master-warrior` was red from run **371** (2026-09-16 14:49 UTC, the merge that recorded one of those directories as gone) through run 390 — **20 runs, ~11½ hours**, measured from the runs themselves because the first fix commit had inherited the timeline from the newest three — while four local gate runs across three pull requests called that step green. Another session (`9a4a65d2`) deleted the two offending prompts to unbreak CI; that is the symptom. The mechanism is that a check whose answer depends on who runs it is worse than no check, because it is believed — so it reads `git ls-files` now, adding every ancestor of every tracked file so a cited directory resolves from what git holds inside it, and where git cannot answer the filesystem fallback prints a warning naming this bug instead of degrading silently. **Cleaning the stale directories off this disk immediately surfaced a second instance:** `test_module_boundaries.py` required the deleted application zone to exist, so it too had passed everywhere for the same wrong reason, and would have failed CI the moment the reference check went green — that entry is retired with its reason recorded, and `domain` (one file left) is flagged as next. Regression test written first and confirmed red for the right reason: five tests, each building a real tiny repository, because the bug is git and the filesystem disagreeing and a stub of either side cannot show that. Verified positively by re-creating the silent condition — it exits 1 and names the reference where before it exited 0. Written up as [`CS-005`](../../Docs/CASE_STUDIES/CS-005_the_check_that_asked_the_wrong_question.md), whose *still open* list names what this did not close: a local gate cannot see a red CI | 🟠 P2 | 2026-09-17 | ✅ 2026-09-17 |
| **[BUG-128](completed/BUG-128_the_time_range_picker_seeds_an_inverted_range.md)** | **A guard that detected the state and declined to repair it.** `TimeRangePickerVM.refresh()` tested `start is None or end is None or start > end` and then repaired only the first two disjuncts — an inverted pair is non-`None` on both sides, so both `or` fallbacks kept what they had and the picker opened on `08 Jul → 01 Jul` with **Apply enabled** and the summary reading `0 days`. The applied window is one the backend reads as empty: nothing to sync, nothing to backtest, no message saying why. `can_apply` could not catch it downstream either — it asks whether both ends are *present*, which an inverted pair satisfies. The VM had **13 unit tests**, one of them on that very condition's unparseable branch, and **no run has ever collected them**: they lived at `src/presentation/ui/qml/TimeRangePicker/tests/` while the gate runs `pytest tests`. PR 1.4b-2 counted twenty such files; this is the first time the hole cost something. Fixed as three explicit branches in `seed_range()` — the defect *was* a fallback that missed the case its own condition named — keeping the end the user last had and re-deriving the start. **The pull request's own review found the second half:** two calendars let the user invert the pair by clicking From after To, and `can_apply` said yes there for the same reason, so it now requires both ends *and* their order and the summary says *"The end is before the start"* instead of clamping to `0 days`. Regression test written first and confirmed red returning `2026-07-08 → 2026-07-01`; the clicked path is held by a test that fails when `_calendar()`'s `clicked` connection is deleted, verified by deleting it. Written up as [`CS-004`](../../Docs/CASE_STUDIES/CS-004_the_tests_the_gate_never_ran.md), whose check — `test_no_test_file_lives_under_src.py`, shrink-only at the 22 that remain — closes the class | 🟠 P2 | 2026-09-16 | ✅ 2026-09-16 |
| **[BUG-127](completed/BUG-127_backtest_market_rule_verification_never_verifies.md)** | Màn Backtest luôn báo *"Not verified against exchange rules"* — tính năng kiểm tra quy tắc sàn (`BOT-095E1`) **chưa bao giờ kiểm tra gì cả**. Đủ mọi mảnh, mỗi mảnh có test xanh, nhưng **thiếu hai sợi dây**: không ai gọi `parse_binance_symbol_metadata()` (producer duy nhất, không có caller nào trong `src/`), và `ISymbolMarketMetadataCache` **không được bind ở đâu cả** — đo trên graph thật thì `resolve()` raise, nên nhánh `except` trong `backtest_presenter.py:405-413` là đường **duy nhất** và màn hình luôn nhận một cache rỗng vĩnh viễn. Cùng bệnh với `CS-002` nhưng ở cơ quan khác: guard của `CS-002` chỉ quét *bus subscriber*, không quét port không có binding. Đo cả cây: 43 tên được `resolve()`, chỉ 3 tên không hề được bind — `IThreadManager` và `Scheduler` do **Engine** đăng ký (hợp lệ), còn lại đúng một cái là bug này. | 🟠 P2 | 2026-09-16 | ✅ **Đã sửa 2026-09-16** — `ISymbolMetadataProvider` (port mới, có verified fake + contract suite chạy cả hai implementation) lấy `exchangeInfo` **đúng payload `get_available_symbols()` vẫn tải rồi bỏ đi**, nên không thêm request weight nào; cache được bind, và worker sync nền của màn Backtest hâm nó cho đúng symbol người dùng sắp chạy — chỗ duy nhất vừa off-thread vừa biết symbol. Presenter mất luôn nhánh `try`/`isinstance`/`except`: thiếu binding phải chết to lúc dựng, chứ không thoái hoá thành một câu trả lời âm vĩnh viễn. Allowlist **36 → 35** (đóng luôn `EPIC-025D` mục 2 — cách thứ ba một dòng rời file đó: bằng một **bản sửa**). Bằng chứng log dương tính trong §3 hồ sơ: sau khi hâm, một giá trị lệnh dưới min notional bị **từ chối thật** kèm con số. |
| **[BUG-126](completed/BUG-126_system_failures_reached_no_subscriber_at_runtime.md)** | **Every swallowed failure reached nobody** — `EPIC-008` §1's own P1 finding, still true two epics after that epic closed it. `safe_ui_action` publishes `UiActionFailedEvent` with a full traceback and `TaskManager` publishes `TaskFailed`; `SystemErrorFeed` was the only subscriber of either, and **nothing ever constructed it** — its subscriptions live in `BaseFeed.__init__`, so a class nobody builds subscribes to nothing. No user could report it: the defect's nature is that it produces no output. Four checks were green — the feed's own test constructed it (the one step production omitted), `test_event_flow_guards.py` checks a subscription addresses its event by class and that is true of one that never runs, ruff's dead-code rules see an unused *import* not an unreachable class, and mypy type-checks it either way. A fifth, HLD §3.5's *re-measured* dead-code list, actively protected it: *"Live, do not delete: imported by `order_feed.py`"* — `order_feed.py` only **mentions** it in a docstring, and a text search cannot tell a mention from an import. Fixed as the mechanism, not the wiring: one non-Qt `SystemFailureLog` constructed by the composition root before `boot()`, reporting at `ERROR` with `[system-failure]` into the log file `--dev` writes and the CI log scan greps — so the headless entry point gets it too, and no screen wires anything. The feed is deleted (`base_feed.py`'s own rule: promote a fact when the **second** consumer appears — there was never a first). Guarded by `test_a_bus_subscriber_is_constructed.py`, which reads the **AST**, not the file text: 8 subscribers reachable, 1 orphan, 0 false positives. Written up as [`CS-002`](../../Docs/CASE_STUDIES/CS-002_the_subscriber_nobody_built.md) | 🔴 P1 | 2026-09-16 | ✅ 2026-09-16 |
| **[BUG-125](completed/BUG-125_ilogger_has_no_exception_and_printf_args_are_dropped.md)** | **`ILogger` has no `exception()`** — and found the way it was meant to be found: by reading `CS-001`'s own *"where else this is still open"* list, the first use that list was put to. Same file as `BUG-124`, one line below it: `self.logger.exception(...)` on the developer-mode **failure** path, whose entire purpose was to leave the switch honest when the write failed — so instead the slot died and the toggle kept the value the user clicked. The engine's `ILogger` declares exactly info/warning/error/debug/critical/trace; `exception` is `logging.Logger`'s. Second defect in the same method: `info("... %s.", enabled)` — the port takes `(message, extra: dict | None)`, so the value landed in `extra` and the log printed a literal `%s` on every successful toggle. Blast radius measured, not assumed: of 241 `logger.*` call sites, 196 are the stdlib logger (which *does* have `exception`), and exactly **one** ILogger site was wrong — `backtest_presenter`'s nine `log_*` calls are a different class, `BacktestEventLogger`. **The test that drove the broken line was green**, because the container fixture answered `Mock()` for every port it did not name: the same blind spot `CS-001` is about, one line away. No new test needed — giving the fixture the real `StdLogger` made the existing test able to fail. The `BUG-124` guard was renamed `test_engine_port_calls_are_real.py` and now covers `self.logger` in presenters too, including the printf shape | 🟠 P2 | 2026-09-16 | ✅ 2026-09-16 |
| **[BUG-124](completed/BUG-124_welcome_start_called_publish_on_the_engine_event_bus.md)** | **Start on the Welcome screen did nothing** — the app opens there (ADR D13) and Start is the only way off it. `welcome_presenter.py` called `self.event_bus.publish(...)`: `publish` is the *app's* `IEventPublisher`, while `BasePresenter.event_bus` is the *engine's* `IEventBus`, whose verb is `emit`. The slot logged `[WELCOME] Start requested.` and died on the next line, so the log agreed with the user's intention while nothing happened. Three nets were silent for three different reasons — `IContainer.resolve` returns `Any` so mypy checked nothing (and `src/shell/` *is* gated); the unit test's hand-written `_Bus` defined `publish` **and** `on` **and** `subscribe`, the union of two interfaces plus an invented verb, so it could not disagree with the code; and no tier imports `src/shell/` wholesale. Fixed as the mechanism: `emit`, the bus named with its type so the call is back under mypy, the double replaced by the real `MemoryEventBus`, and a guard checking every bus call in `src/`+`scripts/` against `IEventBus`'s real methods by name. Written up as [`CS-001`](../../Docs/CASE_STUDIES/CS-001_a_double_that_could_not_disagree.md), the first case study | 🔴 P1 | 2026-09-16 | ✅ 2026-09-16 |
| **[BUG-123](completed/BUG-123_mocked_candle_grid_moves_between_two_calls.md)** | The gate could fail on the clock alone: `mock_klines.build_mock_klines()` read `datetime.now()` **on every call**, and two callers read it at two different moments — the `seeded_history` fixture seeding the store, then `_older_mock_klines()` in the test body deriving the page below it. A minute rolling over between them shifted the second grid a minute later, so the "older" page's newest row landed on the row the chart already held, the store deduplicated it, and 4 of 5 candles prepended: `assert 9 == (5 + 5)` in `test_scrolling_near_the_left_edge_prepends_older_candles`. Two consecutive full-gate runs five minutes apart, one green one red, on the same diff. The same read also put the five seeded symbols on five different grids, which nothing was watching. Fix is the mechanism, not the call site: **one anchor per process** at import, so every current and future caller derives the same grid. Reproduced deterministically with an injected clock that jumps a minute per `now()`, not by waiting for a rollover | 🟡 P3 | 2026-09-16 | ✅ 2026-09-16 |
| **[BUG-122](completed/BUG-122_shutdown_built_a_binance_client_only_to_close_it.md)** | `MarketDataModule.shutdown()` resolved `IExchangeClient` unconditionally to close it — and the port is bound lazily, so a session that never opened a chart **built a Binance client in order to close it**: one network call per exit, and `python-binance`'s `Client.__init__` creates an asyncio event loop in its websocket helper that nothing closes, so the interpreter printed `Exception ignored in BaseEventLoop.__del__` on stderr at exit. The wart was documented in that method's own docstring, ending "needs a way to ask the container whether a singleton was ever instantiated, which it does not currently offer" — stale: `Registration.instantiated` answers exactly that, and a registry read builds nothing. Found by `EPIC-025` PR 1.2, which changed GC timing enough to make the warning appear every run (3/3 clean on `master-warrior`, 3/3 noisy on the branch, identical app logs) — so the bug predates it. Loop creation traced to its call site by patching `asyncio.new_event_loop`, not guessed | 🟠 P2 | 2026-09-15 | ✅ 2026-09-15 |
| **[BUG-120](completed/BUG-120_a_verified_fakes_own_helper_was_verified_by_nothing.md)** | A verified fake's own helper was verified by nothing — `FakeMarketDataSync.was_asked_for()` was the only positive assertion in three screens' tests, and hard-coding it to `return True` left **152 of them green**. Root cause is the mechanism, not the helper: HLD §10.3 makes a fake verified by running the port's contract suite against it, which covers exactly the surface **the port declares** — nothing said who verifies what a fake adds on top, so every fake was free to grow the same hole (Phase 1 adds two more ports, and three market_data ports still have no suite). Fixed at that level: a guard requires every member a fake declares beyond its port to be exercised beside the contract suite, and its message names both legitimate outcomes — test it, or delete it. Both were used: `was_asked_for` got seven tests, `synced_symbols` (zero callers, zero tests) was deleted. Found by the `E12` row added to the PR-review skill four days after `BUG-118`'s search box shipped with 22 green tests that could not fail — same disease, different place | 🟠 P2 | 2026-09-14 | ✅ 2026-09-14 |
| **[BUG-118](completed/BUG-118_ui_tests_leak_qt_objects_until_the_gate_stalls.md)** | `ci-local.ps1 -Full` đứng im ở 96%, không test nào đỏ — **test UI không trả lại object Qt** | 🔴 P1 | 2026-09-14 | User nói "fix tại sao nó treo". Root cause: mỗi presenter test dựng view thật (`QmlHostView` → một `QQuickWidget` + engine QML riêng), và một view Qt là **đồ thị, không phải cây** — view giữ view-model, signal của view-model giữ slot của view — nên refcount của CPython không giải phóng được; `tests/unit/presentation/` lại **không có conftest nào**, không `qtbot`, không ai bơm event loop. Đo bằng probe RSS từng test: rò ~20–30 MB mỗi test UI tới khi worker vượt 3 GB. Sửa ở cơ chế (`fix-bug-rule.md` §2): **một** autouse fixture ở biên tier, không phải 2100 lần teardown — peak RSS của `test_backtest_presenter.py` 2,86 GB → **258 MB** (−91%). Test hồi quy đầu tiên bị **bỏ đi** vì nó xanh cả khi chưa sửa (widget không có vòng tham chiếu thì refcount giải phóng ngay — đúng bẫy `BUG-013`); bản giữ lại nêu đúng **vòng**. Khoảng trống lâu dài lộ ra: gate không biến một test treo thành một test đỏ → cài `pytest-timeout` (60s, đo từ `--durations`) |
| **[BUG-119](completed/BUG-119_the_gate_stalls_silently_one_test_loops_forever.md)** | `ci-local.ps1 -Full` đứng im ở 96%, ba lần dừng sau đúng 3993 test, không test nào đỏ. **Một test chạy vào `while True: time.sleep(1)`**: PR 0.4a-2 đổi `execute_stream()` sang dispatch qua `ICommandDispatcher`, nên mock `app.dispatch` của test thôi chặn — `Mock` truthy lọt qua guard `success`. Worker không báo cáo → controller chờ mãi. Sửa ở cơ chế: một helper `dispatching_app()` cho cả 4 file test. **Hồ sơ này lúc đầu ghi SAI nguyên nhân** (kết luận deadlock xdist vì chỉ `py-spy` 2/4 worker) — giữ lại phần tự phê để không lặp lại | ✅ 2026-09-14 |
| **[BUG-117](completed/BUG-117_positions_table_pnl_frozen_between_account_updates.md)** | Bảng Vị thế "đứng hình": unrealized PnL không đổi giữa hai lần khớp lệnh dù giá vẫn chạy | 🟡 P3 | 2026-09-10 | User đối chiếu ảnh Binance thật (+15,98 USDT) với số app hiển thị (+2,57 USDT, đứng yên). Root cause: `LivePosition` chỉ được tính lại khi sàn đẩy `ACCOUNT_UPDATE` (khớp lệnh/funding) — tick giá live chỉ vẽ chart, không chạm bảng Vị thế. Bản sửa đầu (`QTimer` riêng từng Presenter) bị user chặn lại đúng lúc — "không hotfix, cần scalable" — redesign thành `PositionRefreshService` **một instance duy nhất**, đăng ký lúc boot qua `Scheduler` sẵn có của engine, no-op khi tắt trading, phát lại qua đúng `PositionChangedEvent`/`PositionClosedEvent` mà cả 2 màn đã lắng nghe sẵn — 0 dòng wiring mới ở Presentation layer. 5 test mới. Luật "không hotfix, ưu tiên redesign cơ chế" ghi thành `fix-bug-rule.md` §2 mới. |
| **[BUG-116](completed/BUG-116_limit_order_missing_time_in_force.md)** | Đặt lệnh Limit thủ công luôn bị từ chối: `LIMIT order is missing time_in_force.` | 🟠 P2 | 2026-09-10 | User tự chạy app thật, click "Limit" trên thẻ đặt lệnh thủ công, gửi log lỗi thật. Root cause: `map_order_to_futures_params()` có sẵn guard đúng, từ chối LIMIT thiếu `time_in_force` — nhưng `PreviewOrderQueryHandler.execute()` chưa từng gán field đó, latent từ `EPIC-021E` vì nhánh chiến lược tự động chỉ gửi MARKET; thẻ đặt lệnh thủ công (`EPIC-024B`) là caller LIMIT thật đầu tiên chạm guard. Sửa: gán `time_in_force=TimeInForce.GTC` khi `order_type is LIMIT`, giữ `None` cho MARKET. 2 test mới, xác nhận đỏ đúng lý do (khớp nguyên văn lỗi thật) trước khi sửa. Đánh số **116** (không phải 115) — `BUG-115` đã bị lấy trước lúc merge bởi một phiên song song. |
| **[BUG-115](completed/BUG-115_qquickwidget_transparent_clear_colour_black_or_see_through_on_hardware_compositor.md)** | Mọi `QQuickWidget` nhúng (10 host) dựa vào `setClearColor(transparent)` để "lộ nền cha" — thân modal QML và bảng QML ra **đen** (X11) / **xuyên thấu** (Wayland) trên màn hình thật | 🟠 P2 | 2026-09-09 | User báo "trên Windows tốt, trên Ubuntu bị glitch, nghĩ do cơ chế bridge nào đó" — đúng: lỗi ở cầu nối QtWidgets↔QML, không ở widget nào. Root cause: `QQuickWidget` trên đường vẽ **texture** (mọi phiên desktop thật) là render-to-texture widget — Qt **đục lỗ** backing store dưới nó (`qwidget.cpp`) rồi ghép texture **không blend** lên nền clear `Qt::black`; nền cha không còn ở đó để lộ ra. Giả định "transparent = lộ nền cha" chỉ đúng trên đường **software** (`offscreen`, `widget.grab()`) — tức toàn bộ tầng test, nên 100% test xanh trong khi user thấy lỗi. `BUG-102` sửa `WA_StyledBackground` chỉ chạm phần chrome, không chạm được thân QML (xem §6 hồ sơ đó). Sửa **general**, không vá 10 chỗ (`ONBOARDING.md` §12.5): Engine `TASK-042` cho `create_quick_widget(background=...)` clear đục theo token; app `BOT-132` thêm `qml/embed/QuickSurface(surface: StyleRole)` — nơi **duy nhất** dựng `QQuickWidget`, nền đọc từ **cùng** bảng token `apply_role()` vẽ nền cha; 10 host + 11 preview migrate; xoá bản chép `qml/style.py`. Guard cấm dựng `QQuickWidget`/`setClearColor(` ngoài `qml/embed/` (đỏ 17 chỗ trước khi migrate). Đo lại trên X11 thật: 3 điểm mẫu `#000000` → `#111318`, pixel-diff `grab()` ↔ màn hình 24,12 % → **0,00 %**. Tầng Desktop mới (`scripts/quick_surface_desktop_probe.py`) là tầng đầu tiên của repo chạy đường texture — thứ đã bỏ lọt cả `BUG-102` lẫn bug này. |
| **[BUG-114](completed/BUG-114_position_leverage_field_missing_from_real_v3_payload.md)** | `GetOpenPositionsQuery`/`EnableTradingCommand` crash trên tài khoản có vị thế thật: `/fapi/v3/positionRisk` không có field `leverage`/`marginType` như code giả định | 🔴 P1 | 2026-09-09 | User tự chạy app thật, gửi log lỗi thật + ảnh Binance UI xác nhận vị thế 20x thật. Root cause: `map_futures_position_payload_to_live_position()` đọc thẳng `payload["leverage"]`/`marginType` — shape viết theo tài liệu, chưa xác minh lệnh gọi thật (egress `*.binance.*` bị chặn trong sandbox). Response V3 thật không có 2 field đó. Sửa: suy `leverage` từ `notional/initialMargin` (công thức margin của chính Binance, verify khớp đúng 20x trên 3 payload thật), `margin_type` từ `isolatedMargin`/`isolatedWallet`. 1 test mới dùng đúng payload thật user gửi làm fixture, mutation-verify tái hiện đúng `KeyError: 'leverage'` trước khi sửa. |
| **[BUG-113](completed/BUG-113_live_stream_kline_tick_logged_at_info.md)** | Mỗi tick kline live log ở mức INFO — tái diễn đúng lớp lỗi `BUG-042`/`BUG-095` | 🟡 P3 | 2026-09-09 | User đọc log thật của chính mình, chỉ đúng lý do không cần gợi ý trước. `BinanceWebsocketService._process_socket_message()` gọi `logger.info(...)` cho mọi message kline WebSocket — vài dòng mỗi giây trên symbol đang hoạt động, đúng lớp lỗi `BUG-042` (per-event ở INFO qua `SignalLogHandler`'s queued-signal UI mirror). `BUG-095` đã sửa lớp này ở `futures_user_data_stream.py` nhưng dòng `[Live Stream]` sống ở file khác (luồng giá kline) nên lọt lưới. Đổi `logger.info` → `logger.debug`. 1 test mới cùng khuôn `BUG-095`, xác nhận đỏ đúng lý do trước khi sửa. |
| **[BUG-112](completed/BUG-112_enable_trading_requires_a_strategy_manual_trading_never_needed.md)** | "Bật giao dịch" bắt buộc nạp chiến lược dù chỉ muốn giao dịch thủ công — vòng luẩn quẩn thật | 🟠 P2 | 2026-09-09 | User tự chạy app, mô tả trực tiếp vòng luẩn quẩn kèm log thật: giao dịch thủ công cần Bật giao dịch, Bật giao dịch cần nạp chiến lược, mà nạp chiến lược đúng symbol muốn giao dịch tay lại bị luật khác (`PRO-003` §4.1.2) chặn tiếp. Root cause: gate `NO_STRATEGY_ARMED` (`EPIC-022B`) viết từ trước khi "Bật giao dịch" có nghĩa "tài khoản được phép gửi lệnh thật" dùng chung cho cả chiến lược tick và người bấm tay (`EPIC-024B`) — chưa được cập nhật theo nghĩa mới. Xác minh: đường chiến lược tự động chỉ chạy khi có armed nên bỏ gate không tạo lại được trạng thái "Bật giao dịch vô nghĩa" `EPIC-022B` từng lo. Sửa: bỏ hẳn điều kiện + tham số `strategy_session` khỏi `EnableTradingCommandHandler`, xoá enum `NO_STRATEGY_ARMED` chết. 1 test mới thay thế test cũ (hành vi đối lập), xác nhận đỏ đúng lý do trước khi sửa. |
| **[BUG-111](completed/BUG-111_signed_binance_requests_never_sync_clock_skew.md)** | Mọi request có ký (`futures_*`) thất bại vĩnh viễn với `-1021` khi đồng hồ máy chạy nhanh — chưa từng đồng bộ `timestamp_offset` | 🔴 P1 | 2026-09-09 | User chạy app thật trên Testnet, gửi log INFO thật (`GetOpenPositionsQuery failed: ... APIError(code=-1021)`, 2 lần cách nhau 8s, cùng lỗi). Root cause: `python-binance`'s `timestamp_offset` mặc định `0`, `ExchangeSessionFactory.create_trading_client()` chưa từng chỉnh nó — mọi request có ký dùng thẳng đồng hồ local; Binance từ chối cứng nếu timestamp vượt server-time hơn 1000ms, ngưỡng này `recvWindow` không nới được. `FuturesAccountReader.check_connection()` đã tự đo đúng độ lệch này (`server_time_skew_ms`) từ trước nhưng chỉ để hiển thị, không áp lại. Sửa: `_sync_timestamp_offset()` gọi `futures_time()` rồi gán `client.timestamp_offset` ngay sau khi dựng `Client(...)` — đúng 1 nơi duy nhất mọi session có ký được tạo, nên mọi consumer tự động được sửa. 1 test mới, fake server trả `serverTime: 0` nên độ lệch đúng phải là số âm lớn — xác nhận đỏ đúng lý do (`0` mặc định) trước khi sửa. |
| **[BUG-109](completed/BUG-109_tick_mode_bounded_wide_range_coverage_probe_hangs.md)** | Realtime/tick mode + preset "365 ngày qua" (bounded, không phải unbounded) treo "Chạy Backtest" nhiều phút, không thanh tiến trình | 🟠 P1 | 2026-09-08 | User báo trực tiếp qua log INFO thật + mô tả triệu chứng ("no bị đứng rồi", "đứng lâu mà ko thấy hiện thanh process gì"). Root cause: `_coverage_is_ready()` gọi `probe_coverage()` đồng bộ trên worker thread, không progress bar, dispatch `GetBacktestRangeCoverageQuery` ở `effective_data_interval` = `tick_resolution` (1s, không phải khung hiển thị chart) trên khoảng 364 ngày — 52× cửa sổ 7-ngày đã đo được ở `tick_data_feasibility.md` (~17s, đã kết luận "not usable synchronously"). `TickModeRequiresBoundedRangeRule` (`BUG-073`) chỉ chặn `start_time is None`, chưa từng chặn một khoảng **có giới hạn nhưng quá rộng**. Sửa: thêm ngưỡng `_MAX_TICK_MODE_RANGE_DAYS = 7` (tái dùng đúng số đã validate ở BOT-075) vào rule đó, đưa `start_time`/`end_time` đã resolve vào `PreBacktestInput`, đổi thứ tự `build_run_config()` để resolve range trước khi validate; áp cùng ngưỡng cho `ChartPreviewCoordinator` qua 1 hàm dùng chung `tick_mode_range_too_wide()`. 11 test mới (5 rule thuần + 3 wiring Run button + 3 wiring toolbar preview), xác nhận đỏ đúng lý do trước khi sửa. |
| **[BUG-034](completed/BUG-034_dev_board_live_chart_wrong_axis_scale.md)** | Dev Board Live Chart: nến không hiển thị, trục Y auto-range sai thang đo | 🟠 P2 | 2026-08-23 | **Root cause:** trục Y của main plot dùng chung, và `IndicatorManager.add_overlay()` là **lớp overlay duy nhất** còn tham gia auto-range Y — trend-zone shading và marker vốn đã không (§8.2/§8.3). Một đường không theo thang giá kéo trục ra, nén nến thành vạch. **Sửa:** `addItem(curve, ignoreBounds=True)` — trên main plot, **giá** định nghĩa trục giá; `add_subplot()` không đổi. Giá phải trả (chấp nhận có chủ đích): overlay chạy ngoài dải nến bị cắt ở mép thay vì nới trục — với overlay app đang ship (EMA/WMA/`close + session_range`) khác biệt bằng 0. **Đường đi tới root cause đáng ghi:** 4 lượt đầu kết luận "không tái hiện được" vì pyqtgraph **không** tính lại auto-range lúc thêm item — nó chốt ở lần paint kế tiếp, mà `offscreen` không có paint, nên `viewRange()` đọc ngay sau khi nạp luôn trả về dải *trước khi* item được thêm. Đóng sáng 08-09 dạng không-tái-hiện-được, mở lại và sửa xong chiều cùng ngày. 4 test giữ lại, 2 mutation-verify. Log `[chart-range]` nêu đích danh item kéo trục **ở lại** dù đã sửa (§11.5). |
| **[BUG-068](completed/BUG-068_cross_thread_qbasictimer_start_in_gap_inspection.md)** | QBasicTimer::start: Timers cannot be started from another thread trong quá trình kiểm tra Database Gaps | ⚪ Đóng — không tái hiện được từ môi trường hiện có (trước: 🟠 **P2**) | 2026-08-30 | **Đóng 2026-09-08 (user quyết định):** dừng điều tra, không sửa dòng code nào — đây là cảnh báo, không phải crash, và §3 đã tái hiện sống thật trên `offscreen` cho 0 cảnh báo; nghi là lớp lỗi chỉ tồn tại trên nền tảng có cửa sổ thật mà tầng test của repo về nguyên tắc không nhìn thấy được. Xem §5 của hồ sơ. Khi chạy `GetDatabaseGapsQuery` trên worker thread của `ThreadManager`, xuất hiện 4 cảnh báo Qt timer vi phạm thread affinity. Biến thể của lớp lỗi `BUG-031`. **Cập nhật 2026-08-31:** tái hiện sống thật (app boot thật, DB seed gap thật) trên `offscreen` cho 0 cảnh báo — đã loại trừ query handler, `Dispatcher.dispatch()`, `SignalLogHandler`, và toàn bộ đường `ui_gap_inspector_signal` → `GapInspectorDialog`. Nghi thuộc lớp lỗi chỉ tồn tại trên nền tảng có cửa sổ thật (threaded render loop của Qt Quick, `offscreen` luôn rơi về basic loop) — cần tái hiện trên Windows thật để đi tiếp. |
| **[BUG-107](completed/BUG-107_trading_screen_opens_live_with_no_click.md)** | Mở màn Giao dịch (click thật) vẫn tự sync + mở live stream Binance, dù chưa bật giao dịch | 🟡 P2 | 2026-09-08 | User gửi log thật: 17 giây sau UI-ready, không có dòng click nào ở giữa, app đã sync ETHUSDT rồi mở websocket. Không phải `BUG-104` tái phát — `BUG-104` chỉ bịt đường route-restore lúc boot; đây là phần `BUG-104` cố tình để nguyên: thiết kế gốc `EPIC-021I` là "mở màn Giao dịch ra là live luôn", nên bản thân cú click sidebar thật vẫn đủ kích hoạt. Sửa: `ChartCoordinator.start()` thêm `go_live: bool = False` — mặc định chỉ đọc lịch sử local (`GetHistoricalKlinesQuery`), không network; `TradingPresenter` đọc config `TRADING_CHART_AUTOSTART_ENABLED` (mặc định tắt, cùng khuôn `DEV_BOARD_AUTOSTART_ENABLED`/`BOT-062`); hành động thật sự bật live là `EnableTradingCommand` thành công (`_go_live_if_not_already()`), vì bật giao dịch mà không có giá live là giao dịch mù. Lỗi tự bắt trước khi commit: bản nháp đầu đi qua `_restart_chart()` khiến promotion-lần-đầu gọi `stop()` không cần thiết (có thể giết luôn stream Dev Board đang chạy, vì `stop_stream()` không mang định danh người gọi) — phát hiện bằng chính test đầu viết cho path này, sửa bằng cách tách hẳn `_go_live_if_not_already()` khỏi `_restart_chart()`. 12 test mới (4 `ChartCoordinator`, 8 `TradingPresenter`), 2 quyết định dễ sai nhất mutation-verify thật (mặc định tắt; không `stop()` một stream mình chưa từng mở). Cố tình chưa gộp việc thiết kế lại quyền sở hữu stream dùng chung (refcount theo màn/symbol, giới hạn kiến trúc `EPIC-021I` đã tự ghi nhận) — tách task riêng qua `spawn_task`. |
| **[BUG-106](completed/BUG-106_start_live_syncs_the_whole_picked_date_range_instead_of_the_delta.md)** | Dev Board "Start Live" đồng bộ lại toàn bộ Data Range đã chọn từ Binance thay vì chỉ phần chênh lệch | 🟡 P2 | 2026-09-03 | User báo trực tiếp: bấm Start Live, thanh tiến độ đứng nhiều phút "Đang đồng bộ ETHUSDT 1s (246,000/604,800 nến)" — khớp đúng 7 ngày × 86400s (`DEFAULT_LOOKBACK_DAYS=7` × interval `1s` mặc định tài khoản), chart chỉ hiện 1 nến méo trong lúc đó. Root cause: `_run_sync_and_start()` truyền thẳng `start_time`/`end_time` của ô Data Range (vốn chỉ dùng để giới hạn hiển thị) vào `SyncMarketDataCommand` — bước đồng bộ **mạng thật**, bỏ qua hẳn nhánh `_determine_start_time()` đã có sẵn để chỉ đồng bộ chênh lệch từ nến local mới nhất. Sửa: gọi sync với `start_time=None, end_time=None` luôn — `_run_load_history()` (đọc DB local vẽ chart) giữ nguyên dùng đúng range user chọn. 2 regression test mới xác nhận đỏ đúng lý do trước khi sửa; 107/107 test liên quan xanh sau. Đánh số **106** (không phải 105) — `BUG-105` đã bị lấy trước lúc merge bởi PR khác (candle/volume zoom scale mismatch). |
| **[BUG-105](completed/BUG-105_candle_volume_zoom_scale_mismatch_at_extreme_zoom.md)** | Zoom vào rất sâu: trục Y nến đứng hình trong khi volume vẫn co giãn, lệch tỉ lệ giữa 2 khu vực chart | 🟡 P3 | 2026-09-03 | `FastCandlestickItem.dataBounds(ax=1, orthoRange=...)` tìm cửa sổ nến hiển thị qua `visible_slice_indices()` **thiếu padding** mà path vẽ thật (`_visible_history_slice()`) luôn áp — ở mức zoom đủ sâu, `orthoRange` lọt vào khoảng trống giữa 2 nến, lookup không padding trả rỗng nên rơi vào fallback biên độ toàn bộ lịch sử và đứng hình vĩnh viễn từ đó, trong khi `VolumeItem` đã padding sẵn nên vẫn co giãn đúng. Thêm đúng dòng `padding = candle_width * _VISIBLE_PADDING_WIDTHS` còn thiếu; đồng thời gộp hằng số padding của 2 renderer vào một `DEFAULT_VISIBLE_PADDING_WIDTHS` chung trong `viewport_windowing.py` (theo yêu cầu user) thay vì mỗi file tự khai literal riêng dễ lệch nhau. 2 test mới, xác nhận đỏ đúng lý do bằng mutation trước khi sửa. Đánh số **105** (không phải 104) — `BUG-104` đã bị lấy trước lúc merge bởi PR khác (boot auto-navigate). |
| **[BUG-104](completed/BUG-104_boot_auto_navigates_into_a_screen_that_starts_live_work.md)** | Mở app lên tự chạy live stream — không bấm gì cả | 🟡 P2 | 2026-09-03 | User báo ngay sau `BUG-101`: mở app lên là màn Giao dịch tự sync + live stream. Root cause là **tương tác giữa 2 tính năng** khác nhau, không phải 1 lỗi đơn: `main_window.py` (EPIC-010C, đã test kỹ) nhớ `last_route` và tự `switch_screen()` vào đó lúc boot; `TradingPresenter.__init__` (EPIC-021I) thiết kế cố ý "mở màn ra là live luôn", không có bước Start riêng. Ghép lại: phiên trước ở Trading → phiên sau chỉ cần mở app, không bấm gì, cũng đủ chạy job thật. User chọn hướng sửa: bỏ hẳn nhớ route lúc boot — mọi lần khởi động luôn vào đúng `dashboard` (route mặc định), giữ nguyên nhớ geometry/sidebar (cosmetic, vô hại). `TradingPresenter` giữ nguyên thiết kế, chỉ chạy khi user tự tay bấm vào Trading. 4 test sửa lại theo hợp đồng mới (1 xoá last_route khỏi capture/restore hẳn), xác nhận đỏ đúng lý do trước khi sửa; 5/5 xanh sau. Đánh số **104** (không phải 103) — `BUG-103` đã bị lấy trước lúc merge bởi PR khác (null-vm TypeError trên teardown). |
| **[BUG-103](completed/BUG-103_positions_openorders_kline_tradelog_null_vm_typeerror_on_teardown.md)** | `PositionsTable`/`OpenOrdersTable`/`KlineInspectorTable`/`TradeLogTable` quăng `TypeError` khi ViewModel bị huỷ lúc thoát app | 🟡 P3 | 2026-09-03 | Log thật user gửi lúc thoát app, 4 dòng `TypeError: Cannot read property 'rows' of null` ngay sau `"App stopped."`. Đúng cơ chế `BUG-069` (đã đóng cho `DatabaseStatusTable.qml`) nhưng chưa lan sang 4 file khác dùng chung `DataTable.qml` — `vm` là `QObject` con của Panel, Qt Quick tự null-hoá context property khi nó bị huỷ trước QML engine lúc teardown, mọi binding `vm.rows` còn sống ném `TypeError`. Điều tra mở rộng lộ thêm 2 file cùng lỗi chưa từng xuất hiện trong log phiên này: `KlineInspectorTable.qml` (dialog thật, mở được từ Data Management) và `TradeLogTable.qml` (hiện chưa nối vào màn hình sản xuất nào, sửa phòng ngừa). Sửa cả 4 theo đúng pattern null-safe guard `vm ? vm.x : ...` của `BUG-069`. 4 regression test mới (mô phỏng đúng thời điểm teardown bằng `setContextProperty("vm", None)`), xác nhận đỏ đúng lý do (khớp nguyên văn log user gửi) trước khi sửa. |
| **[BUG-102](completed/BUG-102_overlay_dialog_surface_background_never_paints.md)** | Mọi dialog `Overlay`/`QmlOverlay` (TimeframePicker, SymbolPicker, TimezonePicker, …) nền SURFACE chưa từng thực sự vẽ — thấy xuyên qua màn hình chính | 🟡 P2 | 2026-09-03 | User chụp ảnh app thật trên Wayland: mở `TimeframePicker` thấy chữ màn hình chính đè lên grid, không đọc được. Root cause: `Overlay(QDialog)` tự style bằng `apply_role(self, StyleRole.SURFACE)`, nhưng `QDialog` không phải `QFrame` (khác `Surface`/`Panel`/`Card`) — theo tài liệu Qt, một `QWidget` trần cần `Qt::WA_StyledBackground` thì stylesheet nền mới thực sự vẽ; cờ đó chưa từng được set. Với dialog QtWidgets thuần gần như vô hình (Qt vẫn tô nền mặc định từ palette); với `QmlOverlay` (thân QML dùng `setClearColor(transparent)` có chủ đích, giả định nền `Overlay` đã đặc để lộ ra phía sau) thì lộ rõ thành trong suốt thật. Sửa: 1 dòng `self.setAttribute(Qt.WidgetAttribute.WA_StyledBackground, True)` trong `Overlay.__init__`. 1 regression test mới xác nhận đỏ đúng lý do trước khi sửa; 289/289 test kit/overlay xanh sau. |
| **[BUG-101](completed/BUG-101_backtest_restore_runs_a_live_chart_preview_on_boot.md)** | Restore form Backtest lúc app boot chạy thẳng một chart-preview query thật (200,000 dòng `1s`, ~7.5s) trước khi user chạm vào gì | 🟠 P2 | 2026-09-03 | `state_persistence.py::restore()` tự hứa "opening the screen still runs nothing" nhưng `BackTestPresenter._connect_ui_signals()` nối `selectedSymbolChanged`/`selectedTimeframeChanged`/`timeRangePresetChanged`/`customStart|EndTextChanged` tới các handler gọi `_request_chart_preview()` **trước** khi `restore_into()` chạy — restore một form nhớ từ phiên trước áp qua đúng những setter đó, y hệt user gõ tay, bắn thẳng `GetHistoricalKlinesQuery`/`GetBacktestRangeCoverageQuery` thật. `main_window.py` (route/geometry/sidebar), `dashboard_presenter.py`, `data_management_presenter.py` đã xác minh restore đúng (không chạm). Sửa: cờ `self._restoring_state` quanh `restore_into()`, chặn tại 1 điểm hội tụ duy nhất `_request_chart_preview()` — giá trị form vẫn restore đầy đủ, chỉ hành động mạng bị chặn trong lúc restore. 1 regression test mới xác nhận đỏ đúng lý do (`submit` gọi 2 lần) trước khi sửa; 314/314 test Backtest xanh sau. |
| **[BUG-084](completed/BUG-084_live_sizing_hardcode_chan_moi_lenh.md)** | Sizing hard-code 20%/1x khiến bot **không đặt nổi một lệnh nào** trên tài khoản có số dư thật | 🟠 P2 | 2026-09-02 | `live_trading_coordinator.py` hard-code `PERCENT_OF_EQUITY 20%` + leverage `1.0`; đặt cạnh hạn mức 500 USDT/lệnh thì cửa sổ dùng được chỉ còn ~500–2 500 USDT số dư — số dư mặc định testnet 15 000 nằm ngoài. Sửa: 2 `ConfigKeys` mới (`trading.live_sizing_percent`/`trading.live_leverage`, mặc định giữ nguyên 20.0/1.0) làm sizing thành control thật, đọc ở `binance_bot_module.py::boot()`. Nửa thứ hai — lý do bị chặn chỉ nằm ở một dòng log, không nơi nào trên màn hình phân biệt được "không có tín hiệu" với "có tín hiệu nhưng bị chặn" — sửa bằng domain event mới `LiveOrderBlockedEvent`, publish qua `IEventPublisher`, `OrderFeed` thêm signal thứ tư `orderBlocked`, `TradingPresenter` ghi vào `log_model` của chính màn Giao dịch ở mức `info`. 8 test mới (2 unit files + `OrderFeed` + `TradingPresenter`). |
| **[BUG-100](completed/BUG-100_equity_chart_seed_before_subscribe_misses_sample.md)** | Biểu đồ equity đọc seed trước khi subscribe `EquityFeed` — mẫu ghi đúng lúc đó bị mất | 🟡 P2 | 2026-09-03 | `TradingPresenter.__init__` đọc `render_historical_data(...)` **trước** khi subscribe `EquityFeed` — một sample ghi vào `EquityCurveRecorder` đúng giữa hai điểm đó không nằm trong snapshot lẫn live subscription, mất tới lần mở màn kế tiếp. Đổi thứ tự: subscribe trước, đọc seed sau — an toàn hơn hướng ngược lại vì rủi ro còn lại (duplicate nếu đảo hướng) đã có sẵn guard dedup theo timestamp ở `ChartCard.append_closed_candle()`, còn rủi ro "miss" thì không tự chữa được. |
| **[BUG-099](completed/BUG-099_error_translator_missing_realistic_codes.md)** | 5 mã lỗi Binance thực tế (rate limit, precision, quantity, vượt vị thế theo đòn bẩy, percent-price filter) rơi vào `UNKNOWN` thay vì được phân loại | 🟡 P2 | 2026-09-03 | Thêm `-1015`/`-1111`/`-4003`/`-2027`/`-4131` vào `_UNAMBIGUOUS_CODE_TO_REASON` của `binance_error_translator.py`, theo tài liệu mã lỗi Futures API. 5 test case parametrize mới. |
| **[BUG-098](completed/BUG-098_stale_metadata_cache_never_refreshes.md)** | `FuturesMetadataProvider.get_or_fetch()` không bao giờ coi cache là cũ — `is_stale()` viết ra nhưng chết, không ai gọi | 🟡 P2 | 2026-09-03 | Điều kiện `if cached is not None:` thiếu vế `and not cached.is_stale()`; symbol filter (`minNotional`/`lotSize`/`priceFilter`) có thể lệch khỏi sàn vô thời hạn một khi đã cache lần đầu. Thêm vế điều kiện, 1 test mới xác nhận cache stale ép refresh mạng thật. |
| **[BUG-097](completed/BUG-097_secrets_file_world_readable_permissions.md)** | `secrets.local.json` ghi ra mà không khoá quyền file — API key/secret sàn world-readable trên hệ multi-user | 🟡 P2 | 2026-09-03 | `SecretsFileSource.write()` thêm `os.chmod(self._filepath, 0o600)` sau `json.dump`, bọc try/except OSError (log warning, không raise). Test xác nhận mode file đúng `0o600` (skip trên Windows). |
| **[BUG-096](completed/BUG-096_user_data_stream_reconnect_visibility.md)** | Reconnect của User Data Stream gần như vô hình: sentinel lỗi của `python-binance` bị bỏ qua, `ReadLoopClosed` không được coi là lỗi cần reconnect | 🟠 P2 | 2026-09-03 | Đọc trực tiếp source `python-binance` đã cài để xác minh (egress `*.binance.*` bị chặn trong sandbox): `_read_loop()` đẩy sentinel `{"e": "error", ...}` qua cùng queue `.recv()` đọc — không nhánh nào xử lý, bị bỏ im lặng; sau `MAX_RECONNECTS=5`, `.recv()` raise `ReadLoopClosed` (`Exception` trần, không phải `OSError`) — bay thẳng ra khỏi `except OSError`. Sửa: bắt thêm `ReadLoopClosed`, thêm nhánh log `WARNING` cho sentinel `"error"`, sửa docstring module nói sai renewal log ở INFO. 2 test mới (sentinel logged, `ReadLoopClosed` tự phục hồi không crash). |
| **[BUG-095](completed/BUG-095_account_update_info_log_regresses_bug042.md)** | 4 dòng log `ACCOUNT_UPDATE`/`ORDER_TRADE_UPDATE` per-event ở mức INFO — tái diễn đúng lớp lỗi `BUG-042` | 🟡 P2 | 2026-09-03 | 4 `logger.info(...)` per-message trong `futures_user_data_stream.py` đổi thành `logger.debug(...)` theo `logging-rule.md` Rule 4 & 6 (`INFO` là mức `SignalLogHandler` UI thật sự vẽ lên `LogListModel`, tần suất cao per-event ở đó chính là cơ chế đã đơ UI thread ở `BUG-042`). 3 test mới xác nhận mức log qua `caplog`. |
| **[BUG-094](completed/BUG-094_user_data_stream_stop_start_race.md)** | `stop()` rồi `start()` nhanh có thể để coroutine `_run_stream()` cũ tiếp tục xử lý message sau khi stream mới đã chạy | 🟠 P2 | 2026-09-03 | TOCTOU cùng lớp `BUG-088` ở tầng infrastructure: `_run_stream()` chỉ biết "có bị huỷ chưa", không biết "mình có còn là instance hợp lệ duy nhất". Thêm `self._generation` tăng ở `start()`/`stop()`, `_run_stream()` nhận `generation` tường minh, cả vòng lặp lẫn xử lý message đều kiểm tra khớp `self._generation` hiện tại. 1 test async mới (kèm cap an toàn chống treo nếu regress) + sửa call site `scripts/epic021h_user_stream_probe.py`. |
| **[BUG-093](completed/BUG-093_emergency_stop_leaves_trading_screen_stale.md)** | Emergency Stop xong nhưng bảng Vị thế/Lệnh mở trên màn Giao dịch không được làm mới | 🔴 P1 | 2026-09-03 | `EmergencyStopResult` không mang bản chụp trạng thái đọc lại thật sau khi 3 bước (huỷ lệnh, đóng vị thế, tắt session) chạy xong. Thêm `final_positions`/`final_open_orders`/`final_state_confirmed` (rỗng-có-xác-nhận khác rỗng-không-xác-nhận-được, không suy ra lẫn nhau) + `_read_final_state()` đọc lại `get_positions()`/`get_open_orders()` thật; `TradingPresenter` thay thế bảng khi xác nhận được, cảnh báo và giữ nguyên khi không đọc lại được. |
| **[BUG-092](completed/BUG-092_equity_sample_undercounts_upnl_multi_position.md)** | Equity sample chỉ lấy uPnL của một vị thế trong `ACCOUNT_UPDATE`, không cộng dồn — sai số khi mở ≥2 vị thế | 🟠 P2 | 2026-09-03 | `account_update_equity_sample()` (cũ) đọc uPnL từ phần tử đầu tiên của danh sách vị thế **thay đổi trong event đó**, bỏ qua uPnL của các vị thế khác đang mở nhưng không xuất hiện trong event. Thêm state `self._unrealized_pnl_by_symbol` cộng dồn theo symbol, `EquitySample.unrealized_pnl = sum(...)` trên toàn bộ state đã biết, reset ở `start()`. 2 test mới. |
| **[BUG-091](completed/BUG-091_unmapped_order_status_type_silently_dropped.md)** | Order status/type lạ từ sàn làm parser raise `KeyError`, mất trắng cập nhật thay vì suy giảm an toàn | 🟠 P2 | 2026-09-03 | `OrderStatus[raw]`/`OrderType[raw]`/`TimeInForce(raw)` trần — một giá trị lạ crash toàn bộ `_handle_message()`. Thêm `OrderStatus.UNKNOWN`/`OrderType.UNKNOWN` (theo đúng idiom `OrderRejectionReason.UNKNOWN` đã có), 3 helper dùng chung `order_enum_parsing.py` không bao giờ raise. 4 test mới. |
| **[BUG-090](completed/BUG-090_min_notional_not_enforced_before_live_order.md)** | `minNotional` không được kiểm tra trước khi gửi lệnh thật; exchange reject/network lỗi có thể crash CLI/coordinator | 🔴 P1 | 2026-09-03 | `ExecuteOrderCommandHandler` tính `preview.notional_check` nhưng chỉ dùng để hiển thị, không chặn gửi lệnh; `trade_once_cmd.py`/`live_trading_coordinator.py` dispatch trần, không try/except cho lỗi sàn/mạng. Thêm `ExecuteOrderNotionalRejection.MIN_NOTIONAL` chặn trước khi gọi mạng; bọc try/except ở cả CLI và coordinator (`OrderRejectedByExchangeError` cụ thể, `Exception` rộng có chủ đích cho phần còn lại — `architecture-rule.md` §3 cấm import exception SDK hạ tầng ở tầng application). 4 test mới. |
| **[BUG-089](completed/BUG-089_emergency_stop_shares_toggle_action_tracker.md)** | Emergency Stop dùng chung `ActionOwnershipTracker` với nút bật/tắt giao dịch — click chồng lấp bị hiểu sai | 🔴 P1 | 2026-09-03 | Hai hành động UI độc lập (toggle, Emergency Stop) chia sẻ một tracker một-slot — click chồng lấp giữa hai hành động bị hiểu nhầm thành "cùng một hành động bấm lại" thay vì "hành động khác đang chạy". Tách `self._emergency_stop_tracker` riêng; mỗi hành động kiểm tra tracker của hành động kia trước khi cho phép chạy. 2 test mới. |
| **[BUG-088](completed/BUG-088_trading_session_state_race_enable_vs_emergency_stop.md)** | `TradingSessionState` không khoá: Enable Trading và Emergency Stop chạy đồng thời có thể ghi đè lẫn nhau | 🔴 P1 | 2026-09-03 | TOCTOU: `EnableTradingCommandHandler` đối soát qua 2 lệnh gọi mạng rồi `enable(...)` không kiểm tra state có bị đổi xen giữa (vd. Emergency Stop chạy trong lúc đó) hay không. Thêm `threading.RLock` + `generation` tăng dần trên `TradingSessionState`; `enable(..., expected_generation=...)` từ chối áp dụng nếu generation đã đổi, trả `EnableTradingBlockReason.SUPERSEDED_BY_CONCURRENT_STATE_CHANGE`. 5 test mới. |
| **[BUG-085](completed/BUG-085_live_tick_khong_loc_theo_interval.md)** | Đường live không lọc theo khung thời gian — hai interval cùng symbol làm hỏng state chỉ báo | 🟠 P2 | 2026-09-02 | `MarketTickEventHandler.handle()` lọc **duy nhất** `md.symbol`; `MarketData.interval` bị bỏ qua, khiến hai interval cùng symbol (vd. `1m`/`5m`) đổ chung vào một `StrategyEngine` và làm hỏng state chỉ báo giữ tăng dần theo nến (`BOT-042B/C`). Sửa: `ConfigKeys.TRADING_LIVE_INTERVAL` mới (rỗng = "chưa cấu hình", không đoán khung), `MarketTickEventHandler` lọc thêm `md.interval != live_interval` cạnh bộ lọc symbol có sẵn, `binance_bot_module.py::boot()` chỉ dựng engine khi cả ba `live_symbol`/`live_strategy_key`/`live_interval` đều có. 3 regression test mới (xen kẽ `1m`/`5m`, interval khác nhau, interval rỗng) viết trước và xác nhận đỏ đúng lý do; 9/9 test xanh sau sửa. UI chọn khung thời gian cho đường live để ngoài phạm vi — thuộc `EPIC-021I` §3.3 điểm 4. |
| **[BUG-087](completed/BUG-087_empty_vault_placeholder_hides_unscanned_local_data.md)** | Placeholder nói "Storage Vault trống" dù đĩa có 2 shard thật chưa quét | 🟡 P2 | 2026-09-03 | Tác dụng phụ của `BOT-120`: badge "N shards" và placeholder giữa bảng đều đọc `vm.rowCount` (số dòng đã quét) như thể nó là "số shard trên đĩa" — đúng tình cờ trước `BOT-120` (auto-discover từng tự quét và đổ dữ liệu thật vào bảng), sai sau đó (`rowCount` giờ ở lại 0 tới khi user chủ động quét). Log dev-mode xác nhận trực tiếp: `[storage-vault] 2 local shard(s) on disk` cùng lúc UI hiện "Storage Vault trống". Sửa: thêm sự thật độc lập `knownShardCount` (từ `list_available_shards()`, glob thư mục thuần tuý) xuyên suốt `ScanCoordinator` → `DataManagementPresenter` → `DataManagementViewModel` → `DatabaseStatusPanel`/`DatabaseStatusVM` → QML placeholder; badge giữ nguyên vì nó đúng nghĩa "số dòng đang hiện". 6 test mới/sửa, 113/113 test liên quan xanh. Đánh số **087** (không phải 083) — `BUG-083` đã bị lấy trước lúc merge bởi PR khác (`test_pan_preview_moves_only_the_data_region_not_the_axes`). |
| **[BUG-086](completed/BUG-086_position_changed_event_khong_ban_khi_dong_vi_the.md)** | `PositionChangedEvent` không bắn khi một vị thế đóng về flat — bảng Vị thế trên màn Giao dịch có thể "sống" sau khi vị thế đã chết | 🟠 P2 | 2026-09-02 | `futures_user_data_stream.py::_handle_account_update()`'s nhánh vị thế đóng về 0 giờ `emit`s một `PositionClosedEvent` riêng (không tái dùng `PositionChangedEvent` với `position_amt=0` giả — `LivePosition`'s docstring cấm thẳng "zero never appears here"). `OrderFeed` thêm signal `positionClosed` thứ ba; `TradingPresenter._on_position_closed()` gỡ đúng dòng khỏi bảng. Regression test viết đúng theo yêu cầu §4.2 của hồ sơ: `MemoryEventBus` thật, không mock, không cần mạng. Phát hiện khi dựng `EPIC-021I`, đóng ngay sau `EPIC-021M`. |
| **[BUG-083](completed/BUG-083_pan_preview_test_drags_past_its_own_reanchor_boundary.md)** | Cổng CI bắt buộc đỏ vĩnh viễn: `test_pan_preview_moves_only_the_data_region_not_the_axes` kéo quá ngưỡng re-anchor của chính nó | 🟠 P2 | 2026-09-02 | Không phải lỗi sản phẩm — sản phẩm đúng ở cả hai nhánh. Test sai **hai** chỗ độc lập: (1) kéo `100.0px` trong khi ngưỡng re-anchor đo được là `56.075px` (`min(1121.5 × 0.05, 96.0)`), nên assertion cuối đo trên khung **đã re-render** chứ không phải preview thuần như docstring tự khai; (2) lấy mẫu trục thời gian chỉ **một hàng ngang** tại `center().y()`, rơi vào khoảng trống giữa vạch chia và nhãn — đo toàn dải cho thấy trục **có** đổi thật (3.774/124.850 pixel). Sửa: 2 literal `100.0` → `_SHORT_PAN_PIXELS` (hằng số file đã có sẵn cho đúng việc này), **0 dòng code sản phẩm**. Chứng minh không làm yếu test theo `fix-bug-rule.md` §4 bằng đột biến, không phải khẳng định suông: gieo lại đúng lỗi `BUG-009` (bỏ 2 `setClipRect` trong `_paint_transformed_region`) → bản 40px đỏ đúng assertion của `BUG-009`; bản 100px không còn chạm đường lỗi đó vì re-anchor cắt ngang. Đỏ từ lúc sinh ra (`0c75a7a`, PR #140, 2026-08-27); 10/13 file task EPIC-021 đã ghi "không liên quan" — nay trỏ về hồ sơ này. Cổng bắt buộc xanh hoàn toàn lần đầu kể từ 2026-08-27. |
| **[BUG-082](completed/BUG-082_shared_qml_widget_library_depends_on_screen_modules.md)** | Thư viện widget dùng chung `ui/qml/` phụ thuộc ngược vào `ui/screens/` | 🟡 P3 | 2026-09-01 | `qml/StatCardRow/stat_card_row_widget.py:25` phát biểu bằng văn bản *"`qml/` must not depend on `screens/`"*, nhưng 4 file production trong `qml/` (+5 hit ở `preview`/`tests`) đang import từ `screens.backtest.logic` và `screens.data_management`. Sửa ở `EPIC-021L`: viết guard `ast` trước (đỏ 11 file thật), `git mv` 5 module về đúng thư mục widget sở hữu chúng (`qml/TradeLogTable`/`MetricsDetailPanel`/`DatabaseStatusTable`/`KlineInspectorTable`), sửa ~27 điểm import, guard xanh sau đó. Không sửa assert nào, không đổi hành vi — `tests/unit/presentation/ui/{qml,screens}/` 922/922 xanh. |
| **[BUG-081](completed/BUG-081_binance_endpoint_config_keys_are_dead.md)** | `BINANCE_REST_URL`/`BINANCE_WS_URL` là config chết — sửa chúng không đổi được gì | 🟡 P3 | 2026-09-01 | Xoá 2 key chết khỏi `ConfigKeys`/`app_config.json`, thay bằng `EXCHANGE_MARKET_DATA_VENUE` đọc thật bởi `resolve_market_data_venue()`, nối vào composition root qua `ExchangeSessionFactory` (nơi duy nhất được gọi `Client(...)`, khoá bằng AST guard). Regression test viết trước, xác nhận fail đúng lý do trước fix. Sửa ở `EPIC-021A`. |
| **[BUG-080](completed/BUG-080_settings_api_credentials_never_reach_the_exchange_client.md)** | API Key/Secret nhập ở màn Settings không bao giờ tới exchange client | 🟡 P3 (hạ từ P2) | 2026-09-01 | UI hứa key sẽ được dùng để ký request; không có gì tiêu thụ nó. Sửa 2 giai đoạn: `EPIC-021B` dời secret khỏi `user_config.json` (git-tracked) sang `secrets.local.json` (gitignored) qua `IExchangeCredentialsProvider`; `EPIC-021D` đóng nốt — `ExchangeSessionFactory.create_trading_client(credentials)` (mới) dựng client **có key**, `FuturesAccountReader` dùng nó ký 4 request thật (`futures_ping`/`futures_time`/`futures_account`/`futures_get_position_mode`) qua nút "Kiểm tra kết nối"/`main.py exchange-status`. Xác nhận bằng round-trip HTTP thật qua fake server + chạy CLI thật với key giả (request thật sự gửi đi, bị egress-block chặn ở tầng mạng, không phải "chưa từng thử"). Đánh số **080** (không phải 078) — 078/079 đã bị `BUG-078`/`BUG-079` (phantom shard, trend-zone) lấy trước lúc merge. |
| **[BUG-078](completed/BUG-078_phantom_shard_creation_from_read_paths.md)** | Đọc dữ liệu (không ghi) tự tạo shard rỗng qua `get_session()`'s create-on-first-use, làm auto-discover quét 1350×6 = 8100 kết nối (~79s) lúc boot dù chỉ 9 bảng có dữ liệu thật | 🟡 P2 | 2026-09-01 | Root cause: `SyncMarketDataCommandHandler._determine_start_time()` gọi `get_latest_kline_time()` cho mọi symbol trước khi sync thật, để lại shard rỗng vĩnh viễn cho symbol chưa từng có dữ liệu. Fix 3 phần: (a) `DatabaseManager.has_shard()` chặn mọi method đọc tạo shard, (b) scan đổi đơn vị từ (symbol,interval) sang symbol — 1 session/symbol thay vì 1/cặp (đo được: 3 interval mở đúng 1 session thay vì 3), (c) `PruneEmptyShardsCommand` tự dọn shard rỗng (0 nến ở **mọi** interval, không chỉ 6 mặc định) mỗi lần auto-discover chạy. Cổng CI thật đã chạy xanh: ruff/mypy sạch, sanity 24/24, main (unit+integration) 2945/2945 pass, coverage 95%. Bắt được 1 regression thật khi chạy CI (`shutdown_database_scan_probe.py` mock tên method port cũ) và đã sửa; 1 lần đỏ khác (`test_ui_state_coordinator.py`) đã A/B bằng `git stash` xác nhận là flake môi trường có sẵn (đúng vậy — root cause thật đã được sửa riêng ở `BUG-077` phía dưới, merge vào lúc phiên này đang chạy). Phát hiện phụ: `.gitignore` có dòng `database/` không neo, nuốt nhầm 2 thư mục source — đã sửa. Đổi số từ `BUG-077` gốc vì trùng với 2 bug khác merge cùng lúc — xem §8 hồ sơ. |
| **[BUG-070](completed/BUG-070_symbol_picker_popup_keys_attached_property_warning.md)** | SymbolPicker QML cảnh báo "Could not attach Keys property to: Popup ... is not an Item" | ⚪ **P4** | 2026-08-30 | Di chuyển `Keys.onPressed` từ `pickerPopup` (một `Popup`, không phải `Item`) sang `contentItem: ColumnLayout` (một `Item` thật) trong `SymbolPicker.qml` — cùng hành vi, chỉ khác chỗ đặt. |
| **[BUG-077](completed/BUG-077_ui_state_coordinator_coarse_timer_remaining_time_flake.md)** | `test_marking_again_restarts_the_window...` đỏ ngẫu nhiên dưới tải máy (`ci-local.ps1 -Full`, `-n 6`) | 🟡 P2 | 2026-09-01 | `UiStateCoordinator`'s debounce `QTimer` dùng mặc định `Qt.TimerType.CoarseTimer` — được phép làm tròn deadline lên tới ~5%, nên `remainingTime()` đọc được có thể vượt `debounce_ms` yêu cầu dù đã chờ đủ thời gian thật (khớp đúng `assert 5059 < 5000`/`5036 < 5000` quan sát được). Tái hiện tại chỗ bằng tải CPU giả (~65%/14 lần); sửa bằng `setTimerType(Qt.TimerType.PreciseTimer)` — 0/20 lần đỏ dưới cùng tải sau fix. |
| **[BUG-076](completed/BUG-076_database_status_table_timestamp_overlap.md)** | Cột FIRST RECORD/LAST RECORD trên Database Status Table chồng chéo chữ, không đọc được | 🟡 P2 | 2026-09-01 | `database_status_table_model.py` lưu `first_record`/`last_record` bằng `str(datetime)` (~32 ký tự, có microseconds+tz) nhưng `DatabaseStatusRow.qml`'s 2 `Text` hiển thị chúng không có `elide` — tràn thẳng đè lên cột bên cạnh. Sửa: thêm `elide: Text.ElideRight` + `Layout.minimumWidth: 0` (bắt buộc đi cùng `elide` trong `RowLayout`, nếu không minimum width mặc định lấy từ độ rộng chuỗi chưa cắt). Ảnh chụp user gửi kèm còn nghi 1 chỗ chồng chéo thứ 2 ở progress bar — đã điều tra và **loại trừ** bằng regression test (pass ngay cả khi chưa sửa), không sửa theo giả thuyết chưa xác minh. |
| **[BUG-079](completed/BUG-079_choppy_trend_zone_flips_render_as_striped_black_band.md)** | Chiến lược "Long Term Trend Zone": nền tô xu hướng trong đoạn sideways render thành dải sọc gần đen, đè lên nến | 🟡 P2 | 2026-09-01 | User báo qua ảnh chụp ứng dụng thật, mô tả "mũi tên long/short khó nhìn, vị trí chưa hợp lý". Root cause thật là `compute_strategy_trend_zones()` không có ngưỡng tối thiểu — thị trường đi ngang khiến `classify_trend_zone()` đảo chiều gần mỗi nến, tạo hàng chục span 1-nến (nhiều span rộng 0 giây) xanh/đỏ xen kẽ đứng sát nhau, đọc như khối đen đặc. Tái hiện qua đúng code path production (BackTestView/RegionLayer thật), không phải đoán — loại trừ giả thuyết marker Buy/Sell Flags (đo trực tiếp: marker đơn render đúng 11×9px, LOD nén marker dày đặc thành item riêng biệt, không phải nguyên nhân). Fix: thêm `_MIN_ZONE_BARS = 3`, bỏ hẳn zone ngắn hơn 3 nến thay vì vẽ riêng — 92 span còn 40, 44 span rộng-0 còn 0. **(Đổi số 2 lần: BUG-077 → BUG-078 (2026-09-01, trùng `BUG-077_ui_state_coordinator_coarse_timer_remaining_time_flake.md`) → BUG-079 (2026-09-01, trùng tiếp `BUG-078_phantom_shard_creation_from_read_paths.md` merge trong lúc PR đổi số lần 1 còn mở review) — cùng tiền lệ "PR merge sau thì đổi số" ghi ở đầu file.)** |
| **[BUG-072](completed/BUG-072_intermittent_segfault_tests_integration_worker_load_history.md)** | Segfault không ổn định trong `tests/integration/`, worker thread mid-`_run_load_history` | 🔴 **P1** | 2026-08-31 | `ChartPreviewCoordinator.run_preview()` emit `coverage` chưa unwrap qua `_previewDataReadySignal = Signal(int, object, list, list, list)` — trong test suite, `mock_dispatch` bọc mọi query (kể cả `GetBacktestRangeCoverageQuery`) trong `_FakeResponse`, và dòng `coverage` (khác `raw_klines` 2 dòng trên) thiếu unwrap `.data`, khiến `_FakeResponse` (không metatype Qt nào biết) bay thẳng qua signal cross-thread — khớp đúng dòng `_pythonToCppCopy: Cannot copy-convert ... (_FakeResponse) to C++` log trước lúc crash. Sửa: áp cùng `getattr(response, "data", response)` cho `coverage`, và `mock_dispatch` trả `BacktestRangeCoverage` thật thay vì rơi vào nhánh `else: response.data = []`. |
| **[BUG-075](completed/BUG-075_sanity_tier_unclosed_asyncio_event_loop_from_python_binance.md)** | `tests/sanity/` fail ngẫu nhiên trên bystander test vì `ResourceWarning: unclosed event loop` | 🟡 P2 | 2026-08-31 | `python-binance`'s `helpers.py::get_loop()` tạo `asyncio.new_event_loop()` khi thread hiện tại chưa có loop và không bao giờ đóng nó — cùng cơ chế với `DeprecationWarning` đã allowlist sẵn ("There is no current event loop"), chỉ khác 2 triệu chứng khác nhau của cùng 1 root cause. GC thu hồi loop mồ côi này vào thời điểm không xác định, rơi trúng bất kỳ sanity test nào đang chạy lúc đó (`test_circular_imports.py` — chỉ `ast.parse()`, không liên quan gì). Không sửa được trong app/engine (nội bộ thư viện thứ 3) — thêm allowlist entry `"unclosed event loop"` kèm lý do bằng văn bản, đúng hợp đồng file đã có sẵn. |
| **[BUG-073](completed/BUG-073_backtest_preview_unbounded_tick_coverage_hangs_shutdown.md)** | Backtest chart-preview probe bắn `GetBacktestRangeCoverageQuery` không giới hạn ở tick mode, treo tiến trình lúc thoát app | 🟠 **P1** | 2026-08-31 | `ChartPreviewCoordinator.request_preview()` bắn tự động lúc mở màn Backtest, trước khi toolbar's time-range preset ổn định — nếu execution mode đã là `HISTORICAL_TICK` lúc đó, dispatch `GetBacktestRangeCoverageQuery(start_time=None, interval=1s)`, đúng hazard `TickModeRequiresBoundedRangeRule` đã biết (scan window-function không giới hạn) nhưng rule đó chỉ gác nút "Run Backtest", không gác đường preview này. Log dev-mode thật cho thấy 1 worker kẹt ~19s, vẫn chạy 2s SAU khi "App stopped." đã log. Sửa: `request_preview()` áp đúng điều kiện của rule đó (tick mode + start_time=None) để bỏ qua, không dispatch. |
| **[BUG-065](completed/BUG-065_state_coordinator_test_crashes_a_worker_under_full_parallel_load.md)** | CI thật crash native (`Segmentation fault`/`Fatal Python error: Aborted`) luôn tại `test_history_pagination_controller.py`, cùng cơ chế đã thấy ở `test_ui_state_coordinator.py` | 🔴 **P1** | 2026-08-30 | Bisect nhị phân bằng `pytest -q <id...>` xuống đúng 1 test (693/2806) chứng minh đây là crash **đơn luồng** (loại giả thuyết race 2 luồng ban đầu): `SymbolPickerOverlay` (widget top-level, không có Qt parent) có card nối `clicked`/`favourite_toggled` tới lambda đóng lại `self` — 1 chu trình tham chiếu Python chỉ cyclic GC phá được, và thứ tự finalize GC chọn (tuỳ ý) đụng độ với thứ tự Qt C++ parent-child mong đợi. `test_symbol_picker_overlay.py` đổi sang `qtbot.addWidget(dialog)` cho mọi dialog, buộc `close()`+`deleteLater()` chạy đúng lúc, xác định, thay vì để cyclic GC vấp phải sau này. |
| **[BUG-074](completed/BUG-074_log_viewer_enabled_by_default_leaks_tcp_worker_thread.md)** | `app_config.json`'s `log.viewer.enabled: true` rò 1 thread TCP nền (`Sagittarius-TcpLogWorker`) sống suốt phần còn lại của phiên CI đơn-tiến-trình | 🟡 P2 | 2026-08-31 | Đổi default về `false` (khớp default an toàn của chính `LoggerConfig` engine). Phát hiện phụ trong lúc điều tra `BUG-065`, xác nhận không phải nguyên nhân chính của crash đó. |
| **[BUG-071](completed/BUG-071_checkbox_list_stat_grid_root_width_parent_typeerror.md)** | CheckboxList/StatGrid QML quăng `TypeError: Cannot read property 'width' of null` mỗi lần mở dialog | 🟡 **P3** | 2026-08-31 | Xoá `width: parent.width` khỏi root item của `CheckboxList.qml` và `StatGrid.qml` — cả hai luôn được nạp làm root object của `QQuickWidget` (`SizeRootObjectToView`), thứ không bao giờ gán `parent` QML cho root, nên binding luôn đọc `null`. |
| **[BUG-067](completed/BUG-067_dashboard_sync_market_data_ignores_cancellation_hangs_shutdown.md)** | Dashboard Live Stream sync bỏ qua cancellation, thread kẹt làm treo tiến trình khi tắt app | 🔴 **P1** | 2026-08-30 | Truyền `cancellation_requested=token.is_cancelled` vào `SyncMarketDataCommand` trong `stream_lifecycle_controller.py`. Thêm `shutdown()` giải phóng `ExclusiveAction` và cancel token; thêm `close()` trên `PythonBinanceClient` để ngắt socket `requests.Session` khi engine shutdown (`BinanceBotModule.shutdown`). |
| **[BUG-069](completed/BUG-069_database_status_table_qml_typeerror_null_vm_on_teardown.md)** | DatabaseStatusTable QML quăng hàng loạt TypeError khi ViewModel bị huỷ lúc thoát app | 🟡 **P3** | 2026-08-30 | Thêm null-safe guard `(vm ? vm.rowCount : 0)`, `(vm ? vm.rowsModel : null)` và `Boolean(vm && vm.actionsEnabled)` trong `DatabaseStatusTable.qml` và `DatabaseStatusRow.qml`, triệt tiêu toàn bộ TypeError khi giải phóng ViewModel. |
| **[BUG-066](completed/BUG-066_dev_board_symbol_picker_freeze_on_large_symbol_list.md)** | Dev Board UI freeze >5s khi mở bộ chọn symbol với 1.358 cặp tiền | 🔴 **P1** | 2026-08-30 | Di chuyển Dev Board sang `SymbolPickerModal` (`SymbolPicker.qml` với virtualized `GridView(reuseItems: true)`). Lưu sẵn 1.358 symbols vào `src/config/tradeable_symbols.json` với `ISymbolCatalogRepository`, mở tức thì 0ms không gọi mạng; thêm nút 🔄 trên QML để refresh theo yêu cầu. |
| **[BUG-064](completed/BUG-064_strategy_properties_dialog_needs_save_button_to_commit_any_field.md)** | Dialog "Cài đặt Chiến lược" chỉ commit giá trị khi bấm nút Lưu, Enter/mất focus không có tác dụng | 🟡 P2 | 2026-08-27 | `QLineEdit` trong cả 2 tab không nối signal nào — `save_and_rerun()` là đường duy nhất đọc widget. Thêm `_wire_line_edits_to_save_on_focus_lost()` (nối `editingFinished` chung cho mọi ô, bắt cả Enter và mất focus) + guard chống gọi lặp. Tách luôn `BROKER_PROPERTY_FIELDS` (file mới `logic/broker_properties_schema.py`) làm nguồn khai báo DUY NHẤT cho 12 broker property, dùng chung Coordinator/Dialog — trước đó khai trùng ở `_BROKER_PROPERTIES` (coordinator) và rải rác trong `save_and_rerun()`/`_sync_properties()` (dialog), thêm property mới dễ quên 1 trong 3 chỗ. |
| **[BUG-063](completed/BUG-063_binance_sync_read_timeout_aborts_whole_sync.md)** | Đồng bộ dữ liệu 1 giây mất trắng nến đã tải khi Binance trả lời chậm | 🟡 P2 | 2026-08-27 | `PythonBinanceClient` không có retry/timeout cấu hình được — 1 `ReadTimeout` (mặc định 10s của `python-binance`) trong hàng trăm request tuần tự của 1 lần sync 1s nhiều ngày phá cả buffer chưa kịp lưu. Nâng timeout mặc định lên 30s, thêm `_generate_raw_klines_with_retry()` tự tiếp tục từ `close_time + 1ms` của nến cuối cùng nhận được (không tải lại từ đầu), backoff luỹ thừa tối đa 3 lần lỗi liên tiếp không tiến triển, huỷ được ngay cả giữa lúc chờ backoff. Phát hiện qua log thật user gửi lúc thử `VolumeSpikeFlowStrategy` ở chế độ Historical Tick — bug nằm ở tầng đồng bộ, không liên quan chiến lược. |
| **[BUG-062](completed/BUG-062_database_cancel_flow_test_is_flaky.md)** | `test_database_cancel_button_cancels_active_sync_flow` đỏ ngẫu nhiên khi chạy song song | 🟡 P2 | 2026-08-26 | **Root cause đo được:** `SyncMarketDataCommandHandler` lặp lên `stream_historical_klines`, nhưng fixture đặt độ trễ 0.3s lên `get_historical_klines` — method đường sync **không bao giờ gọi**. Sync xong tức thì, cửa sổ FSM ở `UIMode.SYNCING` chỉ **3.86ms** trong khi `qtbot.waitUntil` poll mỗi ~10ms → pass khi poll may mắn rơi trúng. Sửa: đưa độ trễ về đúng method đường sync dùng (khôi phục ý định gốc của fixture, **không** nới timeout). Cửa sổ: 3.86ms → **287ms**. Chạy 15 lần liên tiếp: 0 đỏ. Lỗi `'Mock' object is not iterable` gặp trước đó là khiếm khuyết **khác**, đã đóng riêng bằng `_FakeExchangeClient` — lần đỏ sau đó có 0 lỗi Mock mà vẫn timeout y hệt, đó là bằng chứng tách hai chuyện. |
| **[BUG-060](completed/BUG-060_leaked_threadpool_worker_survives_the_test_session.md)** | Một `ThreadPoolExecutor` worker non-daemon sống sót qua cả phiên test | ⚪ Đóng — không tái hiện được từ môi trường hiện có (trước: 🟡 P2) | 2026-08-26 | Chạy full suite thứ tự cố định: 3 test trong `test_app_bootstrapper_thread_diagnostic.py` đỏ vì `'ThreadPoolExecutor-44_0'` vẫn còn sống; chạy riêng file đó thì xanh. Diagnostic **báo đúng** — worker rò là có thật, **đúng lớp hazard `BUG-052`** (CPython `_python_exit()` join mọi worker bất kể `wait=`). Đã sửa phạm vi assert của 3 test đó (chúng assert lên global thread state của cả process); **worker rò vẫn chưa tìm ra test nguồn.** |
| **[BUG-059](completed/BUG-059_process_does_not_exit_after_graceful_shutdown.md)** | Thoát app: shutdown chạy hết, log `App stopped.` nhưng tiến trình không return | ⚪ Đóng — không tái hiện được từ môi trường hiện có (trước: 🟠 P1 (cơ chế root-caused, task cụ thể chưa tìm ra)) | 2026-08-26 | **Cập nhật 2026-08-26:** cơ chế chung xác nhận thật — `ThreadPoolExecutor.shutdown(wait=False)` không ngăn được CPython's `_python_exit()` atexit hook join mọi worker thread khi thoát, y hệt `BUG-041` (tái hiện tối giản: `timeout 8 python3 ...` → exit code 124). Đã loại `BinanceBotModule.dispose_all()` (2ms kể cả DB 1.77GB) và chính Historical Tick Backtest của phiên đó (đã hoàn tất 73 phút trước shutdown, cancellation token đã có sẵn) khỏi danh sách nghi phạm. Task cụ thể gây treo **chưa xác định** — thêm `_log_surviving_non_daemon_threads()` (dump tên thread sống ngay sau `app_engine.stop()`) để lần tái hiện sống tiếp theo tự chỉ đích danh. **Rà soát 2026-08-26:** đã map đủ **19 điểm `submit()`** — 6 đường của `ScanCoordinator`/inspector **không có token** (`clear_data`, `purge_all`, `vacuum` rủi ro cao), và `KLineInspectorCoordinator` **không có `cancel()`** nên `shutdown()` bỏ sót hẳn. Quan trọng: **thêm token không đủ** — `run_vacuum` gọi lệnh blocking không có checkpoint, token chỉ chặn được *giữa các shard*. |
| **[BUG-108](completed/BUG-108_ui_freeze_during_historical_tick_backtest.md)** | UI đơ nhiều lần (5.1s → 69.1s) trong lúc chạy Historical Tick Backtest | ⚪ Đóng — không tái hiện được từ môi trường hiện có (trước: 🟠 P1 (đã sửa 1 phần)) | 2026-08-26 | **Cập nhật 2026-08-26:** freeze #1/#2/#3 (giai đoạn nạp tick, gồm outlier 69,1s nặng nhất) root-caused + sửa — `RunHistoricalTickBacktestCommandHandler` dùng `get_klines()` (vật chất hoá cả dải tick 1 lần, tới 2,59 triệu dòng), chưa từng được áp fix streaming mà `BUG-025` đã làm cho `RunStaticBacktestCommandHandler`. Đo thật: `get_klines()` 1,5M dòng = 69,9s + heartbeat gap tới 1,88s; `count_klines()+stream_klines()` = 28,0s + gap tối đa 0,09s. Freeze #4/#5 (sau `ticks_loaded`, trong lúc mô phỏng) **vẫn Open**, chưa root-caused — xem §6 trong hồ sơ. |
| **[BUG-030](completed/BUG-030_parallel_test_run_worker_dies_after_resource_warning.md)** | `ci-local.ps1 -Full` (song song `-n 6`) chết giữa chừng sau `ResourceWarning: unclosed database`, không có summary | 🟡 P2 | 2026-08-21 | Tái hiện 2/2 lần **đúng cùng 1 chỗ** (không phải flaky), chỉ trên Windows. **Cập nhật 2026-08-25:** cơ chế đã chứng minh — `engine.dispose()` chỉ đóng connection *checked-in*; một `Session` còn checked-out thì `dispose_all()` **không** đóng, để lại cho GC → đúng điều kiện sinh `ResourceWarning`. Nên bước "cứ thêm `dispose_all()`" là **không đủ**. Linux đo lại bằng bằng chứng dương tính: 0 connection chưa đóng trên cả 6 worker. Đã có `scripts/bug030_connection_leak_probe.py` để chỉ đích danh file:line khi chạy được trên Windows. |
| **[BUG-061](completed/BUG-061_ci_workflow_missing_from_master_warrior.md)** | CI không chạy lần nào suốt 16 ngày — workflow biến mất khỏi `master-warrior` | 🔴 P0 | 2026-08-26 | **Ba lỗi chồng nhau.** (1) `master-warrior` không có `.github/` nào cả — file chỉ còn trên nhánh `main` cũ, mà `main` cũng đã bị xoá. (2) Trigger vẫn trỏ `[main, develop]`, cả hai đều đã chết. (3) **Mọi run trong lịch sử repo đều đỏ tại bước cài engine**: `pip install git+…` luôn chạy `git submodule update --recursive`, đụng submodule **private** `Sagittarius_LogViewer` → `could not read Username`. Ruff/pytest chưa từng chạy trên CI một lần nào. Sửa: clone engine không kèm submodule rồi cài từ path, trigger đổi sang `master-warrior`, thêm `QT_QPA_PLATFORM=offscreen` + thư viện Qt hệ thống. Mypy **cố ý chưa thêm** — đỏ sẵn 2 lỗi trong `scripts/` trên base sạch. **Còn lỗi thứ tư ngoài repo:** đã push mà vẫn 0 run, 0 check, và workflow duy nhất còn đăng ký là loại `dynamic` của Dependabot → hoặc push-bằng-app-token không kích hoạt workflow (tự khỏi khi người thật merge), hoặc **Actions bị tắt ở Settings** (cần admin bật tay). |
| **[BUG-056](completed/BUG-056_integration_and_sanity_tiers_together_abort_the_process.md)** | Bộ test integration abort/segfault giữa chừng, **không test nào FAILED** | 🟡 P2 | 2026-08-26 | Đóng 2026-08-26. **Chẩn đoán đầu sai**: đổ cho `unittest.mock` — thay bằng fake thread-safe thì crash vẫn còn, chỉ dời chỗ. Gốc thật: `qtbot` xoá widget ở teardown của **chính nó** (chạy sau mọi fixture), không ai bơm event loop, nên chúng bị huỷ ở **setup của test kế tiếp** — đúng lúc worker của test mới đang chạy, GC trên worker thread làm destructor shiboken chạy sai thread. Sửa: xả `DeferredDelete` trước khi boot engine mới. 42 passed (trước: segfault), và nhanh hơn 122s vs 179s. |
| **[BUG-053](completed/BUG-053_multi_line_subplot_script_gets_one_row_per_line.md)** | Script indicator nhiều đường (MACD) tạo 1 subplot row/đường thay vì 1 row chung | 🟠 P1 | 2026-08-26 | Trong chính hồ sơ này (2026-08-26), phát hiện lúc điều tra BUG-034. `IndicatorScriptRunner.draw()`/`IndicatorManager.add_subplot()` thêm tham số `group` để các đường cùng 1 script dùng chung 1 row thay vì mỗi đường 1 row mới. |
| **[BUG-058](completed/BUG-058_unscoped_stylesheets_box_every_label.md)** | Mọi nhãn trong app bị vẽ khung riêng; chữ trong stat tile bị cắt 5px | 🟠 P1 | 2026-08-26 | **Gốc thật của `BUG-008`**: selector kiểu trong Qt khớp cả lớp con, `QLabel` kế thừa `QFrame` → `.ClassName`. Chỗ rò lớn nhất là `QStackedWidget` chứa mọi màn hình. Chữ bị cắt là lỗi **riêng**: `setFixedHeight(74)` < 79px nội dung. 6 PR Engine + guard khoá 0. |
| **[BUG-057](completed/BUG-057_status_row_clipped_to_one_line_of_text.md)** | Mọi dòng bảng Database Status bị cắt còn 14px, 4 nút hành động render thành viền rỗng không chữ | 🟡 P2 | 2026-08-26 | `setIndexWidget()` không cấp chiều cao cho ô; delegate mặc định trả về chiều cao **một dòng text**. Sửa bằng delegate đọc `sizeHint()` của chính widget. Lộ ra khi chụp ảnh trước/sau cho `EPIC-007F`, không phải user báo. |
| **[BUG-055](completed/BUG-055_data_row_action_stretch_not_in_installed_engine.md)** | Bảng Database vỡ — `DataRow` không nhận `action_stretch` | 🟠 P1 | 2026-08-26 | Đóng 2026-08-26: **cùng nguyên nhân với BUG-054** — Engine trong `.venv` là bản cũ, không phải lỗi code app. Sửa bằng cài lại theo `install-rule.md` Option 1. 11 failed+1 error → **61 passed**. |
| **[BUG-054](completed/BUG-054_settings_screen_crashes_on_missing_stylerole_members.md)** | Màn Settings vỡ — `StyleRole` không có `HEADING`/`BODY_LABEL` | 🟠 P1 | 2026-08-26 | Đóng 2026-08-26: repo Engine **có đủ** cả 3 API; bản cài cũ hơn. Cả hai bản **đều báo `2.3.0`** nên version string không phân biệt được — đúng bẫy `install-rule.md` §1b đã ghi từ BUG-044. Bản cài đến từ local path, không phải GitHub. |
| **[BUG-049](completed/BUG-049_sanity_fake_server_thread_leaves_uncollectable_gc_objects.md)** | Fake Binance server's background thread để lại 5 uncollectable GC object lúc interpreter shutdown | 🟢 P3 | 2026-08-25 | Đóng 2026-08-25: root-caused, không sửa code được. `gc.set_debug(gc.DEBUG_UNCOLLECTABLE)` cho thấy đúng dạng cycle metaobject PySide6/shiboken (`QtCore.Property` + closure + `QMetaObject`/`Shiboken.ObjectType`) — đặc tính binding, không phải do `binance_fake_server.py` tạo ra. D6 chỉ là cái khiến `app.boot()` chạy đủ xa để cycle có sẵn lộ ra (trước đó treo/lỗi mạng thật theo BUG-045). |
| **[BUG-048](completed/BUG-048_uncaught_exception_after_boot_hangs_the_process_via_blocking_modal.md)** | Exception không bắt được sau boot làm treo tiến trình vĩnh viễn qua modal dialog chặn | 🔴 **P1** | 2026-08-25 | Đóng 2026-08-25: `_handler` kiểm `is_headless_qt_platform()` trước khi gọi `dialog.exec()`. Xác nhận bằng đúng fault injection đã phát hiện bug — exit code 1 dưới 5s thay vì treo. Regression test đỏ-đúng-lý-do trước fix, xanh sau fix. |
| **[BUG-050](completed/BUG-050_capital_dialog_apply_button_can_never_be_disabled.md)** | `CapitalDialogWidget`'s "Áp dụng" button không bao giờ bị disable, kể cả khi vốn nhập sai | 🔴 **P1** | 2026-08-25 | Đóng 2026-08-25: `__init__` ghi đè nút thật (do `_build_buttons()` tạo trong `super().__init__()`) bằng `self._btn_apply = None` ở cuối `__init__`. Regression test đỏ đúng lý do (`AttributeError: 'NoneType' object has no attribute 'isEnabled'`) trước fix, xanh sau. 0 test coverage trước đó cho cả widget. |
| **[BUG-046](completed/BUG-046_dashboard_exception_fallback_fsm_state_assumption_stale.md)** | `test_dashboard_integration_exception_fallback` giả định FSM đã LIVE lúc assert, thực tế IDLE | 🟡 P2 | 2026-08-25 | Đóng 2026-08-25: `BOT-062` đã đổi mặc định autostart sang tắt, assertion cũ chưa cập nhật theo. Sửa test, không đụng production code. |
| **[BUG-047](completed/BUG-047_dashboard_live_stream_candlestick_history_not_populated.md)** | `test_dashboard_integration_start_stream_chart_rendering`: candlestick history rỗng sau Load History + Start Stream | 🟡 P2 | 2026-08-25 | Đóng 2026-08-25: 2 mock cũ độc lập — `submit()` trả `None` thay vì `Future` (ExclusiveAction/BOT-069 cần), `GetHistoricalKlinesQuery` response sai shape (list thay vì dict theo symbol). Sửa test, không đụng production code. |
| **[BUG-045](completed/BUG-045_sanity_tier_makes_live_binance_network_calls.md)** | Tầng Sanity gọi mạng thật lên `api.binance.com` mỗi lần chạy CI | 🟡 P2 | 2026-08-25 | Đóng 2026-08-25: `EPIC-009` D6 — fake Binance REST server (stdlib, 3 endpoint thật), `Client.API_URL` trỏ vào đó thay vì mock `IExchangeClient`. Cả 17 use case resolve sạch, 0 network call. |
| **[BUG-044](completed/BUG-044_published_engine_has_python2_except_syntax.md)** | `sagittarius_engine` publish lên GitHub không import được — cú pháp `except A, B:` của Python 2 | 🔴 **P1** | 2026-08-25 | Đóng 2026-08-25: merged upstream (PR #175), `requires-python` hạ về `>=3.12` (PR #177). Verify từ repo này bằng clean install, full gate 1.761 pass trên 3.12.3. `v2.1.0`/`v2.2.0` vẫn dính. Thoát được vì `ruff check` báo "All checks passed!" trên file CPython không parse nổi. |
| **[BUG-041](completed/BUG-041_app_shutdown_hangs_on_inflight_thread_pool_task.md)** | App không thực sự thoát tiến trình khi có job nền đang chạy trên `ThreadManager` | 🟡 P2 | 2026-08-24 | Per-scan cooperative cancellation xuyên Presenter → Coordinator → Query Handler; process probe 2.000 pair thoát ~1,1s, full CI 1.700 tests pass. |
| **[BUG-043](completed/BUG-043_run_ui_cannot_import_local_engine.md)** | `run-ui.ps1` không import được Sagittarius Engine local | 🟡 P2 | 2026-08-24 | Bootstrap engine bằng editable sibling checkout hoặc GitHub theo `install-rule.md`; đồng thời cấu hình đúng local import path. |
| **[BUG-042](completed/BUG-042_paper_exchange_log_flood_freezes_ui_thread.md)** | Backtest nhiều trade đơ cứng UI — `PaperExchange` log INFO mỗi lệnh khớp, đổ vào `LogListModel` trên UI thread | 🔴 **P1** | 2026-08-24 | Trong chính hồ sơ này (2026-08-24). Tuân thủ `logging-rule.md` Rule 4 & 6: hạ 3 log per-fill/close sang `DEBUG`, giữ init ở `INFO`. `SignalLogHandler` (INFO) không nhận log per-trade; test red $\rightarrow$ green, CI full pass 1695 tests. |
| **[BUG-039](completed/BUG-039_native_chart_default_regressed_backtest_visuals.md)** | Native chart làm mặc định khiến Backtest mất grid, nến vẽ sai, tự phá chart khi có trend zone | 🔴 **P1** | 2026-08-24 | **Đóng 2026-08-24:** xoá hẳn native chart (`36f3a9f`) — không còn backend thì không còn đường tái hiện. Chính hồ sơ này là lý do quyết định xoá. |
| **[BUG-040](completed/BUG-040_ci_local_flaky_under_full_gate_load.md)** | `ci-local.ps1` flaky dưới tải full gate | 🟡 P2 | 2026-08-24 | Xem hồ sơ. |
| **[BUG-016](completed/BUG-016_chart_migration_benchmark_desktop_contract_hangs_windows.md)** | `chart_migration_benchmark.py --desktop-contract` treo vô hạn trên Windows | 🔴 **P1** | 2026-08-19 | **Đóng 2026-08-24 (moot, không phải fix):** native chart backend và script benchmark của nó đã bị xoá hoàn toàn để mở khoá `EPIC-006F`. |
| **[BUG-038](completed/BUG-038_native_fallback_drops_the_content_it_fell_back_for.md)** | Fallback sang Python vứt luôn nội dung mà nó fallback vì nó — nền trend zone không bao giờ được vẽ | 🔴 **P1** | 2026-08-23 | Trong chính hồ sơ này (2026-08-23). User phát hiện bằng mắt; test và log đều "xanh". Thêm `_apply_after_native_fallback()` phát lại nội dung lên host mới (cả 4 call site) + log `[chart-region]` ghi **đã vẽ mấy vùng lên host nào**. Xác nhận trên app thật: `replayed 1504 item(s)`. |
| **[BUG-037](completed/BUG-037_empty_trend_zone_payload_kills_native_chart_every_run.md)** | Native chart bị bỏ ở **mọi** lần chạy Backtest vì payload trend-zone **rỗng** | 🔴 **P1** | 2026-08-23 | Trong chính hồ sơ này (2026-08-23). `set_script_regions()` raise mà không nhìn payload; presenter emit spans rỗng mỗi lần chạy → cả epic `BOT-098F` chết ở runtime. Test cũ dùng `[]` nên đã đóng băng đúng hành vi sai. |
| **[BUG-036](completed/BUG-036_benchmark_crosshair_contract_synthetic_hover_race.md)** | Gate `Chart Benchmark Contract` lúc PASS lúc FAIL trên cây code giống hệt — synthetic hover ghi đè crosshair | 🟡 P2 | 2026-08-23 | Trong chính hồ sơ này (2026-08-23). Qt tự sinh hover tại con trỏ ma `(8,8)` của platform `offscreen`; `hoverMoveEvent()` đẩy thẳng vào `setCrosshairPosition()`, ghi đè giá trị benchmark vừa đặt. Sửa: đọc property ngay sau setter (đồng bộ), giữ giá trị sau-flush làm diagnostic + WARNING. Tái hiện 5/40 dưới tải, 0/40 sau sửa. |
| **[BUG-035](completed/BUG-035_engine_2_0_0_qml_module_rename_breaks_all_ui.md)** | Engine 2.0.0 đổi tên QML module `QmlShared` → `Sagittarius.UI`, hỏng toàn bộ UI | Cao | 2026-08-23 | 26 file `.qml` + 2 test file. Đóng chung BOT-117/BOT-118. Thêm guard `test_qml_imports_match_engine_qmldir.py`. Gate PASS, 1773 passed. |
| **[BUG-033](completed/BUG-033_realtime_backtest_progress_flood_freezes_ui_thread.md)** | Realtime Backtest tick-level làm UI thread đơ 5+ giây (progress signal flood) | 🟡 P2 | 2026-08-23 | Trong chính hồ sơ này (2026-08-23). Throttle `progress_callback` đổi từ đếm index (mỗi 256 tick/16 bar) sang thời gian thực (`ProgressThrottle`, ~150ms/lần) ở cả 2 handler (realtime + static) — chặn burst ~10,125 lần emit signal cross-thread cho 1 lần chạy 2.59M tick. |
| **[BUG-032](completed/BUG-032_chart_preview_renders_candles_before_running_backtest.md)** | Chart tự động vẽ nến và volume khi chưa nhấn "Chạy Backtest" | 🟡 P2 | 2026-08-22 | Trong chính hồ sơ này (2026-08-22). Không phải bug ngẫu nhiên — tính năng "Live Chart Preview" có chủ đích (`BOT-095D`/`BOT-102`) nhưng thiếu state phân biệt. Thêm `isChartPreview` (ViewModel) + badge QML, bật ở `_on_preview_data_ready`, tắt ở `_on_chart_data_ready`. |
| **[BUG-031](completed/BUG-031_cross_thread_timer_start_hangs_ui_during_backtest.md)** | `QBasicTimer::start: Timers cannot be started from another thread` treo UI màn hình Backtest | 🔴 **P1** | 2026-08-22 | Trong chính hồ sơ này (2026-08-22). Chuyển `apply_height` / `apply_minimum_height` thành `@Slot()` trên `BackTestView` gọi qua `QMetaObject.invokeMethod(..., QueuedConnection)`; bổ sung `@Slot(str)` cho `BackTestViewModel.set_ui_mode`. |
| **[BUG-029](completed/BUG-029_build_native_chart_join_path_powershell5_incompatible.md)** | `build-native-chart.ps1` dùng `Join-Path` 3-5 tham số, không chạy được trên Windows PowerShell 5.1 | 🔴 P1 | 2026-08-21 | Trong chính hồ sơ này (2026-08-21). `ci-local.ps1` tự khai `#Requires -Version 5.1` nhưng bị chính script con phá tương thích. Sửa 4 chỗ thành `Join-Path` lồng nhau 2 tham số. Verify trên đúng PowerShell 5.1 + `$ErrorActionPreference=Stop`. |
| **[BUG-015](completed/BUG-015_native_chart_geometry_rebuild_on_pointer_interaction_windows.md)** | Native chart "dựng lại" geometry OHLCV/volume khi rê chuột — hoá ra không phải bug renderer | 🟡 P2 | 2026-08-19 | Trong chính hồ sơ này (2026-08-21). **Không phải bug native code** — `root->buildCount` luôn = 1, đúng. Bug thật nằm ở probe script đọc property `geometryBuildCount` trước khi render thread kịp publish qua cross-thread hop — cùng lớp lỗi với 3 probe-bug khác đã sửa. Sửa: chờ post-condition thật thay vì đếm `processEvents()` cố định. 25% → 15/15 pass. |
| **[BUG-028](completed/BUG-028_kline_inspector_column_widths_misplaced_scope_qml_warning.md)** | `KLineInspectorModal` phát sinh cảnh báo QML `Unable to assign [undefined] to double` | 🟢 P3 | 2026-08-21 | Trong chính hồ sơ này (2026-08-21). Chuyển 5 thuộc tính `col*Width` về phạm vi root `ModalDialogCard` thay vì bên trong `ColumnLayout`. |
| **[BUG-027](completed/BUG-027_seeded_market_data_repository_missing_seven_port_methods.md)** | `_SeededMarketDataRepository` (Desktop E2E probe) thiếu 7/12 method của `IMarketDataRepository` — cùng lớp `BUG-026` | 🟡 P2 | 2026-08-21 | Trong chính hồ sơ này (2026-08-21). Tự phát hiện qua `EPIC-002A`'s audit `mypy`, không phải user báo. 2/7 method thiếu do chính `BUG-025` (phiên này) gây ra — phạm vi grep khi đó bỏ sót `scripts/`. |
| **[BUG-026](completed/BUG-026_shutdown_probe_missing_stream_historical_klines_implementation.md)** | Test probe đóng-app-khi-đang-sync crash — `_BlockingExchangeClient` thiếu `stream_historical_klines()` | 🟡 P2 | 2026-08-21 | Trong chính hồ sơ này (2026-08-21). Có sẵn từ trước (xác nhận qua `git stash`), lộ ra khi chạy full suite sau `BUG-025`; test double chưa theo kịp interface `IExchangeClient` mới. |
| **[BUG-025](completed/BUG-025_unbuffered_full_materialization_sync_and_backtest_data_paths.md)** | Đường dữ liệu Sync (Binance→DB) và Backtest (DB→RAM) không streaming — RAM phình theo độ dài range | 🔴 **P1** | 2026-08-21 | Trong chính hồ sơ này (2026-08-21). Nhánh Backtest: `count_klines()`/`stream_klines()` mới trên `IMarketDataRepository`, `RunStaticBacktestCommandHandler` tiêu thụ generator thay vì `list`. |
| **[BUG-023](completed/BUG-023_app_shutdown_hangs_when_database_sync_running.md)** | Đóng UI nhưng tiến trình Python + Database Sync không thoát (zombie process) | 🔴 **P1** | 2026-08-20 | Trong chính hồ sơ này (2026-08-21). Bổ sung `shutdown()` vào `DataManagementPresenter` và `DashboardPresenter`, cooperative cancellation trong `rate_limiter` & bulk sync, sửa tên tham số `RepairDataGapCommandHandler`. |
| **[BUG-020](completed/BUG-020_gap_repair_calls_undefined_run_check_status.md)** | Vá lỗ hổng thành công vẫn báo lỗi — gọi `_run_check_status()` không tồn tại | 🟡 P2 | 2026-08-20 | Sửa 2026-08-21 trong đợt refactor `_on_check_status()` của phiên khác |
| **[BUG-019](completed/BUG-019_gap_inspector_modal_unavailable_unknown_qml_module.md)** | `GapInspectorModal` không dựng được — `import Sagittarius.Theme` không tồn tại | 🔴 P1 | 2026-08-20 | Sửa 2026-08-21 bởi phiên khác, đúng đề xuất trong hồ sơ |
| **[BUG-024](completed/BUG-024_trend_zone_background_regions_uncapped_causes_pan_lag.md)** | Nền đỏ/xanh (trend zone) làm pan/zoom lag ~9x — 2065 `LinearRegionItem` không cắt tỉa viewport | 🔴 P1 | 2026-08-20 | Trong chính hồ sơ này (2026-08-20). Refactor `ViewportCulledLayer` ABC + `RegionLayer` mirror `MarkerLayer`. Đo lại: 234.7ms → 20.4ms median mỗi bước pan |
| **[BUG-022](completed/BUG-022_realtime_last_tick_of_every_bar_evaluated_twice.md)** | Realtime đánh giá tick cuối của **mọi** bar 2 lần — sai số PnL/trade | 🔴 P1 | 2026-08-20 | Trong chính hồ sơ này (2026-08-20). Tìm ra nhờ điều tra WARNING theo rule CI/CD mới; 6 tick → 9 lần gọi strategy, nay còn 6 |
| **[BUG-021](completed/BUG-021_realtime_backtest_chart_blank_queries_unsynced_timeframe.md)** | Chart trắng hoàn toàn sau mỗi lần chạy Realtime backtest | 🔴 P1 | 2026-08-20 | Trong chính hồ sơ này (2026-08-20) |
| **[BUG-018](completed/BUG-018_data_management_idle_to_idle_unlock_kills_stat_refresh.md)** | Ô "Stored KLines Records" mãi hiện `—` vì auto-discovery kết thúc bằng transition `IDLE -> IDLE` | 🟡 P2 | 2026-08-20 | Trong chính hồ sơ này (2026-08-20) |
| **[BUG-017](completed/BUG-017_backtest_sync_redownloads_full_range_ignoring_cached_coverage.md)** | "Đồng bộ ngay" tải lại toàn bộ range, bỏ qua phần đã cache | 🟡 P2 | 2026-08-20 | Trong chính hồ sơ này (2026-08-20) |
| **[BUG-014](completed/BUG-014_chart_pans_into_empty_space_and_goes_blank.md)** | Chart pan/zoom ra vùng trống rồi trắng hoàn toàn | 🔴 P1 | 2026-08-19 | Trong chính hồ sơ này |
| **[BUG-013](completed/BUG-013.md)** | `ResourceScope` teardown crash khi bắt đầu backtest mới (stale native dispose callback) | 🔴 P1 | 2026-08-19 | Trong chính hồ sơ này (2026-08-19) |
| **[BUG-012](completed/BUG-012.md)** | Indicator data có leading gap, không có giá trị trước để giữ | 🟡 P2 | 2026-08-18 | [`BOT-085`](../completed/BOT-085_dev_board_volume_spike_after_live_reload.md) |
| **[BUG-011](completed/BUG-011.md)** | UI block ở trạng thái health-check | 🟡 P2 | 2026-08-18 | [`BOT-085`](../completed/BOT-085_dev_board_volume_spike_after_live_reload.md) |
| **[BUG-010](completed/BUG-010_backtest_sync_never_satisfies_range_coverage.md)** | "Đồng bộ dữ liệu ngay" không bao giờ thoả range coverage, bấm bao nhiêu lần cũng vậy | 🔴 P1 | 2026-08-18 | Trong chính hồ sơ này (2026-08-18) |
| **[BUG-009](completed/BUG-009_backtest_cached_frame_preview_widget_shift.md)** | Cached-frame drag preview trông như dịch cả widget rồi giật về | 🟡 P2 | 2026-08-18 | Trong chính hồ sơ này (2026-08-18). Là case study của [`logging-rule.md`](../../.claude/rules/logging-rule.md) |
| **[BUG-008](completed/BUG-008_backtest_chart_toolbar_timeframe_noop.md)** | Nút timeframe trên chart-header chỉ có hình, không có tác dụng | 🔴 P1 | 2026-08-17 | Trong chính hồ sơ này |
| **[BUG-007](completed/BUG-007.md)** | Đóng app desktop nhưng tiến trình Python vẫn chạy | 🔴 P1 | 2026-08-17 | [`BOT-099`](../completed/) |
| **[BUG-006](completed/BUG-006.md)** | Trade marker chồng lên nhau đến mức không đọc nổi chart | 🟡 P2 | 2026-08-17 | [`BOT-098A1`](../completed/) |
| **[BUG-005](completed/BUG-005.md)** | Hộp thoại `Critical System Error` cố định kích thước, không xem hết stack trace | 🟡 P2 | 2026-08-14 | Trong chính hồ sơ này |
| **[BUG-004](completed/BUG-004.md)** | Bảng Trade Logs lệch cột giữa header và hàng dữ liệu | 🟡 P2 | 2026-08-14 | [`BOT-089`](../completed/BOT-089_content_driven_panel_sizing.md) — nguyên nhân thật khác với chẩn đoán ban đầu trong hồ sơ |
| **[BUG-003](completed/BUG-003.md)** | Volume bar bất thường sau khi Start Live reload chart | 🟡 P2 | 2026-08-14 | [`BOT-085`](../completed/BOT-085_dev_board_volume_spike_after_live_reload.md) |
| **[BUG-002](completed/BUG-002.md)** | Chart mất nến sau khi zoom out rồi zoom in | 🟡 P2 | 2026-08-12 | [`BOT-072`](../completed/BOT-072_chart_stale_viewport_window_on_zoom.md) |
| **[BUG-001](completed/BUG-001.md)** | App treo khi chưa sync xong (luồng nền chạm UI trực tiếp) | 🔴 P1 | 2026-08-12 | [`BOT-068`](../completed/BOT-068_ui_thread_affinity_guard.md) |

---

## Ghi chú về chất lượng hồ sơ

`BUG-001`…`BUG-005`, `BUG-011`, `BUG-012` là ghi chú thô (ảnh chụp + log dán
vào), không theo cấu trúc header. Từ `BUG-006` trở đi mới có định dạng chuẩn
(Reported / Severity / Status / Symptom / Root cause / Fix / Regression test)
mà [`fix-bug-rule.md`](../../.claude/rules/fix-bug-rule.md) §7 quy định. Mức
độ và ngày của nhóm cũ ở bảng trên được suy ra từ nội dung file và từ task đã
sửa chúng, không phải do file tự khai — nếu cần chính xác tuyệt đối thì đọc
thẳng file gốc.
