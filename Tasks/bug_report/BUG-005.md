![alt text](image-4.png)

### Vấn đề:
Hộp thoại thông báo lỗi nghiêm trọng (`Critical System Error`) dùng mặc định `QMessageBox` bị cố định kích thước (fixed size), không thể co giãn / resize hoặc phóng to để xem toàn bộ stack trace dài khi xảy ra lỗi ngoài ý muốn.

### Đã xử lý (Fixed):
- Tạo component [`CriticalErrorDialog`](file:///c:/Users/hoang/Documents/Sagittarius-Elite-Warrior/src/presentation/ui/components/critical_error_dialog.py) kế thừa từ `QDialog` với:
  - Bật `setSizeGripEnabled(True)` và thêm nút `WindowMaximizeButtonHint` cho phép kéo giãn cửa sổ hoặc phóng to toàn màn hình tự do.
  - Thêm nút **"Copy Error"** để sao chép nhanh toàn bộ thông điệp lỗi và Traceback vào clipboard chỉ với 1 click.
  - Vùng hiển thị Traceback (`QTextEdit`) dùng font Monospace (`Consolas`), tự động co giãn (`QSizePolicy.Expanding`) khi phóng to hoặc resize cửa sổ.
  - Nút **"Show Details..." / "Hide Details..."** đóng/mở mượt mà và tự điều chỉnh kích thước tối ưu.
- Thay thế trong trình bắt lỗi ngoại lệ toàn cục [`_install_exception_handler`](file:///c:/Users/hoang/Documents/Sagittarius-Elite-Warrior/src/presentation/ui/app_bootstrapper.py).
- Bổ sung bộ kiểm thử tự động tại [`tests/unit/presentation/ui/components/test_critical_error_dialog.py`](file:///c:/Users/hoang/Documents/Sagittarius-Elite-Warrior/tests/unit/presentation/ui/components/test_critical_error_dialog.py).
- **Trạng thái**: Đã fix & 100% CI tests PASSED.