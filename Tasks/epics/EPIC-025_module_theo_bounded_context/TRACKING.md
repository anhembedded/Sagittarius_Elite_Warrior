# EPIC-025 — Tracking (Gantt)

- **How to read it.** One bar per pull request or review; `done` is merged, `active` is being
  worked, `crit` marks a review or a hands-on check that only the user can do, `milestone` marks
  a phase closing. Dates after today are **planning estimates**, revised every time a bar closes —
  HLD §6.4 says only Phase 0 can be estimated before Phase 0 has run, and the Phase 1+ bars will be
  re-cut when Phase 0's measurements are in.
- **Who updates it.** The executor (the EPIC-025 skill, checklist step 11) moves a bar to `done`
  in the same pull request that closes it, and re-dates the following bars if the estimate moved.
- **Renders** on GitHub, in VS Code with a Mermaid extension, or at mermaid.live.

```mermaid
gantt
    title EPIC-025 — module split on the Engine's IExtension microkernel
    dateFormat  YYYY-MM-DD
    axisFormat  %d/%m
    todayMarker stroke-width:2px,stroke:#d33,opacity:0.6

    section Spec
    Round 1 — HLD, ADR, EPIC scaffold           :done,    s1, 2026-09-10, 2026-09-11
    Round 2 — doctrine, workbench rule, SDD     :done,    s2, 2026-09-12, 2026-09-13
    Independent review + round 3               :done,    s3, 2026-09-13, 1d
    D17 sizing, D18/D19 tests, executor skill  :done,    s4, 2026-09-13, 1d
    User review of the spec                    :crit, done, s5, 2026-09-13, 1d

    section Phase 0 — mechanism + market_data (EPIC-025A)
    PR 0.1 baselines and guards                :done,    p01, 2026-09-13, 1d
    User review 0.1 (allowlist, golden master) :crit, done, r01, 2026-09-13, 1d
    PR 0.2 core/ shell/ mechanism              :done,    p02, 2026-09-13, 1d
    User check 0.2 (app opens as before)       :crit, done, r02, 2026-09-14, 1d
    PR 0.3 support/binance_gateway             :done,    p03, 2026-09-14, 1d
    PR 0.4a modules/market_data behind its UI    :done,    p04a, 2026-09-14, 1d
    PR 0.4a-2 CLI inverted, 0.4a-3 contracts    :done,    p04a2, 2026-09-14, 1d
    PR 0.4b-1 Data Management in QtWidgets      :done,    p04b, 2026-09-14, 1d
    PR 0.5 skeleton walks, N=4 not 2 (#214)     :done,    p05, 2026-09-14, 1d
    User check 0.x (app runs: 3 screens + CLI)  :crit, done, r0x, 2026-09-14, 2d
    Phase 0 measured, Phases 1–5 re-estimated  :milestone, done, m0, 2026-09-15, 0d

    section Engine track
    E0 ScheduledJob.cancel()                   :         e0, after s5, 1d
    E1 lift module base + registry (after Phase 1) :     e1, after m1, 3d
    E2 lift surface runtime (after Phase 2)    :         e2, after m2, 3d
    E3 NavigationService, lifecycle, conformance :       e3, after p5, 5d

    section Phase 1 — trading + surfaces (EPIC-025B)
    PR 1.1a IHistoricalKlines + its 6 consumers :done,   p11a, 2026-09-15, 1d
    PR 1.1b IMarketStream + its two screens    :done,    p11b, 2026-09-15, 1d
    PR 1.2 ISymbolCatalog + IRangeCoverage     :done,    p12, 2026-09-15, 1d
    PR 1.3a modules/trading, the move          :done,    p13a, 2026-09-15, 1d
    PR 1.3b its three ports + first consumers  :done,    p13b, 2026-09-15, 1d
    PR 1.3c-1..5 both Presenters, orders, CLI, factory :done, p13c, 2026-09-15, 1d
    PR 1.4a..1.4d surfaces, tables, docks, first probe :done, p14, after p13c, 5d
    PR 1.5a..1.5b Welcome surface, dev mode, restart :done, p15, after p14, 3d
    PR 1.6a support/ui_kit/assets (Phase 4, early) :done, p16a, after p15, 1d
    PR 1.6b support/ui_kit/kit, 28 files       :done,    p16b, after p16a, 1d
    PR 1.6c six ui/common helpers              :done,    p16c, after p16b, 1d
    PR 1.6d constants, state, registry, sidebar :done,   p16d, after p16c, 1d
    PR 1.6e theme_bootstrap, services, embed, banner :done, p16e, after p16d, 1d
    PR 1.6f support/charting, 37 files          :done,   p16f, after p16e, 1d
    PR 1.6g support/indicators, 26 files        :done,   p16g, after p16f, 1d
    BUG-126 failures had no subscriber, CS-002 :done,  b126, after p16g, 1d
    User runs Testnet (orders, cancel, PnL)    :crit,    r1, after b126, 2d
    Phase 1 closed                             :milestone, m1, after r1, 0d

    section Phase 2 — strategy (EPIC-025C)
    modules/strategy, ISizingPolicy, per-symbol session : p2, after m1, 5d
    User runs Testnet (arm, disarm, tick→order) :crit,   r2, after p2, 2d
    Phase 2 closed                             :milestone, m2, after r2, 0d

    section Phase 3 — backtesting (EPIC-025D)
    modules/backtesting, 11 modals → QDialog, golden master : p3, after m2, 8d
    Phase 3 closed                             :milestone, m3, after p3, 0d

    section Phase 4 — support/*, ui/common dissolved (EPIC-025E)
    charting, indicators, ui_kit; Settings dialog; qml/ deleted : p4, after m3, 5d
    User review: designed theme? (D21 deferred)  :crit,    r4, after p4, 1d
    Phase 4 closed                             :milestone, m4, after r4, 0d

    section Phase 5 — Engine navigation (EPIC-025F)
    ScreenRegistry → NavigationService         :         p5, after m4, 4d
    Conformance suite on every surface         :         p5b, after e3, 2d
    EPIC-025 done                              :milestone, m5, after p5b, 0d
```

## Status log

| Date | Bar | Event |
| :--- | :--- | :--- |
| 2026-09-11 | s1 | Round 1 merged (PR #196) |
| 2026-09-13 | s2, s3, s4 | Rounds 2–3, D17–D19, executor skill merged (PRs #197–#205) |
| 2026-09-13 | — | ADR D20–D22 (QtWidgets only, OS theme, panels and dialogs): Phase 0/1/3 bars re-cut |
| 2026-09-13 | s5, p01 | User approved the spec; Phase 0 started with PR 0.1 |
| 2026-09-13 | p01 | PR 0.1 built locally: 3 guards in `tests/unit/architecture/`, allowlist (10 pairs), QML baseline (35), golden master (13 trades), 3948 → 4059 tests collected; self-review split the guards into one file per abstraction level (architecture-rule §5); waiting for the user's review before push |
| 2026-09-13 | r01 | Review 0.1: the user pointed at the decision doctrine (§7) instead of answering; both questions settled by it in `EPIC-025A` §1.1 — allowlist as found, dataset stays generated. The pull request itself still waits for the user's push OK |
| 2026-09-13 | p02 | PR 0.2 built locally: theme layer's global sheet removed (ADR D21a), `core/` + `shell/` with the contribution mechanism, one ConfigManager, the five legacy screens as contributions, two new architecture guards; gate green, 4 153 tests |
| 2026-09-14 | r02 | PR 0.2 merged (PR #210) into master-warrior; work branch fast-forwarded. Phase 0 mechanism is in main; next is PR 0.3 (support/binance_gateway) |
| 2026-09-14 | p03 | PR 0.3 (PR #211): `support/binance_gateway` extracted — 9 modules and 6 tests moved; boundary allowlist 10 → 8 pairs, the epic's first retired entries; `exchange_session_factory` and `binance_error_translator` deferred with reasons (EPIC-025A §1.3) |
| 2026-09-14 | p03, p04 | PR 0.3 merged (PR #211); `src/src`, a dangling symlink committed by accident in `8dc9b3ef`, deleted (PR #212). PR 0.4 split into 0.4a (the module, behind the existing UI) and 0.4b (Data Management in QtWidgets) — two different kinds of risk, EPIC-025A §1.4; 0.4a started |
| 2026-09-14 | p04a | PR 0.4a + 0.4a-2 + 0.4a-3 pushed into PR #213 (8 commits, gate green): `modules/market_data` registered in `shell/modules.py`; CLI parsing inverted so the shell parses once and the module owns `sync`/`stream`; contract suites and verified fakes for `ISymbolCatalogRepository` and `IMarketDataRepository`, which deleted 253 lines of hand-rolled in-memory duplicates. Two bugs found and fixed on the way (BUG-118 leaked Qt objects, BUG-119 an endless `while True` the gate could not report), and `pytest-timeout` installed so a hang fails loudly. Allowlist 41 → 38 |
| 2026-09-14 | p04b | PR 0.4b-1 built locally, gate green (4 286 tests, coverage 95.09%): Data Management's last two QML islands rebuilt as a `QTableView` panel with the app's first four `QAction`s and a kline-inspector `QDialog`; 4 `.qml` files deleted, QML baseline 35 → 31, `Theme.*` bindings 229 → 204, net -1 128 lines. The spec's list of which four widgets was wrong and is corrected in EPIC-025A §1.7 (the time-range and timeframe pickers are shared with four other screens). Two guards written under the pre-ADR doctrine were answered with their own `base-exempt` hatch rather than a raised ceiling. Two questions left for the user: the unreachable integrity audit, and jump-to-date. Waiting for the push OK |
| 2026-09-14 | p04b | 0.4b-2 (move the screen into `modules/market_data/ui/`) **deferred to Phase 4**, measured rather than judged: 8 forward allowlist entries would be traded for 35 backward ones, and the epic's zero-backward-imports property would become 35. A module's `ui/` may import `support/ui_kit`/`support/charting` whole (HLD §6.1) and neither exists before Phase 4, which is where eight of the nine import groups are heading anyway. Recorded in EPIC-025A §1.8, carried into EPIC-025E step 6, and the 8 allowlist entries re-keyed to Phase 1 |
| 2026-09-15 | p11a | PR 1.1a: `IHistoricalKlines` published — a verified fake, a twelve-guarantee contract suite run against **both** that fake and the real query handler, and all six readers of stored candles moved onto it (the trading chart, two backtest coordinators, the Dev Board stream controller, the kline inspector, the CLI's `trade-once`). Allowlist 34 → 28. What the port replaces is a handler whose return type changed with its argument's *runtime type* (`architecture-rule` §2.1): that is what the chart's `getattr(response, "data", response)` + `.get(symbol, [])` + two `isinstance` guards existed for, and four defensive steps became one call. The move also exposed four invalid test fixtures a stubbed dispatcher had been hiding — three sets of candles sharing one `open_time` (one row by primary key in any real store) and one anchored to 2024 while the screen asks a 7-day window — each fixed at the data, never by widening the port. Self-review (`pr-review` skill) caught three blocking defects before the push: one construction site left behind, two tests passing a *pytest fixture function* where the fake belonged, and one test left asserting nothing after its stub went dead |
| 2026-09-15 | p11a, p11b | PR 1.1 **split in two**, and the reason is review size rather than risk: moving the six `IHistoricalKlines` consumers alone touched 28 files and broke 65 tests that had to be repaired one by one, because a stubbed dispatcher had been standing in for a store. Putting `IMarketStream` and the `interactive_shell.py` move in the same diff would have doubled that without changing what could fail. 1.1b carries the remaining 6 of PR 1.1's 12 allowlist entries (28 → 22 as planned; it reached 24, see the 1.1b rows below for the measurement that moved the last two to PR 1.3). The Phase 1 bars are also re-cut here to the five pull requests `README` §"the cut" argues for — ports first, surfaces last — which the old three bars (Welcome first) contradicted |
| 2026-09-15 | p11a | PR 1.1a's review cleanup (gate PASS, 4 397 tests, coverage 95.14%): the nine *should fix* / *nit* findings the review left open. The two that were more than tidying: `ChartFeedCoordinator` had been left holding a `dispatcher` it never read once the candle read moved onto the port — a constructor parameter nobody uses still tells every caller that the class talks to the bus — and the `GetHistoricalKlinesQuery` **dispatch surface was dead**, with nothing in `src/` dispatching it after the six consumers moved. The query, its binding and the `execute()` that returned `list | dict` by runtime type are deleted; the class is now `StoredKlinesReader`, named for what it does, and the union `architecture-rule.md` §2.1 forbids is gone from the codebase rather than merely unused. Three tests got back the assertion their name promised (a realtime run reads nothing, the click handler reads nothing on the main thread, the invalid range reaches no store) and one got its ordering check back across all three mechanisms via a journal. `conftest.build_mock_klines` moved to a plain `mock_klines.py`: importing a *conftest* by name runs it twice under two module identities, which aborted the whole UI integration tier — found by making that mistake here, and it also retires the drifted copy in `test_dev_board_load_more.py` |
| 2026-09-15 | — | `BUG-121` filed while verifying that cleanup: the 47 UI integration tests hang or abort when run in one process, **on the merged tree as well** — a worker from a finished test still feeding the script runner while the next test sets up, the shape `BUG-056` fixed once. The gate cannot see it (xdist splits the tier, `pytest-randomly` reorders it) and `pytest-timeout`'s `signal` method cannot fire on a main thread stuck in Qt's C++ loop, which is the concrete gap `BUG-119` left. Reported with five measured runs per tree and four next steps; not fixed here, because root-causing cross-test state leakage in a Qt tier is its own task |
| 2026-09-15 | p11b | PR 1.1b: `IMarketStream` published — a verified fake, a thirteen-guarantee contract suite, and the two screens that open the live kline websocket (the trading chart and the Dev Board) moved onto it. Allowlist 28 -> 24. What the port removes besides the import is the same `getattr` probing 1.1a found on the history read, in its most dangerous form: `getattr(response, "success", True)` reports success for **any** object without that field, `None` included — so a stream that never opened told the user it was streaming. `StreamOutcome` is a named type with both fields, and `MarketStreamService` reports an unanswered dispatch as a failure. The port dispatches the module's two existing commands rather than reaching past them to `ILiveStreamService`: the module's own CLI dispatches them too, and skipping them would give the CLI and the screens two paths to one websocket. The contract suite's real half runs the whole chain down to `BinanceWebsocketService`'s reference counting with only `_run_stream` stubbed — a second fake would have agreed with the first and with nothing in production. Three consumers also lost a `dispatcher` they no longer used (`ChartFeedCoordinator` in 1.1a's cleanup, then `ChartCoordinator` and `StreamLifecycleController` here), and the trading Presenter tests stopped resolving three foreign ports to `MagicMock`s |
| 2026-09-15 | p11b, p13 | `interactive_shell.py` into `shell/` **moved to PR 1.3**, measured rather than judged: the move was made, the ratchets read, and it was reverted. `exchange-status` is trading's CLI handler with no owning module until 1.3, so the shell keeps importing it from the legacy tree — 2 allowlist entries traded for 2 new `baseline_shell_legacy_imports.txt` lines, a shrink-only ratchet growing so another can shrink. So 1.1b retires 4 of the 6 lines the plan gave it, and the epic README now carries 1.1c as the moved row |
| 2026-09-15 | p12 | PR 1.2: `ISymbolCatalog` and `IRangeCoverage` published, with their four consumers moved — the shared symbol picker (three screens read it), Data Management's auto-discover, and Backtest's chart-preview and data-sync coordinators. Allowlist 24 -> 20, and `market_data` now publishes **all five** ports HLD §3.4 assigns it. Both queries were deleted with their bindings, for the reason PR 1.1a's cleanup established: once the consumers had moved, nothing in `src/` dispatched either one. `BUG-072`'s `getattr(coverage_response, "data", coverage_response)` went with the dispatch — the unwrap existed because a test double's envelope reached a Qt signal typed `object` and **crashed the interpreter**, and a typed return makes the envelope unbuildable. Three things were fixed on the way: `normalised_symbols` lives in `contracts/` rather than beside the service, because a fake in `contracts/testing/` importing it from `application/` would have been the module's first inward import; the repository's fake was renamed `FakeSymbolCatalogRepository` (it had taken the published port's name, while `FakeMarketDataRepository` next door already followed the convention); and `DataSyncCoordinator`'s tests stopped driving a four-field `SimpleNamespace` where production hands over an eight-field `BacktestRangeCoverage`. `ChartPreviewCoordinator` and `DataSyncCoordinator` also lost the `dispatcher` they no longer used — the fourth and fifth such parameter in three pull requests |
| 2026-09-15 | p12, p13 | The two `-> modules.market_data.adapters` lines the epic README gave 1.2 stay where they are, and the allowlist's own comment already said why: `exchange_session_factory` needs splitting one factory per context (it answers both market_data's and trading's factory interfaces, and splitting a shared instance is a behaviour change ADR D12 keeps out of these PRs) — that is PR 1.3's, with `modules/trading`. `backtest_presenter`'s `InMemorySymbolMarketMetadataCache` fallback needs the cache registered in DI and the screen rebuilt, which is Phase 3. So 1.2 retires 4 of the 6 the README listed; the README row is corrected rather than left claiming six |
| 2026-09-15 | p13a, p13b | **PR 1.3 was cut in two, and the halves run in the opposite order to the one the epic README proposed.** Splitting it was the user's call (a single pull request would have been ~3000–4900 lines over the live-order path, with three new ports, on state 21 files read). Putting the **move first** was not a choice: `boundaries/rules.py` returns `False` for any new-tree import of the legacy tree outside the allowlist, and every type the three ports need was in the legacy tree — `IOrderSubmission` needs `OrderPreview`, `ITradingSession` needs `EnableTradingResult`, `IAccountSnapshot` needs `ExchangeConnectionStatus`. `market_data` could publish `IMarketDataSync` at PR 0.5 only because PR 0.4a had already moved its code in, so "publish the port, leave the code" was never a context's *first* step. 1.3a is trading's 0.4a; 1.3b is its 0.5 |
| 2026-09-15 | p13a | PR 1.3a: `modules/trading` exists. `src/application/ports/` (5 files) and `src/application/use_cases/queries/` (3 packages) turned out to be **entirely** trading's and moved whole; `domain/trading`, `use_cases/trading` less arm/disarm, the trading half of `infrastructure/binance` and four services followed. Trading's value objects, six order enums, `FuturesSymbolMetadata` and the seven events it raises went to `contracts/` rather than `core/vo`, on HLD §2.4 measured the way `SymbolMarketMetadata` was — which is why **26 would-be allowlist entries do not exist**: the legacy tree may import `modules.*.contracts` freely. Allowlist 20 → 80, and the two directions are not the same debt: **6** point out of the new tree (3× the shared `ExchangeSessionFactory` instance, `MarginRiskPolicy` sanctioned by the task file until Phase 2, `position_sizing` and `signal_action` which are `strategy`'s vocabulary) and **61** point in, each a consumer waiting for a port. The five `application -> infrastructure.binance.futures_trading_client` entries this whole guard was written for are **gone**, by becoming intra-module rather than by being excused. Gate: 4515 passed / 4 skipped, log file grepped |
| 2026-09-15 | p13a | Three things the move taught, each now recorded where it can be read again: a dotted path **split across two string literals** (`"...src.infrastructure.binance."` `"futures_user_data_stream.AsyncClient"`) survives a mechanical import rewrite invisibly — four `patch()` targets in one file, found only by the gate; `tests/unit/modules/` uses real packages all the way down, and creating the leaf `__init__.py` without its parents made the test directory a top-level package named `binance` that collided with the installed library **and** made ruff reclassify a third-party import as first-party; and PR 1.3a's own new reference checker (review finding S3, merged hours earlier) caught `bolt.prompt.md` pointing at the query directory this pull request emptied — the briefing now names the work and gives the command that finds it, per `.agents/Skills/README.md` §1 |
| 2026-09-15 | r0x, m0 | **Phase 0 CLOSED.** The user ran the app and confirmed the four checks the gate cannot make: the Trading screen loads history, Dev Board's "Start Live" syncs then streams, Data Management syncs a symbol, and the CLI `sync`/`stream` commands work. `EPIC-025A` moves to `completed/` — the first phase of this epic to close, and the commit that created that directory. The bar stayed open for a day after the code was complete, which is the epic README §3 rule working as intended: a phase ends with the app running, not with a green gate |
| 2026-09-15 | p13b | PR 1.3b: `trading` publishes `IOrderSubmission`, `ITradingSession` and `IAccountSnapshot`. Two things had to precede them, and the first corrects the spec: HLD §3.4 said *"`LivePosition` never leaves the module; `PositionSnapshot` is its flat DTO"*, and measured, `Order` is a frozen dataclass with eleven fields and **zero** methods while `LivePosition` is frozen with eight scalars, a `NewType` over `Decimal` and one pure derivation — a `PositionSnapshot` would be field-for-field identical, which is the behaviour-free churn PR 1.2 refused when it kept the name `BacktestRangeCoverage`. So both moved into `contracts/` under their own names and **20 allowlist entries vanished without one consumer being touched**. Two further types the spec named were never written at all (`AccountSnapshot`, `PositionSnapshot`) and `OrderIntent` had to become `OrderRequest` because two other classes already hold that name; HLD §3.4's four trading rows are corrected in the same pull request. Allowlist 80 → 56 |
| 2026-09-15 | p13b | The finding worth more than the import count: four presentation files were reading `enabled`, `orders_sent_this_session` and `known_open_symbols` **straight off the mutable, lock-guarded `TradingSessionState`** — a Presenter on the UI thread while the websocket thread rewrote the symbol set. `TradingSessionState.read_all()` is new and takes the lock once, so `ITradingSession.snapshot()` can answer a frozen three-field value that cannot be a combination which never existed, and it copies the set inside the lock rather than handing out the live object. The symbol lease is deferred to Phase 2 for `architecture-rule` §7.2.1's reason — `strategy` is its first consumer, and a lease with no caller is new locking behaviour on the riskiest state in the app |
| 2026-09-15 | p13b | Consumers moved so far: Settings (both ports) and the two `exchange-status` CLI entry points — 4 more entries retired. All five of `trading`'s fakes and suites now exist (`BUG-120`'s guard named the seven helpers that needed tests, and got them). The two big Presenters, the order CLI commands and `LiveTradingCoordinator` are **PR 1.3c**, with the `ExchangeSessionFactory` split and the DI move: one pull request per kind of risk, which is what splitting 1.3 was for. Gate: 4554 passed / 4 skipped, log grepped |
| 2026-09-15 | p13c | **PR 1.3c was attempted, measured and reverted — the measurement is the point.** Moving the two big Presenters and `arm`/`disarm` onto `ITradingSession` is 10 allowlist entries and *looks* like the Settings move that preceded it. It is not: the change itself is mechanical (one resolve, three dispatches and seven or eight state reads per Presenter, all of which lint and type-check clean), and the gate then reported **72 failures and errors across 12 test files** plus a **log-scan hit the two previous green gates did not have** — `[UI Thread Bridge Error] Exception in _recheck_edge: Signal source has been deleted`, a Qt lifetime error that surfaces once dashboard tests error at setup and teardown runs against half-built objects (the `BUG-056`/`BUG-121` family). Those test suites stub `TradingSessionState` in dozens of places and assert on `dispatcher.dispatch(EnableTradingCommand, ...)`, so each one needs the verified fake and an assertion on the effect rather than the call — PR 1.1a's shape exactly, where the consumer move alone touched 28 files and repaired 65 tests. The working tree was returned to the last green commit rather than left red; 1.3c starts from this number instead of from an estimate |
| 2026-09-15 | p13c | So 1.3c's scope, measured rather than guessed: the `ITradingSession` slice (10 entries, ~72 test repairs), the `IOrderSubmission` slice (the two Presenters, `trade_once_cmd`, `order_dry_run_cmd`, `order_preview_cmd`, their two formatters and `LiveTradingCoordinator`), `IAccountSnapshot.open_positions()` for the Dev Board, a home for `EquityCurveRecorder` (2 entries, and it is a *service* the chart adapter reads rather than a port call — it may belong in `contracts/` or behind a narrow port, and that is a decision, not a move), the `ExchangeSessionFactory` split one-per-context with the DI registrations following it, and 1.1c's two `interactive_shell` lines. Allowlist 56 → 13 |
| 2026-09-15 | p13c | **PR 1.3c-1 shipped green, and the 72 failures turned out to be the wiring, not the slice.** The `ITradingSession` slice is done: both big Presenters and the `arm`/`disarm` handlers now read `snapshot()` and call `enable()`/`disable()`/`emergency_stop()`; 10 allowlist entries retired (56 → 46), including the two the allowlist's own note had scheduled for Phase 2 — a legacy file may import `modules.*.contracts` freely, so the handlers took the port with no entry at all and Phase 2 now only has to move the caller. What made the difference from the reverted attempt: the repair is *not* 72 individual test fixes. Three of the five Trading test modules carried byte-identical copies of the package `conftest.py`'s own `container`/`view`/`presenter` fixtures (the duplication that file's `test-health` C7 note already named), so deleting 156 lines of copies fixed 36 collection errors at once and, as a side effect, gave those tests the `market_data` fakes their local containers never bound. The rest is one substitution per assertion: `dispatch(EnableTradingCommand, …)` becomes `trading_session.enables == 1`, and `dispatch.return_value = result` becomes `enable_answers(result)`. `FakeTradingSession` gained two failure hooks (`enable_raises`, `emergency_stop_raises`) with their own verification tests, because "the port raised and the screen must report it" is a real guarantee four tests need and `Mock(spec=ITradingSession)` is what HLD §10.3 rule 4 forbids. Test count unchanged where it should be (77 collected in `screens/trading/` before and after; 118 in the Dev Board's file), and two assertions got *stronger*: the session-stats card is now checked against stated numbers instead of against the same object the Presenter read from, and "a failed enable" is a named refusal instead of the `None` an untyped dispatch could return. Gate: PASS, 4593 passed / 4 skipped, log grepped — 0 WARNING/ERROR/CRITICAL records and 0 hits for the `_recheck_edge` Qt lifetime error the reverted attempt produced. Remaining in 1.3c: the `IOrderSubmission` slice, `IAccountSnapshot.open_positions()`, `EquityCurveRecorder`'s home, the `ExchangeSessionFactory` split with its DI registrations, and 1.1c's two `interactive_shell` lines |
| 2026-09-15 | p13c | **PR 1.3c-2: the `IOrderSubmission` slice — 14 entries retired, 46 → 32.** Both Presenters, all three order CLI commands, both order formatters' callers and `LiveTradingCoordinator` now go through `preview()`, `submit(live=…)`, `validate()` and `cancel()`. Three decisions worth recording. (1) The port gained a **fourth** method: `order-dry-run` sends to `POST /fapi/v1/order/test`, which is neither `preview()` (no network) nor `submit(live=False)` (every gate evaluated, nothing sent) — a genuinely third thing, so `validate()` publishes it under the app's own existing word for that mode (`OrderSubmissionMode.VALIDATE_ONLY`). (2) `SubmitOrderCommand`/`Handler` moved out of the legacy tree into `modules/trading/application/orders/`, where every other order use case already lives; its docstring now says plainly that nothing is ever created by it, which the name had stopped conveying. (3) `InvalidOrderForSubmissionError` moved from the payload **adapter** into `contracts/`: two legacy CLI files catch it by name, and a consumer may name a module's `contracts/` and nothing else — 2 more entries retired with no consumer churn. The `cast(ExecuteOrderResult, dispatcher.dispatch(...))` in the coordinator is gone with the untyped dispatch it documented. Test work: `FakeOrderSubmission` gained `validate_answers`, `validate_raises` and `submit_raises` (each verified by its own test, `BUG-120`'s guard), the contract suite gained a fourth guarantee — `validate()` reaches the venue and still sends no live order — and `test_live_trading_pipeline_against_fake_server.py` now wires the **real** `OrderSubmissionService` over its recording dispatcher, making it the first test that drives the published port's real implementation end to end. Two commands had no test at all, only formatter tests: `tests/unit/presentation/cli/test_order_cmds.py` is new and holds the guarantee that makes `order-dry-run` safe to run with trading off — it validates the order it previewed and never submits one (probed: deleting the `validate()` call fails 3 of its 6 tests). Gate: PASS, 4605 passed / 4 skipped, log grepped, 0 WARNING/ERROR/CRITICAL records |
| 2026-09-15 | p13c | **PR 1.3c-3: `IAccountSnapshot.open_positions()` and a new `IEquityCurve` — 3 entries retired, 32 → 29.** The Dev Board's manual order card read the real current position by dispatching `GetOpenPositionsQuery`; it now calls the port that already published that read in PR 1.3b, and its test states the position on `FakeAccountSnapshot` instead of a dispatch stub. The `EquityCurveRecorder` question TRACKING had left open as *a decision, not a move* is answered by measurement: both Presenters read exactly **one** thing off that service — the backlog — so `IEquityCurve` has one method, `samples()`, and `architecture-rule.md` §7.2.1's condition is met rather than anticipated (the second consumer already exists). `record()` is deliberately **not** on the port: the only writer is the module's own user-data stream, and publishing a write would let a screen append a sample the venue never reported. The port answers a tuple, which retires the list-copy the property used to make on every read purely so a caller could not edit the `deque` — an immutable answer says that instead of defending against it. `FakeEquityCurve` + `EquityCurveContract` ship with it, and both implementations run the suite **now** (the recorder is in-memory, so nothing waits for a later pull request); its `seed()` is named `seed` and not `record` so nothing in a consumer's test can be mistaken for the production write path the port refuses to publish. Six test files that built the Dev Board were binding `EquityCurveRecorder` and would have been handed a `MagicMock` for two foreign ports; all six now bind the verified fakes. The remaining `interactive_shell` pair is **not** in this slice: the allowlist's own note says PR 1.3 retires them by making the CLI table a contribution each module declares, which is a new mechanism and gets its own step. Gate: PASS, 4617 passed / 4 skipped, log grepped, 0 WARNING/ERROR/CRITICAL records |
| 2026-09-15 | p13c | **PR 1.3c-4: the `ExchangeSessionFactory` split, four pull requests after the allowlist first scheduled it — 4 entries retired, 29 → 25.** One class implemented two contexts' ports from the legacy tree: `market_data`'s `IExchangeSessionFactory` and `trading`'s `ITradingSessionFactory`. It is now `MarketDataSessionFactory` (market_data's adapter, public sessions only) and `FuturesSessionFactory` (trading's adapter, the signed one plus the `exchangeInfo` session its own metadata provider reads). Two of trading's three adapters took the **port** on the way, not the concrete class they used to annotate. The one real decision: `EPIC-021A`'s guard allowed exactly **one** file to construct `binance.client.Client`, by equality. I tried keeping that literally — a builder in `support/binance_gateway/adapters/` — and the boundary rule refused it, correctly: a module may import `support/*` only through `contracts/`, and a contract cannot name the raw SDK type (`IExchangeSessionFactory`'s own docstring rules that out). So the guard's allowed set went from one file to two, both named in full, and its docstring now states the rule the count was only ever a proxy for: **only a session factory mints a session**, so venue flags and credentials are not scattered. A new test pins that the set stays exact paths rather than drifting into a directory or a glob, and the asymmetry is part of the rule — market_data's factory *cannot* build a signed session at all. The 30s timeout (`BUG-063`) moved into the shared kernel as vocabulary two contexts must agree on; the `BUG-111` clock sync stayed with the signed session, which is the only thing that needs it. 13 test files and 5 probe scripts followed; the old factory's integration test became `test_session_factories_against_fake_server.py` and covers both. Gate: PASS, 4619 passed / 4 skipped, log grepped, 0 WARNING/ERROR/CRITICAL records |
| 2026-09-15 | p13c | **PR 1.3c-5: the CLI table becomes a contribution — the last 2 entries of part C retired, 25 → 23, and PR 1.3c is done.** `interactive_shell.py` held `{"sync": SyncCliHandler, ...}`, naming classes inside a module. PR 1.1b had tried to fix that by moving the shell and reverted, measured: the move only traded these 2 entries for 2 new lines on the shell's shrink-only legacy-import baseline. What actually retires them is the inversion `IContributionRegistry` already makes for panels — `BoundedContextModule` gains a sixth hook, `declare_cli(registry)`, `shell/cli_registry.py` collects, and nothing about where `interactive_shell.py` lives had to change. One deliberate leftover, written down rather than hidden: `exchange-status` is still a hand-written entry in that file, because its handler formats through `exchange_status_formatter`, which needs `presentation.enum_labels` — moving the handler into `modules/trading/cli/` would make a module import the legacy tree, which the boundary rule refuses outright rather than allowlists. I tried the move, hit exactly that, and reverted it; the entry is a `presentation` → `presentation` import costing nothing here, and it goes when `EnumLabels` finds a `support/` home in Phase 4. A command word is an identity, not a sort key, so a second claim raises at boot naming both modules — the one real difference from the panel registry, where two modules picking `order = 10` is normal. Tests: `tests/unit/shell/test_cli_registry.py` is new (6 tests, the same three properties `test_contribution_registry.py` holds for panels), and the interactive-shell tests now resolve the **real** registry with the real declarations — probed by making `market_data.declare_cli()` declare nothing, which fails 3 of them. Ten of those tests also stopped using the one-answer `resolve.return_value` double that `_resolving_app`'s own docstring warns about, since they all needed a type-routed one anyway. Gate: PASS, 4627 passed / 4 skipped, log grepped, 0 WARNING/ERROR/CRITICAL records |
| 2026-09-15 | p14 | **PR 1.4a: the surface host exists — `IPlaceHost` had no implementation for four pull requests.** The places were declared and the descriptors validated since PR 0.2, and nothing could actually show a contributed widget. `shell/workbench_surface.py` is that implementation: every `Place` onto the `QMainWindow` part HLD §11.2 assigns it (header and context bar as two toolbars on two rows, workspace as the central widget, rail and console and dev_probe as `QDockWidget`s tabbed per area, status tile in the `QStatusBar`), plus `save_perspective()`/`restore_perspective()`. `shell/surface_building.py` is the other half — one function from "a module contributed a panel" to "the user can see it", so the host stays indifferent to the mechanism and a test or a preview can drive it directly. 33 new tests, and the ones worth reading are the refusals: a second WORKSPACE, an untitled dock, a place the surface does not accept, two docks with one title. Three things were measured rather than assumed. (1) **`IPlaceHost` had to become a `Protocol`**: `class WorkbenchSurface(QMainWindow, IPlaceHost)` raises `TypeError: metaclass conflict` on import — `architecture-rule.md` §2.1 reason (a), with `IStateContributor` as the precedent this follows rather than inventing a second answer. (2) **`setWindowFlags(Qt.Widget)` is needed even with a parent**, and a bitmask assertion on it cannot work: `Qt.WindowType.Widget` is `0`, so the call *clears* the flag set and `isWindow()` is the only way to see it — I wrote the wrong thing first, probed it, and the test now pins the mechanism that works. (3) **A `MODAL` is kept, not placed**: a dialog is not part of a layout, so the host wraps the contributed body in a `QDialog` and raises it on request, which is what stops it becoming the permanent form the card shape was. Two mypy findings the gate caught and no test would have: `bytes(QByteArray)` has no stub overload, and `QByteArray.data()` is typed `bytes | bytearray | memoryview`. Gate: PASS, 4661 passed / 4 skipped, log grepped, 0 WARNING/ERROR/CRITICAL records. Nothing is wired to a screen yet — 1.4b moves Trading onto it |
| 2026-09-15 | p14 | **PR 1.4b-1: the surface host moves to `support/ui_kit`, behind a read port — the shell was the one place neither caller could reach.** 1.4b's job is Trading's two live tables as `QTableView` panels the module contributes, and sizing it found a boundary fact first: during the strangler period **two** kinds of caller have to render a surface — a legacy screen still carried by `shell/legacy_screen_adapter.py`, and a module's own `ui/` — and *neither may import `shell/`*. Measured, not assumed: adding `support.ui_kit.workbench_surface -> shell.surfaces` to the host fails `test_module_boundaries.py` with exactly that pair, and the same rule refuses `presentation -> shell` for every file except the GUI entry point. Both callers **may** import `support/ui_kit` whole, which is the zone HLD §6.1 named for exactly this ('the allowance exists for the surface host'), so the host went there. Three moves and one new port, no behaviour change: `workbench_surface.py` and `surface_building.py` to `support/ui_kit/`; the `Surface` dataclass to `core/contracts/surface.py`, because `support/*` may not import the shell either; and `Surface.is_open_in(dev_mode=…)` became `shell/surfaces.py::surface_is_open()` — the type is vocabulary every zone speaks, while *which key means what* is this application's policy, and a `core` type evaluating `"dev.mode"` would have carried policy into the kernel. The new port is `IContributionTable.panels(surface_id, place)`: the builder used to take the concrete `ContributionRegistry`, a `shell/` class, and the shape was already in the codebase — PR 1.3c-5 split `ICliRegistry` (declare) from `ICliCommandTable` (read) for the same reason, that the consumer must not be able to declare. `ContributionRegistry` implements both, and a new test drives `build_surface` with a hand-written table that is *not* the shell's registry, so a builder reaching for a registry-only method fails here rather than as an `ImportError` in whichever module tried to render a surface. Gate: PASS, 4665 passed / 4 skipped (4661 → 4665: 2 new tests, and the logging-namespace guard gained 3 parametrized files while 1 id was renamed by pytest's duplicate-id disambiguation), log `logs/ci-local-20260915-144614.log` grepped, 0 WARNING/ERROR/CRITICAL records. 1.4b-2 is the panels themselves |
| 2026-09-15 | p14 | **PR 1.4b-2: the two live tables become the platform's own, and 4 more `.qml` files are gone.** `PositionsTable.qml` + `PositionRow.qml` and `OpenOrdersTable.qml` + `OpenOrderRow.qml` were two hand-drawn tables — a QML `ListView` whose delegate painted every cell, with a "Huỷ" button in every open-order row — which is the shape ADR D20 rules out on the desktop. Replaced by `components/order_book/`: two `QAbstractTableModel`s and two `QTableView` panels, following PR 0.4b's Database Status rebuild rather than inventing a second answer. Three things the user gains because the platform's table brings them and the `ListView` could not: **column sorting** (click Unrealized PnL to bring the worst position to the top — neither table had any sorting, and it had to be made correct: on display text `"-9.00 USDT"` sorts after `"+10.00 USDT"`, so each model serves a `SORT_ROLE` carrying the number behind the text), **the cancel action out of the rows** (one `QAction` in the toolbar and the row's context menu, `Del`, operating on the selected order, enabled only when one is selected), and **a confirmation before it sends** — the QML button fired a cancel straight at the Presenter, which `Docs/HLD/11_desktop_workbench.md` §11.5 does not allow for something irreversible at the venue. What did **not** change is the contract both screens hold: `set_rows(rows)`, `cancelRequested(symbol, client_order_id)`, and the row projections with every format string intact — so `TradingView` and `DashboardView` needed an import change and nothing else, and no Presenter or Presenter test was touched. Colour went: the delegates painted PnL from `BULL_COLOR`/`BEAR_COLOR`, and ADR D21 leaves colour to the OS palette, so the sign stays in the text and a losing row's PnL cell is bold — the substitution PR 0.4b made for a shard with gaps. Ratchets lowered in the same commit, as their own instructions demand: `baseline_qml_files.txt` 31 → 27 and `baseline_app_styling.json`'s `qml_files` 31 → 27, `qml_theme_refs` 204 → 188. 28 new tests; every guarantee of the 4 deleted test files is restated except "profit and loss use distinct colours" (gone with the colour) and "a null `vm` after load does not throw" (gone with the QML engine that could set a context property to `null`). Verified by breaking it (`pr-review` E12): removing the selection-changed connection fails exactly the four cancel-action tests. **Module ownership stays put, and that is the measurement worth carrying forward:** these panels need nothing from the widget kit, but the legacy Presenters feed them, a legacy file may import a module only through `contracts/`, and a module-owned feed would need an authoritative open-orders read off the UI thread — `IAccountSnapshot` publishes positions only. So `modules/trading/ui/` waits for the feed, not for the widgets. **One thing the reconciliation of the test count exposed:** the 4 deleted test files were *colocated* under `src/presentation/ui/qml/*/tests/`, and the gate runs `pytest Sagittarius_Elite_Warrior/tests` — so they never ran in it, and the guarantees they were supposed to hold were unprotected. 20 such files remain in 8 QML packages; each one goes with the `.qml` it tests, and the replacements land under `tests/` where the gate can see them |
| 2026-09-15 | p14 | **PR 1.4c-1: what the host still needed before a real screen could move onto it.** Sizing the Dev Board conversion found three gaps, and each is a decision rather than a chore. (1) **The environment banner is not a place.** `EPIC-021K`'s "which venue am I in" warning is something every screen gets from its shell, and `test_environment_banner_all_screens.py` scans every navigable route for the widget — so a screen that moved onto the workbench and lost it would be a regression the user meets on Testnet, and no module should be contributing a `HEADER` widget that happens to be a warning. `WorkbenchSurface` now owns the slot, filled from the same composition-root-registered widget factory `PageShell` uses, as a non-movable, non-floatable toolbar row above the header — a warning the user can drag into a corner is a warning that stops working. (2) **`IContributionTable.surface(surface_id)`**, so the port answers both halves a renderer needs: what this surface accepts and what was contributed to it. The object that validated the contributions already held both, and a legacy screen being converted may not import `shell/`, so it could not otherwise name its own surface. `build_surface()` now takes the id instead of a `Surface`. (3) **`fill_surface(host, table, container)`** splits out of `build_surface()`: a converted `View` builds its own host and places its own chart, toolbars and log, then wants the contributed panels added to *that* host — building a second one would hand the user two workbenches, one of them empty. Gate: PASS |
| 2026-09-15 | p14 | **PR 1.4c-2: the Dev Board is a workbench — the first screen actually rendered by the surface host.** `PageShell` and its single `QSplitter` are gone from `DashboardView`: the scrolling column of `ChartCard`s is the workbench's central widget, and Positions, Open orders, Equity and the Controls column are `QDockWidget`s the user can move, tab, float, hide and have remembered by the perspective; the System Monitor log is the bottom dock; `DevBoardPanel`'s three buttons are the header toolbar and its price ticker and websocket pill are status-bar tiles, which is the split HLD §11.2 assigns (a toolbar carries what the user *does*, the status bar what the app *reports*). **What this fixes for the user:** the two account tables used to sit squeezed above the charts because the old rail was a fixed narrow column and a many-column table did not fit it (`BOT-128`'s own finding) — as docks they are as wide as they are dragged. **One thing the conversion turned up and it would have shipped silently:** the environment banner (`EPIC-021K`) reaches every screen from its shell, so a screen that changes shell loses it — `test_environment_banner_all_screens.py` scans every navigable route, and the fix is `app_bootstrapper` registering the factory on both shells until the last `PageShell` goes in Phase 4. Verified by breaking it (`pr-review` E12): removing the host's banner call fails exactly the `dashboard` route and nothing else. `BOT-014`'s "this is a developer testbed, not a dashboard" label moved from the page-title band (a `QMainWindow` has none) to the context bar, which is the part that says what the surface is pointed at. Three tests whose subject was the old layout were rewritten as five: the docks by title (the key a perspective stores them under), the toolbar/status-bar split, the identity label, and — because `DashboardView` declares its own `Surface` (a file under `presentation/` may not import `shell/`) — one asserting that declaration equals the shell's `dev_board` entry, so one declaration cannot quietly become two. No Presenter was touched. Gate: PASS, 4688 passed / 4 skipped (4686 → 4688: five new tests for three removed), log grepped, 0 WARNING/ERROR/CRITICAL records |
| 2026-09-15 | p14 | **PR 1.4c-3: one card per dock, and the order form is a dialog on `F9`.** `DevBoardPanel` was a widget that owned a scrolling column of six cards; it is now a `QObject` that *builds* them, and `DashboardView` places five in their own docks (Data & stream, Strategy, Last signal, Session, Indicators) and the sixth — the manual-order form — as a dialog. **What this changes for the user:** the four cards they are not using can be hidden or tabbed away instead of scrolled past, and order entry stops occupying the layout for something done occasionally (HLD §11.3). The dialog is modeless and `F9` raises it, which is MetaTrader's own key for "new order" and the shape HLD §11.2 says to apply rather than invent; one `QAction` carries the shortcut, the header button and the enabled state. Two things the change paid for itself with: the panel stopped painting the app background with a stylesheet of its own, so `set_style_sheet_calls` 151 → 149 (a `QObject` paints nothing, and the workbench supplies the surface); and the existing tests found a real defect rather than reading it off — the symbol picker and the range picker were parented to `self`, and a `QDialog` parented to a `QObject` raises `TypeError`, so both now take the window behind the button that opens them. Every private attribute stayed where it was, which is why all 30 `test_dev_board_panel.py` tests and the manual-order Qt-click integration test needed no change beyond the fixture that used to `resize()` the panel. `preview.py` places the cards itself now, and the gate caught that it did not — a preview is the one thing that builds this class outside a screen. `SPEC-005`'s surfaces, trigger and evidence rows follow the flow to the dialog. Gate: PASS, 4692 passed / 4 skipped (4688 → 4692, four new tests), log grepped, 0 WARNING/ERROR/CRITICAL records |
| 2026-09-15 | p14 | **PR 1.4c-4: the mechanism walks — the first panel a bounded context owns renders in a real run.** `trading` now contributes one `DEV_PROBE` to the Dev Board: the live trading session's own three fields (the `enabled` gate `EPIC-021G` makes the user set every run, the order counter a session limit is checked against, and the conservative `known_open_symbols` set that blocks a second order on a symbol — added when an order is *sent*, so a stale entry silently refuses trades). One `snapshot()` per refresh, asserted against the module's own verified fake through `snapshot_reads`, because three reads could show a count from before an order beside a symbol set from after it. **The finding that made this PR bigger than a panel: `contribute()` was a hook no code path called.** Modules could declare panels since PR 0.2 and nothing would ever ask for them — the GUI entry point built its own registry and only ever handed it the five legacy screens. Fixed where the hook table says it belongs: `contribute()` runs *after* `boot()`, which is the entry point's call, so the composition root now records the module **instances** it registered (`RegisteredModules` — a module is stateful, and a second instantiation would contribute against a graph it never built) and `shell/contribution_assembly.py` collects everything once the app is up, then binds `IContributionTable`. **A second finding, same shape:** `test_module_declarations.py`'s docstring has claimed since PR 0.2 that a `sys.modules` snapshot in `test_module_contribution_laziness` checks HLD §6.1's rule (e); that file did not exist. It does now, and its real assertion runs in a **subprocess** — this test session has imported Qt long before the guard runs, so only a fresh interpreter can say what a headless `sync` pays for `contribute()`, which is zero. Verified by breaking it (`pr-review` E12): importing `QWidget` at `probes.py`'s module level fails exactly that test. 22 new tests. Gate: PASS, 4717 passed / 4 skipped (4692 → 4717: 22 tests plus 3 parametrized files for the logging guard), log grepped, 0 WARNING/ERROR/CRITICAL records |
| 2026-09-15 | p14 | **PR 1.4d: the duplication number gets a ratchet, and a claim of mine gets corrected.** The plan for 1.4d was to move the seven `ui/common` files these two screens share into `modules/trading/ui/`. Measured first: all seven — `order_feed`, `equity_feed`, `equity_chart_adapter`, `order_fill_marker`, `market_tick_feed`, `live_order_book_coordinator`, `execute_order_block_reason` — have **exactly the two legacy Presenters** as consumers, and a legacy file may import a module only through `contracts/`. So the move waits for those Presenters, which wait for `support/ui_kit` and `support/charting` in Phase 4 (PR 1.4b-2's entry measured the same wall for the widgets). **The correction:** my 1.4a–1.4c entries said the epic's "59 duplicated members" was stale and measured 34 today. It is not stale. `tools/measure_duplicate_members.py` was committed in Phase 0 and still reports exactly 59 for the `dashboard+trading` pair; my 34 came from a definition I invented — the two Presenter *files*, not the two screen packages, and without the tool's "defined in no third package" rule — because I wrote a second measuring script before looking in `tools/`. `ONBOARDING.md` §7's "survey before you invent" is exactly the rule that would have prevented it; the duplicate script is deleted. What was genuinely missing is a **guard**: `test_presenter_duplication_only_shrinks.py` now holds the Phase 1 pair at 59 and the cross-package total at 132, shrink-only in both directions, so the number cannot grow between phases (a third screen reacting to the same order events, or a fix copied into the second Presenter — how `BUG-084` and `BUG-086` each came to be fixed twice) and cannot be quietly left un-lowered when the move finally happens. Gate: PASS |
| 2026-09-15 | p15 | **PR 1.5a: the app opens on Welcome — the first screen the shell itself owns.** ADR D13's user-visible change: the application no longer opens on a developer testbed that happened to be first in the sidebar. `shell/welcome/` is a `WorkbenchSurface` with the app's name, its version and **which venue this run talks to** — both venues, because where prices come from and where an order would go are independent settings (ADR §2), and a user who does not know they are on Testnet is one click from a real order. **Start is an intent, not a navigation call:** the screen publishes `StartRequested` and the entry point decides it means Trading, so the day there is a real login the button keeps raising exactly this and nothing on the screen changes (HLD §4.6's own words). The shell cannot navigate — a route change is `MainWindow`'s, in the legacy tree it may not import — and a navigation port with one caller would pre-empt the Engine's `NavigationService` (Phase 5), so the bus is the honest join. It arrives as a `ScreenContribution` with `contributor_id="shell"`, through the same path every screen takes, so `ScreenRegistry`, `PresenterManager` and the sidebar never learn a third kind exists. Four things the gate caught that reading would not have: (1) **`app.version`** — the shell shows a version and may not import the legacy window title, so it reads two new config keys, with `test_app_version_matches_pyproject.py` keeping `app_config.json` honest against `pyproject.toml`; (2) the sidebar icon `home` **does not exist** in the vendored Lucide set, and `IconLoader` answers a missing name with a blank glyph *and a WARNING* the log scan fails on — now `zap`; (3) `mypy` refused `_build_welcome_presenter`, because the contract hands over a `BaseView` while this Presenter needs this view's signals — an `isinstance` check with a real message, not a cast, and `shell/` being inside the type gate is why it surfaced; (4) five tests and two probe scripts pinned `dashboard` as the default and had to follow the decision — including `test_boot_never_constructs_a_non_default_screen`, which is now a **stronger** guarantee: booting constructs neither trading screen, so the Dev Board's feed-opening Presenter is no longer built just because the app started. One more thing a test taught: a `BasePresenter` with no reference is collected the moment the line ends, taking its signal connections with it — the first version of the Start test asserted on a presenter Python had already freed. Gate: PASS, 4740 passed / 4 skipped, log grepped, 0 WARNING/ERROR/CRITICAL records |
| 2026-09-15 | p15 | **PR 1.5b: the developer-mode switch, with the restart that is the only honest way to apply it — and one gate deliberately not closed.** `dev.mode` is read once, at boot, because it decides whether a whole surface exists; a value that changed mid-run would mean loading or unloading a module, which the mechanism cannot do. So the Welcome screen's switch writes `user_config.json` through `IConfigWriter` — **bound for the first time in this commit**, and its first caller, which is what starts retiring `settings_presenter.py`'s downcast to `ConfigManager` — and then offers **Restart now**. The one real subtlety is in a pure function: `--dev`/`--debug` on the old command line beat the file on the next run, so turning developer mode *off* from a session started with the flag strips it, and turning it on keeps the command line as it was. A failed `startDetached` leaves this session running rather than quitting into nothing, and the setting is on disk either way. Two findings: a failed *save* must put the switch back and offer no restart (the notice is a fact about the file, and a cheerful "restart to apply" over a failed write is a lie the user acts on), and `mypy` caught that `QProcess.startDetached` answers `(started, pid)` in PySide6 — a lambda handing that tuple back as a `bool` would have reported every failed start as a success, forever, because a non-empty tuple is truthy. **The gate not closed:** ADR D14 says the Dev Board's screen exists only under `dev.mode`, and its *surface* is gated (its probes appear and disappear with the switch, which is what makes the switch visible at all). The **screen** is not, and the measurement is the reason: `dev.mode` ships `false`, and that screen still carries manual order entry and the strategy controls, which have no home on the Trading surface until those move — gating it now would take live manual trading away from the user. `SPEC-011` writes the whole flow down, including that non-promise and the human step for the switch. Gate: PASS, 4759 passed / 4 skipped, log grepped, 0 WARNING/ERROR/CRITICAL records. **Phase 1 has no pull requests left** |
| 2026-09-16 | p14 | **Review of 1.4c-1…1.5b: one finding, and it is against my own slice.** `dev_board_panel.py` went **1015 → 1074 lines** while the pull request that touched it (1.4c-3) is called "one card per dock". `architecture-rule.md` §5 rule 4 is not negotiable about >400 lines, and what 1.4c-3 actually moved was the *placement* of the cards, not the building of them: the six `_build_*_card` methods and their twenty `_sync_*`/`_on_*` handlers are still one class. The violation predates the branch; growing it does not. **Exit, and why it is not immediate:** the split needs each card to own its own widgets *and* its own enable/disable rules — `_sync_trading_state` and `_sync_controls_active` reach across cards today — which is the same rewrite the screen gets when it moves into `modules/trading/ui/` behind `support/ui_kit`. Doing it twice, the second time under a different import root, is how a refactor gets abandoned half-done; doing it *now* also means rewriting a screen the user has not yet accepted on Testnet. So it is scheduled with the move, and this row is what stops it being forgotten. The other two review notes: the `IContributionRegistry` + `IContributionTable` pair on one class is multiple inheritance §2 tells you to avoid, kept for consistency with `CliRegistry` (both bases are pure ABCs, no state, no shared method names); and the Dev Board screen is still not gated on `dev.mode`, which is the user's call, recorded in the 1.5b entry |
| 2026-09-16 | p16a | **PR 1.6a: `support/ui_kit` starts from its clean leaf, and two guards turn out to have stopped watching a tree.** Phase 1 closed with its own last criterion blocked on a package scheduled four phases away: the two screens cannot move into `modules/trading/ui/` until `support/ui_kit` and `support/charting` exist, and Phase 2's strategy cards sit behind the same wall. Measured before cutting, rather than assumed: `support/charting` **cannot** go first — `chart_card` imports `domain.indicator_scripts` and seven `presentation.ui.*` packages — while `assets/`, `kit/`, `qml/embed`, `qml/kit`, `theme_bootstrap.py` and `constants.py` import **only each other**. So `ui_kit` is extractable now, bottom-up, and `assets/` is the leaf: 4 files whose imports are stdlib, PySide6, the Engine and themselves, with 40 consumers. The move costs **zero** allowlist entries either way (`rules.py::_legacy_may_import` lets a legacy file import `support/**` whole, and nothing in `assets/` points back), so the ratchet is untouched and the styling census is byte-identical (149 / 27 / 188). **What the move exposed is the part worth reading.** `test_ui_root_is_where_we_think_it_is` failed — the landmark assertion `(_UI_ROOT / "assets" / "palette.py").is_file()`, written against a typo in `parents[4]`, caught something else entirely: the three guards in `test_widget_guards_hold.py` (inline stylesheets locked at 0, bare Qt bases at a ceiling of 2, unscoped container sheets at 0) scanned `src/presentation/ui` only — so since **PR 1.4b-1** the surface host in `support/ui_kit` had been unguarded by all three, and this move would have taken `palette.py`, the colour guard's own exempt file, out of the scanned tree while the guard stayed green on what was left. Retargeted to both roots, measured first: the new root contributes **0** findings to all three, so nothing is waved through; the ceiling stays 2; E12 probes (a `#F3BA2F` literal, then a `setStyleSheet("#1a1b22")`, both in `workbench_surface.py`) fail exactly the two guards that now claim to watch it. The palette guard, `scanned_roots_registry.py`, `ui-presentation-rule.md`'s icon path, `palette.prompt.md`, `tools/measure_app_styling.py::PALETTE_MODULE` and two test files to the `unit/support/ui_kit/assets/` mirror all moved in the same commit — the registry's own rule. **One process note:** my first gate run was not a verification. I ran `ruff` green, *then* fixed two relative imports (`from .assets`, `from ..assets` — the dotted rewrite had missed them), and the gate caught both as `I001`. That is `ci-rule` B6's exact failure mode, and the run cost 3½ minutes to learn nothing new. Gate: **PASS**, 4764 passed / 4 skipped, log `logs/ci-local-20260916-014840.log` grepped — 0 WARNING/ERROR/CRITICAL records, and the count moved 4761 → 4764 for the three `BUG-123` tests in the row below, nothing else |
| 2026-09-16 | p16a | **`BUG-123`, found by the 1.6a gate: a suite that could fail on the clock alone.** Two consecutive full runs, five minutes apart on the same diff — one green, one red on `test_scrolling_near_the_left_edge_prepends_older_candles` with `assert 9 == (5 + 5)`. Not a flake, and `ci-rule` §3 is why I did not re-run it: `mock_klines.build_mock_klines()` read `datetime.now()` **on every call**, and two callers read it at two different moments — the `seeded_history` fixture seeding the store, then `_older_mock_klines()` in the test body deriving the page below what the screen shows. `.replace(second=0)` truncates to the minute, so the two agree for most of a minute and disagree completely once one rolls over: the second grid shifts a minute later, the "older" page's newest row lands exactly on the row already in the store, and 4 of 5 candles prepend. Reproduced deterministically by injecting a clock that jumps a minute per `now()`, not by waiting for a rollover — which also surfaced a second instance nothing watched: the five `SEEDED_SYMBOLS` are seeded in one loop, so a rollover mid-loop left five symbols on five grids in one store. Fixed as the mechanism, per `bug-fix-rule` §2: **one anchor per process**, read at import, so every current and future caller derives the same grid — passing the anchor per call site would leave the next caller free to read the clock again. Regression test written first and confirmed red for the right reason, three tests in `tests/integration/presentation/ui/test_mock_klines_is_one_grid.py`, the third pinning that a frozen anchor still never claims a candle from the future. report in `completed/`, board row moved, ROADMAP row brought up to date through 1.6a |
| 2026-09-16 | p16b | **PR 1.6b: the widget `kit/` moves, and leaving `presentation/` turns the type checker on.** `assets/` going in 1.6a made `kit/` a leaf — re-measured rather than assumed: it depends on nothing inside `presentation/ui`, 41 files outside it import it, and `qml/kit`, `qml/DataTable`, `components/sidebar` and `components/environment_banner` all wait on it. 28 files, 4708 lines, zero allowlist entries in either direction, styling census unchanged at 149 / 27 / 188, duplication metric still 59. `style.py` and `page_shell.py` travelled along although HLD §11 / ADR D21 still schedule them for deletion: a package is the unit that moves, and a package split across two trees mid-migration costs more than two doomed files do. **The part that was not mechanical.** `pyproject.toml` excludes `src/presentation/` from mypy *wholesale* — 52% of the 2026-08-21 baseline was one PySide6 `@Property` false-positive class (EPIC-002A §2) — so moving out of that tree put 28 files under the checker for the first time. Three errors, **none** of them that class, so `pyproject`'s own rule applied ("a file not in this list that fails must be fixed, not added"): `QByteArray.data()` is typed `bytes \| bytearray \| memoryview` and only the first has `.decode()`, now `bytes(...).decode()`; and `_TabButton`'s two size hints read `self.layout()`, correctly typed `QLayout \| None`, which would have been an `AttributeError` on a widget without one — they read the layout the constructor built instead. The `N802/N803/N815` ignore for Qt overrides also had to follow the widgets, or the move would have had to rename 14 Qt overrides, which is a behaviour change dressed as a move. **Three guards retargeted, each probed by breaking it, and one of them found the move itself:** `test_the_kit_is_not_a_hiding_place` compared `_KIT_CARD_FILES` against an empty directory and failed — the honest way for a moved tree to announce itself — so `_KIT_DIR` follows, and that guard's scan now reads both trees because the legacy tree is down to **one** Card (`ChartCard`, which leaves for `support/charting`) and was one move away from inspecting nothing; the `QQuickWidget` and hand-seeded-theme rules gained `support/ui_kit` for the same reason, both being about what the running application does. Probes: a `class _ProbeCard(Card)` in `kit/surface.py` fails the kit guard, a `QQuickWidget(parent)` in `kit/overlay.py` fails the embed guard. Also retargeted in the same commit: the tests mirror `unit/presentation/ui/kit` → `unit/support/ui_kit/kit`, that mirror's `conftest.py` exemption key, `tools/measure_app_styling.py::STYLE_BUILDER`, `tools/kit_showcase`, and the three `.agents/Skills/` briefings that point a reader at the kit. Gate: **PASS**, 4766 passed / 4 skipped, log `logs/ci-local-20260916-021052.log` grepped — 0 WARNING/ERROR/CRITICAL records, and the count delta is **entirely** bookkeeping: 4764 → 4766 is the two new roots in `scanned_roots_registry.py`, which `test_scanned_roots_are_not_empty.py` parametrizes one case per root (93 → 95 collected, measured by stashing the diff and re-collecting). No test body was added, moved between tiers, or removed — the `unit/presentation/ui/kit` → `unit/support/ui_kit/kit` mirror move is 28 files at the same count |
| 2026-09-16 | p16c | **PR 1.6c: six `ui/common` helpers move, and a row of the HLD turns out to be unreachable.** The six are `action_ownership_tracker`, `app_defaults`, `base_feed`, `health_status_report`, `health_feed`, `health_check_coordinator` — each a leaf, 42 import sites rewritten, 0 allowlist entries, no behaviour change. Unlike `kit/` this splits a package, and that is the plan rather than a compromise: `EPIC-025E` step 4 *dissolves* `ui/common`, whose files have three different destinations. The re-export in `common/__init__.py` was **removed**, not repointed: a shim would have worked (legacy may import `support/**` whole), which is why it would have been the wrong answer — 42 consumers would keep naming a package this epic deletes, and nothing would fail in between to say so. **The finding, and it is against the design rather than the code.** §3.5 assigned `sync_progress_*` to `support/ui_kit` too. It cannot come: `sync_progress_feed` reads `modules.market_data.contracts.events.sync_events`, and §6.1's `support/* → modules/*` prohibition has **no** contracts exception. Checked against the rule table itself rather than reasoned about — `import_is_allowed("support.ui_kit.sync_progress_feed", "modules.market_data.contracts.events.sync_events")` returns `False`, while the other three edges this PR needed all return `True`. So the rule refuses it; an allowlist entry is not what is missing. A feed whose whole job is normalising *one module's* events is that module's UI, which makes `modules/market_data/ui/` the destination — a change to what the epic promised, so it is recorded as **open** for the user in §3.4, §3.5 and `EPIC-025E` step 3, and the pair was left where it is. **mypy again:** leaving `src/presentation/` put these six under the checker for the first time and found one `architecture-rule` §2.1 violation the wholesale exclusion had been hiding — `default_interval(allowed: object = None)`. `object` has no `__contains__`, so `candidate not in allowed` does not typecheck and the annotation described no contract at all; it is `Container[str] \| None` now, the narrowest type the body needs, and both real callers already pass one. Tests: 4 mirror files to `unit/support/ui_kit/`; `health_check_coordinator` and `health_status_report` had no mirror file to move — they are proven from `screens/test_system_health_logging.py` and one integration flow, which is where they stay. Gate: **PASS**, 4782 passed / 4 skipped, log `logs/ci-local-20260916-030352.log` grepped — 0 WARNING/ERROR/CRITICAL records. One run covers this commit and `BUG-124`'s beside it; the count delta 4766 → 4782 is all new guards, measured not guessed: 2 in `test_engine_port_calls_are_real.py`, 9 in `test_case_study_index_is_consistent.py`, and 5 parametrized cases in `test_scanned_roots_are_not_empty.py` (95 → 100 — one per new guard row plus one per new scanned root). No test body removed or moved between tiers |
| 2026-09-16 | p15 | **`BUG-124`: PR 1.5a shipped a dead Start button, and the reason it shipped is now a case study.** The user pressed Start on Welcome — the app's default route (ADR D13) and the only way off it — and got an uncaught `AttributeError`: `welcome_presenter.py` called `self.event_bus.publish(...)`, but `publish` is the *app's* `IEventPublisher` while `BasePresenter.event_bus` is the *engine's* `IEventBus`, whose verb is `emit`. The slot logged `[WELCOME] Start requested.` and died on the next line, so the diagnostic agreed with the user's intention while nothing happened. `emit` is also the right design, not just the spelling that runs: `publish` takes an `IDomainEvent` and `StartRequested` is deliberately a UI intent. **Three nets, three different silences** — `IContainer.resolve` returns `Any`, so mypy checked nothing even though `src/shell/` *is* gated; the unit test's hand-written `_Bus` defined `publish` **and** `on` **and** `subscribe`, the union of two interfaces plus an invented verb, so a double written from the caller's needs could not disagree with the caller; and no tier imports `src/shell/` wholesale while an `AttributeError` in a Qt slot needs the signal to fire. Fixed as the mechanism: `emit`; the bus held under `self._bus: IEventBus` so the call is back under the type gate (measured — re-adding `publish` now fails mypy, and before the annotation the same edit was clean); the double replaced by `_RecordingBus(MemoryEventBus)`, the real bus recording through a real subscription, which made the assertion stronger by also proving publisher and subscriber keys agree; and a guard, `test_engine_port_calls_are_real.py`, reading every bus call in `src/`+`scripts/` against `IEventBus`'s real methods **by name**, which is what makes it immune to the `Any` that beat mypy. Regression test written first, red with the production traceback at the unit tier. **And the process change the user asked for:** `Docs/CASE_STUDIES/` with `CS-001` — a defect that got through a green gate gets one screen, a short **list** not an analysis, naming which net was silent and what checks it now; `bug-fix-rule` §6.5 makes it mandatory in the fixing commit, `testing-rule` §2 carries the double-shape clause, `ONBOARDING` §8 gains trap 12, `pr-review` gains E13/E14, and `test_case_study_index_is_consistent.py` enforces the form mechanically — unlisted file, dead citation, missing section, or over 35 lines. Gate: **PASS**, 4782 passed / 4 skipped, same log as PR 1.6c above |
| 2026-09-16 | p15 | **`BUG-125`: the case study's own list paid for itself within the hour.** `CS-001` ends with *"where else this is still open"*, and the first entry was "any `container.resolve()` result is `Any`". Reading it, I checked the other three collaborators `BasePresenter` exposes and found two more defects in the same file, one line below `BUG-124`: `self.logger.exception(...)` — the engine's `ILogger` has only info/warning/error/debug/critical/trace, `exception` is `logging.Logger`'s — on the developer-mode **failure** path, whose entire job was to leave the switch honest when the write failed, so the slot died and the toggle kept the clicked value; and `info("... %s.", enabled)`, where the port takes `(message, extra: dict \| None)` so the value landed in `extra` and every successful toggle logged a literal `%s`. Both reproduced against the real `StdLogger` before being touched. **Blast radius measured, not assumed:** of 241 `logger.*` call sites in `src/`+`scripts/`, 196 are the stdlib logger — which *does* have `exception` — and exactly **one** `ILogger` site was wrong. `backtest_presenter`'s nine `self._logger.log_*()` calls are a `BacktestEventLogger`, a different class; that is also why the guard is keyed on `self.logger` **inside a `BasePresenter` subclass** and not on the name `logger`, which means three different types in this tree, and why `config`/`dispatcher` were measured and left out. **The third net failed the same way again:** the test that drove the broken line was green because the container fixture answered `Mock()` for every port it did not name — so no new test was written; giving the fixture a real `StdLogger` made the existing one able to fail, which is `CS-001`'s lesson restated (the tier and the assertion were already right, only the double was wrong). `BUG-124`'s guard was renamed `test_engine_port_calls_are_real.py` and covers presenters' loggers too, including the printf shape; both new checks probed by re-introducing each defect. One correction of my own: the guard's docstring first claimed 20+6 bus and 14 logger call sites, copied from an earlier grep — running the guard's own collectors says **15 and 5**, and grep answers 26 because it counts docstrings and imports. The file now carries the measured number. Gate: **PASS**, 4785 passed / 4 skipped (4782 → 4785, the three new guard tests), log `logs/ci-local-20260916-031711.log` grepped — 0 WARNING/ERROR/CRITICAL records |
| 2026-09-16 | p16d | **PR 1.6d: six more groups into `support/ui_kit`, and a Phase 5 ratchet retired by accident.** `constants.py`, `state/` (13 files), `registry/` (8), `sidebar/` (9), `symbol_picker/` (6), `app_log_panel.py` — each a leaf once `kit/` had gone. **The unplanned result:** moving `registry/` deleted four of the nine lines in `baseline_shell_legacy_imports.txt`, which that file itself had scheduled for **Phase 5** (the Engine's `NavigationService` replacing `ScreenRegistry`). They retired four phases early and for a different reason than planned — `shell -> support/**` is permitted by the rule table, so the permission stops needing to be recorded. 9 → 5, and the file's "which phase removes which line" note now says so. **Two measurements on the way.** First, the mypy exclusion: these files leaving `src/presentation/` produced 6 errors, and unlike 1.6b and 1.6c **5 of them are** the PySide6 `@Property` false-positive class that exclusion exists for (EPIC-002A §2) — `Property("QVariant", ...)` takes a type name as a string at runtime while the stub says `type`, and the descriptor is read as its own type. The sixth is not that class and is not a defect either: the engine declares `PresenterManager.register(name, presenter_class: type, view_factory)` while its body only stores the argument in a dict and calls it later, so a factory callable is fine — verified by reading the method, which performs no `issubclass` check. So the two paths were **re-keyed** into the exclude list on PR 0.4a's precedent, with the reason and an exit written beside them: a path-only move neither fixes frozen debt nor creates it, and fixing 6 typing errors inside a move makes the move unreviewable. Second, **my own rewriter broke a file**: `sidebar/` contains `sidebar.py`, so rewriting `from .sidebar import Sidebar` into an absolute path made the package import itself. mypy caught it; the check that now closes the class is importing all 40 moved modules in one pass, which reports 0 failures. The same rewriter had needlessly absolutised 17 other intra-package relative imports — put back, because a relative import inside a package needs no change when the package moves, and each one was a diff hunk that changed nothing in a pull request claiming to be a pure move. Allowlist unchanged at 36 lines, styling census unchanged at 149 / 27 / 188, duplication still 59. **A third self-inflicted one, and the reason it is worth a line:** the relative-restore pass was right for every file in the moved packages except `preview.py`, which `scripts/preview_qml.py` imports **by path** rather than as part of its package — so a relative import there raises `ImportError: attempted relative import with no known parent package`, at discovery time, for every preview. The gate caught it. Closed as a class, not an instance: the preview discovery now reads **both** UI trees (it had stopped finding `sidebar` at all, which is what the guard actually reported), raises on a colliding key instead of silently shadowing a widget nobody could then open — zero collisions across the 14 `preview.py` files, measured — and a new test fails on a relative import in any `preview.py`, probed by putting the broken line back. Gate: **PASS**, 4786 passed / 4 skipped on the second run, log `logs/ci-local-20260916-034446.log` grepped — 0 WARNING/ERROR/CRITICAL records. 4785 → 4786 is the one new preview guard; the 40 moved modules and their mirrors move at the same count |
| 2026-09-16 | p16e | **PR 1.6e: four more groups, and a value object that no allowlist entry could have unblocked.** `theme_bootstrap.py`, `services/` (2 files, zero dependencies), `qml/embed` → `support/ui_kit/embed` and `environment_banner/` (3). The banner was a leaf **except** for `domain.value_objects.venue_alignment`, and the rule for `support/*` is absolute: it may not import the legacy tree at all, contracts or not — `_support_may_import` returns `False` for every legacy zone. So unlike a module's backward import there was no allowlist route; the VO itself had to move. HLD §02 had already assigned `VenueAlignment` to `support/binance_gateway/contracts` and its own imports were already from precisely there, so it was an in-plan move that unblocked the banner in the same pull request. Note what `qml/embed` moving means: the single place in the application where a `QQuickWidget` may be constructed now lives under `support/`, and `test_quick_widget_only_in_embed.py`'s landmark test is what failed to say so — `_EMBED_DIR` and the theme-seeding exemption both follow. **The import-check added in 1.6d paid for itself immediately:** `environment_banner/` contains `environment_banner.py` — the second package in this series whose name collides with a module inside it — and the rewriter made the package import itself again, `ImportError: partially initialized module`. Caught before the gate this time rather than by it. mypy clean on 439 files with no new exclusion, allowlist unchanged at 36, census unchanged at 149 / 27 / 188. Gate: **PASS**, 4786 passed / 4 skipped, log `logs/ci-local-20260916-040241.log` grepped — 0 WARNING/ERROR/CRITICAL records, count unchanged (a move, with its test mirrors moving at the same count) |
| 2026-09-16 | p16f | **PR 1.6f: `support/charting` arrives, and the boundary question it raised is the substance.** 37 files — `chart_card` (27), `timeframe_picker` (2), the QML `TimeframePicker` (8) — after three unblockings each measured first: `qt_platform.py` and `enum_labels.py` had **zero** app imports and went to `ui_kit`; `InfoField`, the one name `chart_card` needed out of `domain/indicator_scripts`, was split into `support/charting/contracts` where HLD §3.4 had already put it, with `base_indicator_script.py` re-exporting it so no consumer changed; and `qml/host.py`'s `QmlOverlay` went to `ui_kit`, since `support -> legacy` is refused outright and the QML dialog needs it. **The finding, and it is against the rule table rather than the code:** `support/charting -> support/ui_kit` was forbidden — `_support_may_import` permitted only `contracts/`. But HLD §6.1 already calls *both* packages UI: that is precisely why `_UI_SUPPORT_ZONES` exists and lets a module's `ui/` import them whole. Measured before changing anything: `chart_card` reads `ui_kit`'s palette, widget kit, QML embed host, `theme_bootstrap` and display-timezone service across **20 imports in 12 files**, so the alternatives were an ABC façade over `Palette`/`StyleRole`/`apply_role`/`QmlOverlay` written for a single consumer, or charting carrying its own copy of the kit — the duplication this epic exists to delete. So the rule grew the same allowance it already gave modules' `ui/`, **for those two zones only**, with six edges pinned in `test_boundary_rules.py` including the ones that must still fail (`binance_gateway -> ui_kit.kit`, `charting -> modules.*.contracts`, `charting -> domain`). Recorded in HLD §6.1 and §3.4. **Two more classes closed:** `chart_card/chart_card.py` was the third package-whose-name-collides-with-a-module-inside-it, and this time the fix is general — a pass that finds every such package and repairs the self-import — rather than one more file edited by hand. And `chart_toolbar.py` built its QML path as `parents[2] / "qml" / ...`: directory-hop arithmetic, which a move breaks by construction. `parents[2]` had been `presentation/ui` and became `src/support`, so the toolbar loaded nothing; it now reaches a sibling package via `parents[1]` and **raises at import** if the `.qml` is not beside it. Found by the all-modules import check, not by the gate. **mypy:** 43 pre-existing errors in 20 files came with the package. Classified by code before deciding, because "43 errors, excluded" is not a record — 13 `import-untyped` are `pyqtgraph` shipping no `py.typed` (a dependency decision, not this move's), 16 `attr-defined` are PySide6 stub gaps, 8 are `annotation-unchecked` bodies, 6 are real frozen typing debt in the geometry batching — and re-keyed on PR 0.4a's precedent. **Two were not re-keyed, because this pull request caused them and fixes them:** the `InfoField` re-export had to be explicit for mypy's implicit-reexport rule, and `qt_platform.py`'s `QCoreApplication.platformName` stub gap took a one-line local ignore rather than a whole path. Allowlist unchanged at 36, census unchanged at 149 / 27 / 188, QML baseline re-pathed at the same count of 27. **And the gate found the third instance of the guard-narrowing class, which is why this pull request also builds the seam that ends it.** `test_the_ceiling_is_not_stale` reported the bare-Qt-base ratchet at **1** instead of 2 — not progress: `_CachedFrameOverlay` left the scanned set with `chart_card`. A ratchet going *down* is the most expensive way for a narrowed scan to present, because it looks like the rule being satisfied. Same shape as PR 1.6a (`palette.py` left the colour guard's scan) and PR 1.6b (three widget guards had not been watching the surface host since 1.4b-1). Three repairs of one shape means the shape is the bug, so the roots now live in `tests/unit/architecture/ui_trees.py` and five guards plus their `scanned_roots_registry.py` rows derive from that one tuple: the next move edits it once, and forgetting fails every guard at once instead of quietly narrowing each. The card rule had to be **restated** rather than retargeted, too: "a Card lives in `components/`" cannot be said with one directory once the UI occupies three trees, so it is now a list of *card-owning packages* — the legacy component tree, the kit where `Card` is defined, and `chart_card`, which is a card's own package — with the documented intent unchanged (never alongside a screen). Gate: **PASS** on the second run, 4797 passed / 4 skipped, log `logs/ci-local-20260916-042823.log` grepped — 0 WARNING/ERROR/CRITICAL records. 4786 → 4797: +6 boundary-rule cases for the new allowance and the edges that must still fail, +4 non-emptiness cases for the third UI root, +1 for the card rule restated as owning packages. **Reviewed with the `pr-review` skill before merge (user decision 2026-09-16, one review per pull request), and it found two things against this pull request.** *Blocking, L6:* the `docs(ci)` commit carried the **entire 44-file `support/charting` rename** — `git mv` stages, so the next `git commit` took the whole index, which is exactly the trap that checklist row describes. Not cosmetic: a mixed commit is a code commit, so the documentation-only push exception applied to it did **not** hold. History rewritten into two correctly-scoped commits before merge; `master-warrior` never received it under the wrong exception, because the checkout that would have merged it was blocked by the work in progress. *Should-fix, J4:* `test_quick_widget_only_in_embed.py`'s landmark test still read the legacy tree alone, so it would not have noticed a `support/` tree disappearing — the seam existed and that guard still named the tree itself, which is the rot `ui_trees.py` was built to end. Both fixed in this pull request |
| 2026-09-16 | p16g | **PR 1.6g: `support/indicators` arrives, and two clauses of the design turn out to contradict each other.** 26 files — `domain/{indicators,indicator_scripts,scripting}`, `application/services/indicator_script_registry.py` and `components/indicator_scripts` as `ui/`. The cleanest move of the series: measured before cutting, the **only** external imports were `core.vo` and `support/charting/contracts`, both already permitted, so nothing had to be unblocked first. It is also the first time the epic has taken code out of `src/domain/` rather than out of the UI. **The contradiction.** `test_module_domain_is_qt_free.py` reserved `support/indicators/**` as Qt-free in Phase 0 — before the package existed — while HLD §3.5 assigns the same package a `QAbstractListModel`. Both clauses were written in good faith and neither was wrong; they simply could not both hold once the package was real. Resolved without weakening either: the guard now names the three Qt-free **sub-packages** rather than the package, which is exactly how `modules/*` is written two lines above it, and the boundary rule's *importing* side widened once — a support package's own `ui/` is display code and may use the UI kit, as `modules/X/ui` may. The edges that must keep failing are pinned: `binance_gateway -> ui_kit` (no `ui/`, no reason), `indicators.indicator_scripts -> ui_kit` (the mathematics reaching for a widget — forbidden from the other side too, by the Qt-free guard), and anything in `support/` reaching a module or the legacy tree. **mypy: 2 errors, both fixed rather than re-keyed** — `rowCount`/`data` annotated `QModelIndex` where Qt's supertype takes `QModelIndex \| QPersistentModelIndex`, a real Liskov violation. The fix applied what the repository had already written: the `AnyIndex` alias in `order_book/table_models.py`. It had to *move* to `support/ui_kit/model_indexes.py` first, because reading it where it was is `support -> legacy` and stays refused after that file becomes `modules/trading/ui` — a second copy of a four-line alias per tree is the duplication this epic deletes. mypy clean on 448 files with **no** new exclusion, which is the first move in this series to manage that. **`components/strategy_params` did not come:** `bot_params_form.py` imports `BaseStrategy` from `domain/strategies`, which is Phase 2's, so §3.5's assignment of it to this package cannot be satisfied yet — and a form rendering *a strategy's* parameters reads more like `modules/strategy/ui` anyway. Recorded as **open** for the user, same shape as `sync_progress_*`. **And the gate found the second instance of the `parents[N]` class, so this pull request closes it.** The moved `test_indicator_script_conventions.py` computed `_BOT_ROOT` as `parents[3]` — correct at `tests/unit/domain/`, one level too shallow at `tests/unit/support/indicators/`, so it went looking for `tests/src/binance_bot_module.py` and raised `FileNotFoundError`. PR 1.6f's `chart_toolbar.py` was the first. A hop count is broken by any change of depth, which is every move this epic makes, so there are now two answers: that file finds the root by a **landmark** (`pyproject.toml` above it), and `tests/unit/architecture/test_no_root_is_found_by_counting.py` checks the whole tree — it reads every `<ROOT-ish name> = ...parents[N]` and asserts the value really is the repository root. Matching by *name* is the trick: no import, no execution, only the author's own statement of intent. Subject measured at **49** such assignments today, all currently correct, and probed by mis-counting one (it names the file, the line and what the expression actually resolves to). A wrong hop count now fails in milliseconds at the file that has it, instead of three and a half minutes later somewhere downstream. Allowlist unchanged at 36, census unchanged at 149 / 27 / 188. Gate: **PASS** on the second run, 4808 passed / 4 skipped, log `logs/ci-local-20260916-045218.log` grepped — 0 WARNING/ERROR/CRITICAL records. 4797 → 4808: +3 boundary-rule cases for the widened importing side, +2 for the new hop-count guard, +5 non-emptiness cases for its four roots and the retargeted script-conventions row, +1 the moved registry key |
| 2026-09-16 | b126 | **`BUG-126`: measuring `ui/common` for the next extraction found `EPIC-008` §1's P1 finding still open two epics later.** `safe_ui_action` publishes `UiActionFailedEvent` with a full traceback and `TaskManager` publishes `TaskFailed`; `SystemErrorFeed` was the only subscriber of either, and **nothing ever constructed it**. Its subscriptions live in `BaseFeed.__init__`, so a class nobody builds subscribes to nothing — every swallowed UI failure and every dead background task reached nobody, for the whole period the epic that fixed it was closed. Found while listing `ui/common`'s 17 remaining files by consumer count: this one read **0**. **No user could have reported it**, because the defect's nature is that it produces no output; `BUG-124` is the concrete cost, and its traceback survived only because `app_bootstrapper` happens to catch `sys.excepthook`, which `safe_ui_action` bypasses by catching first. **Four nets green, a fifth actively protecting it:** the feed's own test constructed it (the one step production omitted, so that file could never fail for this); `test_event_flow_guards.py` checks a subscription addresses its event by class, which is true of one that never runs; ruff's dead-code rules see an unused *import*, never an unreachable class; mypy checks one either way; and HLD §3.5's **re-measured** dead-code list said *"Live, do not delete: imported by `order_feed.py`"* — `order_feed.py` only **mentions** it in a docstring, and a text search cannot tell a mention from an import. That row is corrected in this pull request. **Fixed as the mechanism** (`bug-fix-rule` §2 names 'add the same wiring to every Presenter' as the tell of a hotfix): one `SystemFailureLog`, constructed by the composition root before `boot()`, reporting at `ERROR` with a `[system-failure]` tag into the log file `--dev` writes and `ci-local.ps1`'s scan greps. Three properties decided it and each is a reason the feed was the wrong shape — one owner at boot rather than one per screen; **no Qt**, because a logger is thread-safe, so the CLI entry point with no `QApplication` gets it too; and it reports somewhere that exists, since this app has no app-wide log panel and wiring N screens is the duplication the rule forbids. The feed is **deleted**: `base_feed.py`'s own rule is to promote a fact when the *second* consumer appears, and there was never a first. **The guard, `test_a_bus_subscriber_is_constructed.py`:** a class calling `.on(...)` must be named elsewhere in `src/`+`scripts/`, read from the **AST** — a first draft grepped the text and called `SystemErrorFeed` reachable off those two docstrings, which is the HLD's own error reproduced. *Named* rather than *called*, because a screen class is handed to a registry as a bare reference; measured, that threshold separates cleanly — 8 reachable, 1 orphan, 0 false positives — and a third test feeds it a synthetic orphan in prose and requires red, then one construction and requires green. The regression test asserts against the graph `create_app()` builds, the only tier that can fail for this bug, red beforehand with `assert ['UiActionFailedEvent', 'TaskFailed'] == []`. **`core/contracts/` was the first home for the report type and the Qt-free guard refused it** — `UiActionFailedEvent` lives under the engine's `pyside_mvc` extension — and hiding that behind `if TYPE_CHECKING:` would have satisfied the guard while answering nothing, since with the feed deleted there is exactly one consumer. It sits beside that consumer in `shell/`. Written up as `CS-002`, with `ONBOARDING` §8 trap 13, a `testing-rule` §2 clause and `pr-review` E15. **Reviewed with the `pr-review` skill before merge, and it found two things against this pull request.** *Blocking, D4:* `test_system_error_report.py` had a function-local `from ... import task_manager` — `code-quality-rule` §4 bans a local import in a test case with no exception. *Should-fix:* the test that import served asserted on the **engine's source text** (`'name or (' in source`) to justify the normaliser reading `task_name` with no fallback — a text assertion over another repository's formatting, which is precisely the measurement this whole pull request is about. Both removed by making the fallback unnecessary: the summary now names `task_id` as well as `task_name`, which a reader needs anyway to correlate with the other `runtime.tasks.*` events, so an empty name degrades the line instead of leaving a dead end. Allowlist unchanged at 36, census unchanged at 149 / 27 / 188 |
