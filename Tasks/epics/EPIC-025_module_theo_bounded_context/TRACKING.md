# EPIC-025 — Tracking (Gantt)

- **How to read it.** One bar per pull request or review; `done` is merged, `active` is being
  worked, `crit` marks a review or a hands-on check that only the user can do, `milestone` marks
  a phase closing. Dates after today are **planning estimates**, revised every time a bar closes —
  HLD §6.4 says only Phase 0 can be estimated before Phase 0 has run, and the Phase 1+ bars will be
  re-cut when Phase 0's measurements are in.
- **Who updates it.** The executor (the EPIC-025 skill, checklist step 11) moves a bar to `done`
  in the same pull request that closes it, and re-dates the following bars if the estimate moved.
- **Renders** on GitHub, in VS Code with a Mermaid extension, or at mermaid.live.

```mermaid
gantt
    title EPIC-025 — module split on the Engine's IExtension microkernel
    dateFormat  YYYY-MM-DD
    axisFormat  %d/%m
    todayMarker stroke-width:2px,stroke:#d33,opacity:0.6

    section Spec
    Round 1 — HLD, ADR, EPIC scaffold           :done,    s1, 2026-09-10, 2026-09-11
    Round 2 — doctrine, workbench rule, SDD     :done,    s2, 2026-09-12, 2026-09-13
    Independent review + round 3               :done,    s3, 2026-09-13, 1d
    D17 sizing, D18/D19 tests, executor skill  :done,    s4, 2026-09-13, 1d
    User review of the spec                    :crit, done, s5, 2026-09-13, 1d

    section Phase 0 — mechanism + market_data (EPIC-025A)
    PR 0.1 baselines and guards                :done,    p01, 2026-09-13, 1d
    User review 0.1 (allowlist, golden master) :crit, done, r01, 2026-09-13, 1d
    PR 0.2 core/ shell/ mechanism              :done,    p02, 2026-09-13, 1d
    User check 0.2 (app opens as before)       :crit, done, r02, 2026-09-14, 1d
    PR 0.3 support/binance_gateway             :done,    p03, 2026-09-14, 1d
    PR 0.4a modules/market_data behind its UI    :done,    p04a, 2026-09-14, 1d
    PR 0.4a-2 CLI inverted, 0.4a-3 contracts    :done,    p04a2, 2026-09-14, 1d
    PR 0.4b-1 Data Management in QtWidgets      :done,    p04b, 2026-09-14, 1d
    PR 0.5 skeleton walks, N=4 not 2 (#214)     :done,    p05, 2026-09-14, 1d
    User check 0.x (app runs: 3 screens + CLI)  :crit, done, r0x, 2026-09-14, 2d
    Phase 0 measured, Phases 1–5 re-estimated  :milestone, done, m0, 2026-09-15, 0d

    section Engine track
    E0 ScheduledJob.cancel()                   :         e0, after s5, 1d
    E1 lift module base + registry (after Phase 1) :     e1, after m1, 3d
    E2 lift surface runtime (after Phase 2)    :         e2, after m2, 3d
    E3 NavigationService, lifecycle, conformance :       e3, after p5, 5d

    section Phase 1 — trading + surfaces (EPIC-025B)
    PR 1.1a IHistoricalKlines + its 6 consumers :done,   p11a, 2026-09-15, 1d
    PR 1.1b IMarketStream + its two screens    :done,    p11b, 2026-09-15, 1d
    PR 1.2 ISymbolCatalog + IRangeCoverage     :done,    p12, 2026-09-15, 1d
    PR 1.3a modules/trading, the move          :done,    p13a, 2026-09-15, 1d
    PR 1.3b its three ports + first consumers  :active,  p13b, 2026-09-15, 2d
    PR 1.3c the two Presenters, CLI, factory   :         p13c, after p13b, 3d
    PR 1.4 Trading + Dev Board as surfaces, 59→0 :       p14, after p13, 5d
    PR 1.5 Welcome surface, dev.mode gate, restart :     p15, after p14, 3d
    User runs Testnet (orders, cancel, PnL)    :crit,    r1, after p15, 2d
    Phase 1 closed                             :milestone, m1, after r1, 0d

    section Phase 2 — strategy (EPIC-025C)
    modules/strategy, ISizingPolicy, per-symbol session : p2, after m1, 5d
    User runs Testnet (arm, disarm, tick→order) :crit,   r2, after p2, 2d
    Phase 2 closed                             :milestone, m2, after r2, 0d

    section Phase 3 — backtesting (EPIC-025D)
    modules/backtesting, 11 modals → QDialog, golden master : p3, after m2, 8d
    Phase 3 closed                             :milestone, m3, after p3, 0d

    section Phase 4 — support/*, ui/common dissolved (EPIC-025E)
    charting, indicators, ui_kit; Settings dialog; qml/ deleted : p4, after m3, 5d
    User review: designed theme? (D21 deferred)  :crit,    r4, after p4, 1d
    Phase 4 closed                             :milestone, m4, after r4, 0d

    section Phase 5 — Engine navigation (EPIC-025F)
    ScreenRegistry → NavigationService         :         p5, after m4, 4d
    Conformance suite on every surface         :         p5b, after e3, 2d
    EPIC-025 done                              :milestone, m5, after p5b, 0d
```

## Status log

| Date | Bar | Event |
| :--- | :--- | :--- |
| 2026-09-11 | s1 | Round 1 merged (PR #196) |
| 2026-09-13 | s2, s3, s4 | Rounds 2–3, D17–D19, executor skill merged (PRs #197–#205) |
| 2026-09-13 | — | ADR D20–D22 (QtWidgets only, OS theme, panels and dialogs): Phase 0/1/3 bars re-cut |
| 2026-09-13 | s5, p01 | User approved the spec; Phase 0 started with PR 0.1 |
| 2026-09-13 | p01 | PR 0.1 built locally: 3 guards in `tests/unit/architecture/`, allowlist (10 pairs), QML baseline (35), golden master (13 trades), 3948 → 4059 tests collected; self-review split the guards into one file per abstraction level (architecture-rule §5); waiting for the user's review before push |
| 2026-09-13 | r01 | Review 0.1: the user pointed at the decision doctrine (§7) instead of answering; both questions settled by it in `EPIC-025A` §1.1 — allowlist as found, dataset stays generated. The pull request itself still waits for the user's push OK |
| 2026-09-13 | p02 | PR 0.2 built locally: theme layer's global sheet removed (ADR D21a), `core/` + `shell/` with the contribution mechanism, one ConfigManager, the five legacy screens as contributions, two new architecture guards; gate green, 4 153 tests |
| 2026-09-14 | r02 | PR 0.2 merged (PR #210) into master-warrior; work branch fast-forwarded. Phase 0 mechanism is in main; next is PR 0.3 (support/binance_gateway) |
| 2026-09-14 | p03 | PR 0.3 (PR #211): `support/binance_gateway` extracted — 9 modules and 6 tests moved; boundary allowlist 10 → 8 pairs, the epic's first retired entries; `exchange_session_factory` and `binance_error_translator` deferred with reasons (EPIC-025A §1.3) |
| 2026-09-14 | p03, p04 | PR 0.3 merged (PR #211); `src/src`, a dangling symlink committed by accident in `8dc9b3ef`, deleted (PR #212). PR 0.4 split into 0.4a (the module, behind the existing UI) and 0.4b (Data Management in QtWidgets) — two different kinds of risk, EPIC-025A §1.4; 0.4a started |
| 2026-09-14 | p04a | PR 0.4a + 0.4a-2 + 0.4a-3 pushed into PR #213 (8 commits, gate green): `modules/market_data` registered in `shell/modules.py`; CLI parsing inverted so the shell parses once and the module owns `sync`/`stream`; contract suites and verified fakes for `ISymbolCatalogRepository` and `IMarketDataRepository`, which deleted 253 lines of hand-rolled in-memory duplicates. Two bugs found and fixed on the way (BUG-118 leaked Qt objects, BUG-119 an endless `while True` the gate could not report), and `pytest-timeout` installed so a hang fails loudly. Allowlist 41 → 38 |
| 2026-09-14 | p04b | PR 0.4b-1 built locally, gate green (4 286 tests, coverage 95.09%): Data Management's last two QML islands rebuilt as a `QTableView` panel with the app's first four `QAction`s and a kline-inspector `QDialog`; 4 `.qml` files deleted, QML baseline 35 → 31, `Theme.*` bindings 229 → 204, net -1 128 lines. The spec's list of which four widgets was wrong and is corrected in EPIC-025A §1.7 (the time-range and timeframe pickers are shared with four other screens). Two guards written under the pre-ADR doctrine were answered with their own `base-exempt` hatch rather than a raised ceiling. Two questions left for the user: the unreachable integrity audit, and jump-to-date. Waiting for the push OK |
| 2026-09-14 | p04b | 0.4b-2 (move the screen into `modules/market_data/ui/`) **deferred to Phase 4**, measured rather than judged: 8 forward allowlist entries would be traded for 35 backward ones, and the epic's zero-backward-imports property would become 35. A module's `ui/` may import `support/ui_kit`/`support/charting` whole (HLD §6.1) and neither exists before Phase 4, which is where eight of the nine import groups are heading anyway. Recorded in EPIC-025A §1.8, carried into EPIC-025E step 6, and the 8 allowlist entries re-keyed to Phase 1 |
| 2026-09-15 | p11a | PR 1.1a: `IHistoricalKlines` published — a verified fake, a twelve-guarantee contract suite run against **both** that fake and the real query handler, and all six readers of stored candles moved onto it (the trading chart, two backtest coordinators, the Dev Board stream controller, the kline inspector, the CLI's `trade-once`). Allowlist 34 → 28. What the port replaces is a handler whose return type changed with its argument's *runtime type* (`architecture-rule` §2.1): that is what the chart's `getattr(response, "data", response)` + `.get(symbol, [])` + two `isinstance` guards existed for, and four defensive steps became one call. The move also exposed four invalid test fixtures a stubbed dispatcher had been hiding — three sets of candles sharing one `open_time` (one row by primary key in any real store) and one anchored to 2024 while the screen asks a 7-day window — each fixed at the data, never by widening the port. Self-review (`pr-review` skill) caught three blocking defects before the push: one construction site left behind, two tests passing a *pytest fixture function* where the fake belonged, and one test left asserting nothing after its stub went dead |
| 2026-09-15 | p11a, p11b | PR 1.1 **split in two**, and the reason is review size rather than risk: moving the six `IHistoricalKlines` consumers alone touched 28 files and broke 65 tests that had to be repaired one by one, because a stubbed dispatcher had been standing in for a store. Putting `IMarketStream` and the `interactive_shell.py` move in the same diff would have doubled that without changing what could fail. 1.1b carries the remaining 6 of PR 1.1's 12 allowlist entries (28 → 22 as planned; it reached 24, see the 1.1b rows below for the measurement that moved the last two to PR 1.3). The Phase 1 bars are also re-cut here to the five pull requests `README` §"the cut" argues for — ports first, surfaces last — which the old three bars (Welcome first) contradicted |
| 2026-09-15 | p11a | PR 1.1a's review cleanup (gate PASS, 4 397 tests, coverage 95.14%): the nine *should fix* / *nit* findings the review left open. The two that were more than tidying: `ChartFeedCoordinator` had been left holding a `dispatcher` it never read once the candle read moved onto the port — a constructor parameter nobody uses still tells every caller that the class talks to the bus — and the `GetHistoricalKlinesQuery` **dispatch surface was dead**, with nothing in `src/` dispatching it after the six consumers moved. The query, its binding and the `execute()` that returned `list | dict` by runtime type are deleted; the class is now `StoredKlinesReader`, named for what it does, and the union `architecture-rule.md` §2.1 forbids is gone from the codebase rather than merely unused. Three tests got back the assertion their name promised (a realtime run reads nothing, the click handler reads nothing on the main thread, the invalid range reaches no store) and one got its ordering check back across all three mechanisms via a journal. `conftest.build_mock_klines` moved to a plain `mock_klines.py`: importing a *conftest* by name runs it twice under two module identities, which aborted the whole UI integration tier — found by making that mistake here, and it also retires the drifted copy in `test_dev_board_load_more.py` |
| 2026-09-15 | — | `BUG-121` filed while verifying that cleanup: the 47 UI integration tests hang or abort when run in one process, **on the merged tree as well** — a worker from a finished test still feeding the script runner while the next test sets up, the shape `BUG-056` fixed once. The gate cannot see it (xdist splits the tier, `pytest-randomly` reorders it) and `pytest-timeout`'s `signal` method cannot fire on a main thread stuck in Qt's C++ loop, which is the concrete gap `BUG-119` left. Reported with five measured runs per tree and four next steps; not fixed here, because root-causing cross-test state leakage in a Qt tier is its own task |
| 2026-09-15 | p11b | PR 1.1b: `IMarketStream` published — a verified fake, a thirteen-guarantee contract suite, and the two screens that open the live kline websocket (the trading chart and the Dev Board) moved onto it. Allowlist 28 -> 24. What the port removes besides the import is the same `getattr` probing 1.1a found on the history read, in its most dangerous form: `getattr(response, "success", True)` reports success for **any** object without that field, `None` included — so a stream that never opened told the user it was streaming. `StreamOutcome` is a named type with both fields, and `MarketStreamService` reports an unanswered dispatch as a failure. The port dispatches the module's two existing commands rather than reaching past them to `ILiveStreamService`: the module's own CLI dispatches them too, and skipping them would give the CLI and the screens two paths to one websocket. The contract suite's real half runs the whole chain down to `BinanceWebsocketService`'s reference counting with only `_run_stream` stubbed — a second fake would have agreed with the first and with nothing in production. Three consumers also lost a `dispatcher` they no longer used (`ChartFeedCoordinator` in 1.1a's cleanup, then `ChartCoordinator` and `StreamLifecycleController` here), and the trading Presenter tests stopped resolving three foreign ports to `MagicMock`s |
| 2026-09-15 | p11b, p13 | `interactive_shell.py` into `shell/` **moved to PR 1.3**, measured rather than judged: the move was made, the ratchets read, and it was reverted. `exchange-status` is trading's CLI handler with no owning module until 1.3, so the shell keeps importing it from the legacy tree — 2 allowlist entries traded for 2 new `baseline_shell_legacy_imports.txt` lines, a shrink-only ratchet growing so another can shrink. So 1.1b retires 4 of the 6 lines the plan gave it, and the epic README now carries 1.1c as the moved row |
| 2026-09-15 | p12 | PR 1.2: `ISymbolCatalog` and `IRangeCoverage` published, with their four consumers moved — the shared symbol picker (three screens read it), Data Management's auto-discover, and Backtest's chart-preview and data-sync coordinators. Allowlist 24 -> 20, and `market_data` now publishes **all five** ports HLD §3.4 assigns it. Both queries were deleted with their bindings, for the reason PR 1.1a's cleanup established: once the consumers had moved, nothing in `src/` dispatched either one. `BUG-072`'s `getattr(coverage_response, "data", coverage_response)` went with the dispatch — the unwrap existed because a test double's envelope reached a Qt signal typed `object` and **crashed the interpreter**, and a typed return makes the envelope unbuildable. Three things were fixed on the way: `normalised_symbols` lives in `contracts/` rather than beside the service, because a fake in `contracts/testing/` importing it from `application/` would have been the module's first inward import; the repository's fake was renamed `FakeSymbolCatalogRepository` (it had taken the published port's name, while `FakeMarketDataRepository` next door already followed the convention); and `DataSyncCoordinator`'s tests stopped driving a four-field `SimpleNamespace` where production hands over an eight-field `BacktestRangeCoverage`. `ChartPreviewCoordinator` and `DataSyncCoordinator` also lost the `dispatcher` they no longer used — the fourth and fifth such parameter in three pull requests |
| 2026-09-15 | p12, p13 | The two `-> modules.market_data.adapters` lines the epic README gave 1.2 stay where they are, and the allowlist's own comment already said why: `exchange_session_factory` needs splitting one factory per context (it answers both market_data's and trading's factory interfaces, and splitting a shared instance is a behaviour change ADR D12 keeps out of these PRs) — that is PR 1.3's, with `modules/trading`. `backtest_presenter`'s `InMemorySymbolMarketMetadataCache` fallback needs the cache registered in DI and the screen rebuilt, which is Phase 3. So 1.2 retires 4 of the 6 the README listed; the README row is corrected rather than left claiming six |
| 2026-09-15 | p13a, p13b | **PR 1.3 was cut in two, and the halves run in the opposite order to the one the epic README proposed.** Splitting it was the user's call (a single pull request would have been ~3000–4900 lines over the live-order path, with three new ports, on state 21 files read). Putting the **move first** was not a choice: `boundaries/rules.py` returns `False` for any new-tree import of the legacy tree outside the allowlist, and every type the three ports need was in the legacy tree — `IOrderSubmission` needs `OrderPreview`, `ITradingSession` needs `EnableTradingResult`, `IAccountSnapshot` needs `ExchangeConnectionStatus`. `market_data` could publish `IMarketDataSync` at PR 0.5 only because PR 0.4a had already moved its code in, so "publish the port, leave the code" was never a context's *first* step. 1.3a is trading's 0.4a; 1.3b is its 0.5 |
| 2026-09-15 | p13a | PR 1.3a: `modules/trading` exists. `src/application/ports/` (5 files) and `src/application/use_cases/queries/` (3 packages) turned out to be **entirely** trading's and moved whole; `domain/trading`, `use_cases/trading` less arm/disarm, the trading half of `infrastructure/binance` and four services followed. Trading's value objects, six order enums, `FuturesSymbolMetadata` and the seven events it raises went to `contracts/` rather than `core/vo`, on HLD §2.4 measured the way `SymbolMarketMetadata` was — which is why **26 would-be allowlist entries do not exist**: the legacy tree may import `modules.*.contracts` freely. Allowlist 20 → 80, and the two directions are not the same debt: **6** point out of the new tree (3× the shared `ExchangeSessionFactory` instance, `MarginRiskPolicy` sanctioned by the task file until Phase 2, `position_sizing` and `signal_action` which are `strategy`'s vocabulary) and **61** point in, each a consumer waiting for a port. The five `application -> infrastructure.binance.futures_trading_client` entries this whole guard was written for are **gone**, by becoming intra-module rather than by being excused. Gate: 4515 passed / 4 skipped, log file grepped |
| 2026-09-15 | p13a | Three things the move taught, each now recorded where it can be read again: a dotted path **split across two string literals** (`"...src.infrastructure.binance."` `"futures_user_data_stream.AsyncClient"`) survives a mechanical import rewrite invisibly — four `patch()` targets in one file, found only by the gate; `tests/unit/modules/` uses real packages all the way down, and creating the leaf `__init__.py` without its parents made the test directory a top-level package named `binance` that collided with the installed library **and** made ruff reclassify a third-party import as first-party; and PR 1.3a's own new reference checker (review finding S3, merged hours earlier) caught `bolt.prompt.md` pointing at the query directory this pull request emptied — the briefing now names the work and gives the command that finds it, per `.agents/Skills/README.md` §1 |
| 2026-09-15 | r0x, m0 | **Phase 0 CLOSED.** The user ran the app and confirmed the four checks the gate cannot make: the Trading screen loads history, Dev Board's "Start Live" syncs then streams, Data Management syncs a symbol, and the CLI `sync`/`stream` commands work. `EPIC-025A` moves to `completed/` — the first phase of this epic to close, and the commit that created that directory. The bar stayed open for a day after the code was complete, which is the epic README §3 rule working as intended: a phase ends with the app running, not with a green gate |
| 2026-09-15 | p13b | PR 1.3b: `trading` publishes `IOrderSubmission`, `ITradingSession` and `IAccountSnapshot`. Two things had to precede them, and the first corrects the spec: HLD §3.4 said *"`LivePosition` never leaves the module; `PositionSnapshot` is its flat DTO"*, and measured, `Order` is a frozen dataclass with eleven fields and **zero** methods while `LivePosition` is frozen with eight scalars, a `NewType` over `Decimal` and one pure derivation — a `PositionSnapshot` would be field-for-field identical, which is the behaviour-free churn PR 1.2 refused when it kept the name `BacktestRangeCoverage`. So both moved into `contracts/` under their own names and **20 allowlist entries vanished without one consumer being touched**. Two further types the spec named were never written at all (`AccountSnapshot`, `PositionSnapshot`) and `OrderIntent` had to become `OrderRequest` because two other classes already hold that name; HLD §3.4's four trading rows are corrected in the same pull request. Allowlist 80 → 56 |
| 2026-09-15 | p13b | The finding worth more than the import count: four presentation files were reading `enabled`, `orders_sent_this_session` and `known_open_symbols` **straight off the mutable, lock-guarded `TradingSessionState`** — a Presenter on the UI thread while the websocket thread rewrote the symbol set. `TradingSessionState.read_all()` is new and takes the lock once, so `ITradingSession.snapshot()` can answer a frozen three-field value that cannot be a combination which never existed, and it copies the set inside the lock rather than handing out the live object. The symbol lease is deferred to Phase 2 for `architecture-rule` §7.2.1's reason — `strategy` is its first consumer, and a lease with no caller is new locking behaviour on the riskiest state in the app |
| 2026-09-15 | p13b | Consumers moved so far: Settings (both ports) and the two `exchange-status` CLI entry points — 4 more entries retired. All five of `trading`'s fakes and suites now exist (`BUG-120`'s guard named the seven helpers that needed tests, and got them). The two big Presenters, the order CLI commands and `LiveTradingCoordinator` are **PR 1.3c**, with the `ExchangeSessionFactory` split and the DI move: one pull request per kind of risk, which is what splitting 1.3 was for. Gate: 4554 passed / 4 skipped, log grepped |
| 2026-09-15 | p13c | **PR 1.3c was attempted, measured and reverted — the measurement is the point.** Moving the two big Presenters and `arm`/`disarm` onto `ITradingSession` is 10 allowlist entries and *looks* like the Settings move that preceded it. It is not: the change itself is mechanical (one resolve, three dispatches and seven or eight state reads per Presenter, all of which lint and type-check clean), and the gate then reported **72 failures and errors across 12 test files** plus a **log-scan hit the two previous green gates did not have** — `[UI Thread Bridge Error] Exception in _recheck_edge: Signal source has been deleted`, a Qt lifetime error that surfaces once dashboard tests error at setup and teardown runs against half-built objects (the `BUG-056`/`BUG-121` family). Those test suites stub `TradingSessionState` in dozens of places and assert on `dispatcher.dispatch(EnableTradingCommand, ...)`, so each one needs the verified fake and an assertion on the effect rather than the call — PR 1.1a's shape exactly, where the consumer move alone touched 28 files and repaired 65 tests. The working tree was returned to the last green commit rather than left red; 1.3c starts from this number instead of from an estimate |
| 2026-09-15 | p13c | So 1.3c's scope, measured rather than guessed: the `ITradingSession` slice (10 entries, ~72 test repairs), the `IOrderSubmission` slice (the two Presenters, `trade_once_cmd`, `order_dry_run_cmd`, `order_preview_cmd`, their two formatters and `LiveTradingCoordinator`), `IAccountSnapshot.open_positions()` for the Dev Board, a home for `EquityCurveRecorder` (2 entries, and it is a *service* the chart adapter reads rather than a port call — it may belong in `contracts/` or behind a narrow port, and that is a decision, not a move), the `ExchangeSessionFactory` split one-per-context with the DI registrations following it, and 1.1c's two `interactive_shell` lines. Allowlist 56 → 13 |

