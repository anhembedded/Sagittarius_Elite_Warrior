# 🏛️ Epics — Sagittarius Elite Warrior

Quy ước mới (2026-08-20): mọi Epic từ giờ có **thư mục riêng của chính nó**,
không còn 1 file `.md` nằm chung với task thường trong `Tasks/backlog/`.
Lý do giống hệt lý do tách `Tasks/bug_report/` khỏi `ROADMAP.md`: 1 epic có
nhiều task con, nhiều lần cập nhật trạng thái — nhét hết vào 1 file phẳng
hoặc vào bảng Kanban chính làm cả 2 nơi đều khó đọc.

## Bố cục

```text
Tasks/epics/
├── README.md                          # File này
└── EPIC-XXX_slug_ngan_gon/
    ├── README.md                      # Tổng quan epic: mục tiêu, bối cảnh, danh sách task con
    ├── incomplete/                    # Task con CHƯA xong — task mới luôn tạo ở đây
    │   └── EPIC-XXXA_ten_task.md
    ├── completed/                     # Task con ĐÃ xong, dời từ incomplete/ sang
    │   └── EPIC-XXXB_ten_task.md
    └── cancelled/                     # Task con bị HUỶ — chỉ tạo khi thật sự cần
        └── EPIC-XXXC_ten_task.md
```

- **ID Epic**: `EPIC-XXX` (số tăng dần riêng, không dùng chung pool với
  `BOT-XXX`/`BUG-XXX` — độc lập, giống cách `BUG-XXX` đã tách khỏi `BOT-XXX`
  từ trước). Epic tiếp theo lấy số lớn nhất đang có trong `Tasks/epics/` + 1.
- **ID task con**: `EPIC-XXX` + chữ cái (`EPIC-001A`, `EPIC-001B`, …) — tự
  chứa trong đúng thư mục epic của nó, không cần đăng ký ở pool nào khác.
- **Khi task con xong**: `git mv incomplete/EPIC-XXXA_*.md completed/`, cập
  nhật `Status` trong file, cập nhật dòng tương ứng ở bảng trong epic's
  `README.md`.
- **Khi task con bị huỷ**: `git mv` sang `cancelled/` (tạo thư mục nếu chưa
  có), **không xoá file**. Lý do phải là "việc này không còn tồn tại/không còn
  đúng", và **viết ra ở đầu file** — huỷ mà không ghi lý do thì lần sau sẽ có
  người mở lại đúng task đó. Bảng ở epic's `README.md` giữ dòng của nó với
  trạng thái ❌ để lịch sử không bị thủng.

  Thư mục là nguồn sự thật mà người ta *liệt kê*, README là thứ người ta *đọc*.
  Để một task đã huỷ nằm lại `incomplete/` thì `ls` vẫn báo nó là việc đang
  chờ, dù README nói ngược lại — đó chính là kiểu lệch đã khiến `EPIC-003D`
  bị đưa vào danh sách việc còn lại nhiều lần. Tiền lệ có sẵn ở cấp repo:
  [`Tasks/cancelled/`](../cancelled/).
- **`Tasks/ROADMAP.md` chỉ giữ 1 dòng liên kết** tới `README.md` của epic —
  không chép lại nội dung, không viết đoạn dài. Muốn biết chi tiết thì mở
  thẳng file epic. (Áp dụng từ epic mới tạo theo quy ước này trở đi — các
  epic cũ dạng file phẳng trong `Tasks/backlog/`/`Tasks/completed/`
  — ví dụ `BOT-109`, `BOT-112`, `BOT-115` — **không** bị di dời ngược lại,
  để tránh phá link đang trỏ tới chúng mà không ai yêu cầu.)

## Epic đang có

| ID | Tên | Trạng thái |
| :--- | :--- | :---: |
| **[EPIC-001](EPIC-001_ema_trend_pullback_tradingview_cross_reference/README.md)** | Đối chiếu `EmaTrendPullbackStrategy` với TradingView thật | 🟡 Đang làm (1/2 task con xong) |
| **[EPIC-002](EPIC-002_static_type_checking_in_local_ci/README.md)** | Kiểm tra kiểu tĩnh (`mypy`) trong CI cục bộ | ✅ **Hoàn thành (5/5 task con)** — 2026-09-08; `EPIC-002D` đóng theo quyết định user, `--strict` sống thật ở 3/4 layer `src/` (`domain`/`application`/`infrastructure`), `presentation/` cố ý để ngoài (false positive `@Property` PySide6) |
| **[EPIC-003](EPIC-003_presenter_and_god_file_decomposition/README.md)** | Phân rã Presenter/File quá tải (Coordinator Pattern, Domain Policy, QML) | 🟡 Đang làm (5/8 xong, 1 huỷ — `003D` hết đối tượng sau `EPIC-006F`; `003F1` xong 02/09) |
| **[EPIC-004](EPIC-004_static_security_and_quality_analysis/README.md)** | Static security & quality analysis gate (Bandit + magic-number + code-smell qua Ruff) | ✅ Hoàn thành (4/4 task con) — 2026-09-02 |
| **[EPIC-005](EPIC-005_qml_to_qtwidgets_migration/README.md)** | Rút khỏi QML về QtWidgets, **trừ chart** — theo từng màn hình, mỗi bước rollback được | ⏹️ **Bị thay thế bởi `EPIC-006`** — 6/6 task con xong (`005F` do `EPIC-006D/E` làm) |
| **[EPIC-006](EPIC-006_drop_qml/README.md)** | Bỏ hẳn QML, thuần QtWidgets | ✅ **Hoàn thành (6/6 task con)** — 2026-08-25; Elite hết sạch `.qml`. Kit QML của Engine ở lại (sample app cần) — tháo kit là task riêng repo Engine |
| **[EPIC-007](EPIC-007_chuan_hoa_card_dung_chung/README.md)** | **Chuẩn hoá card dùng chung, đưa hình dạng lên Engine** — gộp ~10 biến thể màu card về 1 token, 6 hình dạng surface lên `pyside_mvc.widgets`, cắt 3 import chéo màn hình | ✅ **Hoàn thành (8/8 task con, `007A`–`007H`)** — 2026-08-26; guard bare-base 17 → 2, trên `screens/` là 0 |
| **[EPIC-008](EPIC-008_chuan_hoa_luong_event/README.md)** | **Chuẩn hoá luồng sự kiện** — Shared Kernel + port, `BaseEvent` kế thừa được thật, `EventRegistry` + catalog sinh tự động, 3 Feed thay 48 signal cầu nối | ✅ **Hoàn thành (8/8 task con)** — 2026-08-25 |
| **[EPIC-009](EPIC-009_sanity_tier_redesign/README.md)** | **Thiết kế lại tầng Sanity** — mô hình composition-root (scan chứ không liệt kê), `diagnostic_guard`, tầng out-of-process `--self-check`, fake Binance REST server. 9 file/~24 lần boot → 2 file/1 lần boot | ✅ **Hoàn thành (5/6 task con, 1 hoãn)** — 2026-08-25; ADR *Approved*. Tìm và đóng `BUG-045`, `BUG-048`, `BUG-049` |
| **[EPIC-010](EPIC-010_ghi_nho_gia_tri_cuoi/README.md)** | **Ghi nhớ giá trị cuối** — ~45 giá trị user đặt đang mất sạch mỗi lần mở lại app; chỉ 5 key của màn Settings sống sót. Store riêng `ui_state.json` (không dùng `user_config.json`), slice theo màn, merge khi ghi | ✅ **Xong 2026-08-26 (8/8 task con)** — app nhớ geometry/route/sidebar, symbol+interval của Dev Board và Database, 21 giá trị form Backtest, và checklist indicator script. Cơ chế đã promote lên Engine (`ui_state` extension, Engine PR #216). Khôi phục **layout** đa-instance vẫn mở, đúng như design tự dán nhãn |
| **[EPIC-011](EPIC-011_dong_bo_skill_dinh_ky_jules/README.md)** | **Đồng bộ 7 skill chạy định kỳ (`.jules/`) với repo hiện tại** — 7 agent chạy không người trông, prompt của chúng đã trôi: cả 7 dùng sai cổng CI (`-UnitOnly` thay vì `-Full`), Sentinel trỏ vào một rule file chưa bao giờ tồn tại, 6/7 còn nhắm vào QML đã bị gỡ từ `EPIC-006`, Palette mất sạch bãi săn. Rút phần chung ra `.jules/README.md`, mỗi agent neo vào một nguồn việc tự làm mới, cộng một guard cơ học | ✅ **Hoàn thành (8/8 task con)** — 2026-08-26; guard sống trong `ci-local.ps1 -Full`, verify hai chiều |
| **[EPIC-012](EPIC-012_di_chuyen_skills_ve_agents_va_lich_2_ngay/README.md)** | **Dời `.jules/` sang `.agents/Skills/`, xoá `.jules/`, lịch chạy 2 ngày/agent** — hỏi rõ trước khi xoá vì `.jules/` từng được mô tả là quy ước của một dịch vụ ngoài; 9 file dời bằng `git mv`, mọi link tương đối tính lại theo cấp thư mục mới, guard đổi tên và vá luôn một lỗ chưa từng được kiểm (`.claude/` thiếu trong `CHECKED_ROOTS`), 7 Routine bền (`create_trigger`) chạy mỗi 2 ngày/agent | ✅ **Hoàn thành** — 2026-08-27 |
| **[EPIC-013](EPIC-013_hop_dong_tuong_minh_va_tach_coordinator/README.md)** | **Hợp đồng tường minh (cấm duck-typing ngầm) & tách nốt Coordinator** — Presenter + 6 Coordinator gọi 15 thành viên của `view` mà 0 khai báo; `IView` của Engine khai 1 method `bind()` không View nào implement; 74 tham số ctor trong đó 24 là accessor state. Luật §2.1 (ABC mặc định, `Protocol` chỉ khi Shiboken/multiple-inheritance chặn) + `IBacktestView` + `BacktestScreenState` + tách `chart_preview`/`chart_feed` | ✅ **Hoàn thành (7/7 task con)** — 2026-08-27 |
| **[EPIC-014](EPIC-014_widget_chon_symbol_va_timeframe_dung_chung/README.md)** | **Widget chọn Symbol & Timeframe dùng chung** — ba bản picker symbol khác nhau, picker timeframe chỉ cho chọn 5/16 khung. Thêm yêu thích/gần đây dùng chung mọi màn hình | ✅ **Hoàn thành** — 2026-08-28. *(Ghi nhận 2026-08-30: hàng này bị thiếu ở bản trước của file — epic đã xong từ lâu, chỉ chưa lên index này; xem `ROADMAP.md` để biết trạng thái luôn chính xác hơn file này.)* |
| **[EPIC-015](EPIC-015_qml_tung_widget_khong_chuyen_chart/README.md)** | **QML từng widget, chart ở lại QtWidgets** — chart pyqtgraph + shell ở lại QtWidgets vĩnh viễn, mọi widget khác chuyển dần sang QML theo 4 phase tăng dần rủi ro (modal độc lập → nhúng màn không chart → nhúng cạnh chart thật) | ✅ **Hoàn thành cả 4 phase** — 2026-08-30; user xác nhận bằng mắt chart không giật ở Phase 4 (widget QML đầu tiên chạy cạnh chart pyqtgraph thật trong production) |
| **[EPIC-016](EPIC-016_screen_registry_pattern/README.md)** | **Screen Registry Pattern** — `MainWindow` hard-code `_NAV_SECTIONS`/`_setup_router()`, sửa 1 màn mới đòi đụng 2 chỗ. `ISidebar` (Protocol, lý do (b) — `Sidebar` đã kế thừa `BaseView`; bỏ `select_section()`/`set_navigation()` — không caller nào cần), `AbstractScreenModule` (ABC), `IScreenRegistry`/`ScreenRegistry`, Section/Item Sequence hai tầng — không tạo type song song với `ITab`/`SidebarSection` có sẵn. `MainWindow` hết import bất kỳ màn cụ thể nào | ✅ **Hoàn thành (4/4 task con)** — 2026-08-30; sanity 8/8, integration 42/42, 757 test `screens/` |
| **[EPIC-017](EPIC-017_presentation_hard_design_round2/README.md)** | **Presentation Hard-Design Round 2** — verify độc lập report ngoài (6 điểm): 2 điểm mở task (Settings hard-code field 3 màn khác — đăng ký eager tại bootstrap, không phải trong `__init__` từng Presenter vì lazy-loading; dùng `TimeFrame` Enum **đã có sẵn** thay literal `"1m"`), 1 điểm route sang `EPIC-003` (`003G`, không trùng epic), 1 điểm scope-corrected (boilerplate chỉ đúng 3/4 presenter, lõi đã DRY), 2 điểm từ chối (auto-discovery strategy/indicator; bỏ Junction hack `run-ui.ps1`) | ✅ **Hoàn thành (2/2 task con)** — 2026-08-30 |
| **[EPIC-018](EPIC-018_app_wide_hard_design_sweep/README.md)** | **App-Wide Hard Design Sweep** — rà soát ngoài `presentation/ui` (application/domain/infrastructure/cli). 3 việc đã sửa trước khi có task/ADR (lỗi quy trình, user chỉ ra giữa chừng): `sync_cli_handler.py` dead-code failure branch, `app_bootstrapper.py` hasattr dead branch x4, `RunBacktestCommand` hasattr dead code. Từ epic này: mỗi task rà soát chỉ đúng 1 module (`018C`-`018F`), cộng lại phủ hết `src/` | ✅ **Hoàn thành (7/7 task con)** — 2026-08-30 |
| **[EPIC-019](EPIC-019_dashboard_backtest_shared_coordinators/README.md)** | **Dashboard/Backtest: gộp logic Presenter trùng lặp thành Coordinator** — báo cáo ngoài (Gemini) claim thiếu lớp cha chung, verify độc lập: 4/7 claim sai/bịa (`DashboardSymbolPickerSource` không tồn tại, nút Refresh không tồn tại, Timeframe đã dùng chung từ `EPIC-014`/`EPIC-015`), 2/7 đúng (`SymbolOptionsCoordinator`, `HealthCheckCoordinator` dùng chung Dashboard+Backtest) + 1 finding tự phát hiện (133 method Property boilerplate ở ViewModel, chưa có factory) | ✅ **Hoàn thành (3/3 task con)** — 2026-08-30 |
| **[EPIC-020](EPIC-020_page_shell_bo_cuc_4_dai/README.md)** | **`PageShell` — bố cục 4 dải dùng chung** — user đối chiếu mockup vs app thật, phát hiện 2 bug (nút "Mở rộng" Backtest ẩn vĩnh viễn do thiếu 1 signal connection; tiêu đề Dev Board bị cắt do fixed-width) + 3 màn lệch bố cục (Database rail bên trái, Backtest/Dev Board không có đầu trang riêng). `PageShell` (header/context-bar/workspace+rail-luôn-phải/console) áp cho cả 4 màn, kèm 1 bug thật thứ 3 tự bắt được khi viết abstraction (`QSplitter` tạo lại mỗi lần gọi làm cascade-delete widget con đang sống). *(Đổi số từ `EPIC-018` khi merge nhánh — trùng ID với `EPIC-018_app_wide_hard_design_sweep`.)* | ✅ **Hoàn thành** — 2026-08-30; verify bằng build app thật offscreen + chụp ảnh so sánh, không chỉ đọc code |
| **[EPIC-021](EPIC-021_ket_noi_binance_futures_testnet/README.md)** | **Kết nối Binance USD-M Futures Testnet & đường đi lệnh thật** — app hiện **không có khái niệm môi trường sàn**: DI dựng `PythonBinanceClient` không tham số (`binance_bot_module.py:231`) nên luôn mainnet ẩn danh, key user nhập ở Settings không bao giờ tới client (`BUG-080`), 2 key endpoint trong config là config chết (`BUG-081`), và parser metadata (step/tick/minNotional) chỉ được `tests/` gọi. Chọn **Futures** thay vì Spot vì mô hình khớp lệnh của repo đã là futures (SHORT + leverage trong `PaperExchange`); Settings có **2 lựa chọn độc lập** (nguồn dữ liệu vs nơi đặt lệnh); `TradingVenue` **không có member MAINNET** — rào an toàn bằng type, không bằng cấu hình | ✅ **Hoàn thành (13/13 task con + `BOT-124`)** — 2026-09-02; màn hình **Giao dịch** mới (**I**), banner môi trường + Emergency Stop + trade marker ở cả 5 màn (**K**), chart vốn realtime (**M**); 7 mốc đầu chạy headless bằng CLI — **G** là lệnh thật đầu tiên, **H** là User Data Stream nối vào `OrderFeed`, **J** là tier `tests/testnet/` opt-in chạm sàn thật |
| **[EPIC-022](EPIC-022_chien_luoc_song_tren_man_giao_dich/README.md)** | **Chiến lược sống trên màn Giao dịch** — chọn chiến lược + Thông số Chiến lược ngay trên màn Giao dịch, nối vào engine đổi được lúc chạy (`LiveStrategySession` thay 4 field đóng băng lúc boot), vẽ đường chỉ báo + vùng xu hướng của chính chiến lược đó lên chart live, và 2 rào an toàn: chưa nạp chiến lược thì không bật được giao dịch (`NO_STRATEGY_ARMED` — sửa đúng chỗ UI từng báo "Trading đang BẬT" trong khi không có gì sinh tín hiệu), đang giao dịch thì không đổi được chiến lược | ✅ **Hoàn thành (6/6 task con)** — 2026-09-07 |
| **[EPIC-023](EPIC-023_dev_board_tinh_nang_co_ban_giao_dich/README.md)** | **Dev Board có các tính năng cơ bản của màn Giao dịch** — theo yêu cầu trực tiếp của user: combo "Strategy" trên Dev Board là hàng giả (hard-code, không nối `ArmStrategyCommand`), không có Vị thế/Lệnh chờ/Vốn/Bật-Tắt-giao-dịch/Dừng-khẩn-cấp — chỉ kiểm thử được chỉ báo, không kiểm thử được chiến lược sống. User chốt: Dev Board có **toàn quyền** điều khiển giống hệt Trading (không chỉ hiển thị), tái dùng nguyên `StrategyArmingCoordinator`/`OrderFeed`/`EquityFeed`/`SignalFeed` đã viết theo Protocol/Feed dùng chung từ `EPIC-021`/`EPIC-022` — không sửa file nào của `screens/trading/` | ✅ Hoàn thành (4/4 task con) |
| **[EPIC-024](EPIC-024_modularize_trading_core_va_giao_dich_thu_cong/README.md)** | **Giao dịch thủ công trên Dev Board + Modularize Trading Core** — từ [`PRO-003`](../proposal/PRO-003.md): user quan sát Trading screen hiện chỉ có một nơi gọi cơ chế đặt lệnh (chiến lược tự động), muốn tự tay đặt Long/Short như sàn thật qua ĐÚNG cơ chế đó, không phải một đường đi riêng. Cơ chế đó có tổng quát cho caller thứ hai (con người) hay không **chưa được chứng minh** — (B) xây tính năng thủ công chính là bước chứng minh, không phải hệ quả của một sự thật đã chứng minh sẵn. (C) modularize Market Connector/Market Order/Strategy Engine chốt phạm vi *sau* (B), dựa trên bằng chứng thật (B) bộc lộ — không phải "làm sau nếu có thời gian" | ✅ Closed (A, B done; C cancelled) — **A** xong: 3 handler đổi sang port mới `ITradingSessionFactory` · **B** xong (đóng 2026-09-11, đã khớp lệnh thật trên Testnet, lộ `BUG-112`/`116`/`117`) · **C** cancelled 2026-09-11, absorbed into `EPIC-025` → epic **closed** |
| **[EPIC-025](EPIC-025_module_theo_bounded_context/README.md)** | **Split the app into bounded-context modules on the Engine's `IExtension` microkernel** — from [`PRO-004`](../proposal/PRO-004.md): the user wants *"a fairly large redesign"* because the app is split by technical layer and nobody owns a bounded context (59 method names duplicated Trading ↔ Dev Board, `binance_bot_module.py` at 750 lines, `BUG-112`/`BUG-117` as the price paid). 4 bounded contexts (`strategy` = Core) + 4 support packages + kernel; a module is an `IExtension`; Trading and Dev Board become surfaces (Dev Board gains `dev_probe` for exploring exchange APIs); a Walking Skeleton inside a Strangler Fig, 6 phases, each one PR with the app running; the Engine receives mechanism (`EPIC-001D`/`TASK-043`). North star: [`Docs/HLD/`](../../Docs/HLD/README.md) | 🟠 **Phase 5 blocked on the Engine** — Phases 0–4 are done (Phase 4 closed 2026-09-19, see the end of this row); Phase 5 (`EPIC-025F`, `ScreenRegistry` → the Engine's `NavigationService`) cannot start until `Sagittarius_Engine`'s own `TASK-043`/`EPIC-001D` ships — verified 🔵 Backlog in that repository 2026-09-19, not assumed. **Phase 0 closed 2026-09-15** after the user ran Testnet. Phase 1 shipped `modules/trading` (PRs 1.3a–1.3c×5), both screens as surfaces on a nested `QMainWindow` (1.4a–1.4d), and Welcome as the default route with the developer-mode restart (1.5a–1.5b); the allowlist of backward imports is **56 → 23** (this row said 36 until 2026-09-16; the file has held 23 entries since PR 1.3c-5, and `Tasks/ROADMAP.md` had the right number all along — see `DECISION_2026-09-16`'s note on where the wrong one came from). Its last criterion did not close and was measured rather than judged: the two screens cannot move into `modules/trading/ui/` until `support/ui_kit` and `support/charting` exist, so **Phase 4's extraction was pulled forward** and is running bottom-up as PRs 1.6a–1.6f — `assets`, `kit`, six `ui/common` helpers, `constants`/`state`/`registry`/`sidebar`/`symbol_picker`, `theme_bootstrap`/`services`/`embed`/`environment_banner`, then `support/charting` itself. 1.6d retired four lines of `baseline_shell_legacy_imports.txt` that were scheduled for Phase 5. 1.6f brought `support/charting` and 1.6g `support/indicators`, the latter mypy-clean with no new exclusion. Three defects are fixed and written up: `BUG-124` (Start on Welcome called the app port's verb on the engine's bus) and `BUG-125` (`ILogger` has no `exception()`), which together created `Docs/CASE_STUDIES/` and `fix-bug-rule` §6.5; then `BUG-126`, found by measuring `ui/common` rather than by a user — `EPIC-008` §1's P1 finding was still open, because the feed that answered it was never constructed, so every swallowed failure reached nobody. `CS-002` and a guard that reads the AST for any subscriber the application never names. **Phase 2 has started:** PR 2.1a measured its whole cut (26 files / 2782 lines, 13 consumers) and the measurement caught that moving `SignalAction` into `strategy/contracts` as the HLD specifies would have made `trading` import `strategy`, which that phase's own done-when forbids — because one file held both trading's `(side, reduce_only)` pair and strategy's `SignalAction` bridge. The pair is published, the bridge leaves with its two callers in 2.1b, and a naming compromise two documents recorded is retired. **PR 2.1b moved the context in** — 31 files, `src/application/services/` and `src/domain/strategies/` gone, allowlist 23 → 42 with every new line naming its repayment — and found the boundary rule narrower than the HLD's own assignment for the third time in this epic: the strategies could not read the indicator library the design gives them, so the imported side widened once for that package's Qt-free sub-packages. **2.1c** then published `IArmedStrategy` and *deleted* `IStrategyCatalog` after building it, because all four of its would-be consumers need the strategy classes rather than the keys; **2.1d** published `ISizingPolicy` (ADR D17) and took the allowlist 42 → 40 → **36**, retiring the `trading → backtesting` pair the ratchet was written to catch. That one found `MarginRiskPolicy` holding two rules with different owners — only `calculate_margin_and_notional()` is sizing, while leverage, mark-to-market and PnL realization are a paper broker's books — so it was split rather than moved whole, which is what keeps Phase 3 from having to reach back the other way. **2.1e** then brought strategy's UI in (11 files) and, because the duplication ratchet refused the bare move, extracted the `StrategyCardViewModel` the two live screens had been writing twice — nineteen identical members each. The Phase 1 duplicated-member count **fell 59 → 39 for the first time since `PRO-004`**, the total 132 → 115, and the allowlist went 36 → 44 with every line grouped by its exit. **2.1f** shipped the symbol lease and found the rule it enforces already living in a Presenter — three order-submission callers, one check — so it moved onto the order path where every caller inherits it, with three measured deviations from the specified design and a race test the review's own probe showed was missing. **2.1g** brought `trade-once` into the module, published the four trading-limit value types `ExecuteOrderResult` had been carrying since PR 1.3b, and corrected the plan's own claim that `declare_cli()` was the mechanism — it is the interactive shell's registry, which `trade-once` has never used. Allowlist **44 → 37**. **PR 2.1c-2 closed Phase 2's coded work**: the tick handler moved in and `StrategyModule.boot()` took its subscription, which emptied `src/application/event_handlers/` and left the four backtest use cases as all that remains under `src/application/`. Its finding is a hook read rather than assumed — `subscribe(bridge)` is the wrong door twice over (nothing calls it, and its `QtEventBridge` would move live order submission onto the Qt main thread), and SDD §03 had been carrying that contradiction. Allowlist **37 → 36**. What Phase 2 still needs is not code: the user confirming arm / disarm / tick → order on Testnet. **Phase 3 has therefore started, and its first step was a deletion:** PR 3.1a removed `RunBacktestCommand`, `StopBacktestCommand` and `BacktestState`, bound in the composition root and dispatched by nobody — and re-measuring them is what turned cleanup into a finding, because that loop republished stored candles as `MarketTickEvent` on the real bus, which since PR 2.1c-2 is the live strategy's subscription. One dispatch would have placed a real order from historical data. Reading the next item turned up **`BUG-127`**: `ISymbolMarketMetadataCache` is bound nowhere and nothing calls `put()`, so the Backtest screen's exchange-rule verification (`BOT-095E1`) has always answered 'not verified'. See **`BUG-127` then came out of reading Phase 3's item 2**, and it is `CS-003`: `BOT-095E1`'s exchange-rule check on the Backtest screen had said *"not verified"* for every symbol since it shipped, because the port that stores the filters was **bound by nobody** and the only thing that produces them had no caller — `resolve()` raised on every presenter construction and the `except` built an empty cache, so what read as a fallback was the only path. Six checks were silent, one of them `CS-002`'s own guard, written for this disease and scoped to bus subscribers. Its wider check now ships: *every type the app `resolve()`s must be bound*. **PR 3.1b** then published `IStrategyEngine` behind `IStrategyEngineFactory` — two ports because a contract may not carry `StrategyEngine` any more than `BaseStrategy`, and two methods because `run_batch()` has no caller outside the module — bound `ISizingPolicy` at last, and took the allowlist **35 → 29** with the golden and hand-verified-trade tests passing **unchanged**. Phase 3's own measurement (`EPIC-025D` §4) split the phase where the QML is: the backtest screen's 74 files carry 29 imports of packages ADR D21 *deletes*, so they travel with Phase 4 like Phase 1's two screens did. **Phase 4 is running**: PRs 4.1a/4.1b brought trading's six feeds and `order_book` in; PRs 4.3a–4.3l deleted the QML tree entirely (`.qml` 27 → 0). **PR 4.3m** closed the last Phase-1-era blocker — `modules/strategy/ui/`'s shared classes, imported directly by `trading`/`dashboard`/`backtest`, moved to `presentation/ui/common/` (the ADR's original "duplicate per screen" plan measured worse — Phase 1 pair 32 → 63 — before it shipped); duplication total 107 → 99, allowlist 58 → 40, two real bugs fixed on the way. **PR 4.4's scope is now measured**: 133 files / 27,480 lines — larger than any pull request this epic has landed, split into 4.4a–4.4f. **4.4a done**: `symbol_options_coordinator`/`sync_progress_feed` moved into `modules/market_data/ui/`, `sync_progress_report` into its `contracts/` — no new port turned out to be needed, `ISymbolCatalog` already answered everything the first file calls; allowlist 40 → 45 (five shrink-only entries, same shape as PR 4.1a's six feeds), duplication unchanged. **4.4b done**: `screens/data_management` (23 files) + `qml_property.py` moved into `modules/market_data/ui/`; ten predicted allowlist entries retired, one new one found (the shell's own screen-registration import, Phase 5's to retire, not Phase 4's); `mypy` checked this tree for the first time and its 115 pre-existing `@Property`-class errors were re-keyed into `pyproject.toml`'s exclude list rather than fixed inside the move, matching PR 1.6f's own precedent. **4.4c was attempted, found a real Engine dependency cycle (the moved screens' reads of `modules.strategy.contracts` would force `trading.dependencies` to include `"strategy"`, which cycles with `strategy`'s already-real `"trading"` edge), and was reverted uncommitted.** Its accepted fix — `trading` defines its own four ports, `strategy` implements them via a translating adapter, zero new dependency edges — landed 2026-09-18 (`DECISION_2026-09-17...md` §8, `TRACKING.md` `p4-portinv`), and **4.4c itself (the screen move) is now done too (PR #232)** — `trading`+`dashboard`+the three shared classes moved into `modules/trading/ui/{trading,dashboard}/`, `tests/sanity/` green this time (no cycle), duplication ratchet's Phase 1 pair **32 → 0**, allowlist 30 retired/4 new, and a genuine new finding: `support/indicators/ui/` joined `_UI_SUPPORT_ZONES` since `dashboard` (and `backtest`, still to move) both need its Qt widgets through their own `ui/`. **4.4d is now done too**: `screens/backtest` (79 files) + `base_event_logger.py` moved into `modules/backtesting/ui/`, flat (the module's only screen); its test tree followed (124 unit + 3 integration files); `src/presentation/ui/screens/` now holds only `settings`; allowlist 5 retired/3 new (10 → 8); `mypy` left `presentation/`'s exclusion for the fifth time and measured 190 pre-existing errors across 26 of 80 files, re-keyed the same way as 4.4a–c. **4.4e is now done too**: the monolithic `screens/settings` (6 files) left the legacy `AbstractScreenModule` mechanism entirely — trading and market_data each contribute their own `SETTINGS_SECTION` (`modules/trading/ui/settings_contribution.py`, `modules/market_data/ui/settings_contribution.py`) rendered by a new `shell/settings/` surface on `SettingsSurface`; the venue-lock-while-trading-on guard stayed with trading's section and was deliberately **not** replicated onto market_data's (no live-trading interlock applies to a data-source venue) — a design finding, not an omission. A shared `StatusMessageViewModel` (`support/ui_kit/status_view_model.py`) replaced three byte-identical status-signal trios (trading's own new settings view model, market_data's, and the pre-existing `TradingViewModel`), cutting the duplication-guard's pair-count and correcting its baseline to 66 — arithmetic 64 (merge-base, confirmed unchanged by an independent review re-measuring that exact commit, contradicting this row's own first-draft "already-stale" claim) − 4 (names the deleted `settings` package itself had contributed to the `settings+trading.ui` pair) + 9 (raw new names in the two new triads) − 3 (folded into `StatusMessageViewModel`) = 66. Two real gaps this PR's own settings screen exposed, both fixed at the shared mechanism rather than the call site: `tests/conftest.py`'s `real_screen_registry()` never called `assemble_contributions()`, the one production step that binds `IContributionTable`, invisible until settings became the first non-dev-gated screen to resolve it; and `SettingsView` was not wrapped in `PageShell`, silently dropping the mandatory `EPIC-021K` environment banner. `mypy` on the four new `@Property`-class files found 45 real (env-verified, not measurement-artifact) PySide6 false positives, re-keyed the same way as 4.4a–d. Allowlist +1 (trading's settings presenter reads the still-legacy `presentation/cli/exchange_status_formatter.py`, deferred pending that CLI code's own future move). **4.4f's own classification found it needs splitting too**: `binance_bot_module.py` (612 lines) is not the empty husk §3.15 assumed — `modules/backtesting/module.py` and `modules/strategy/module.py` had each already documented, in their own docstrings, that their remaining bindings deliberately stay there until deleting the composition root itself is the actual reason to move them (Phase 2/3's own one-piece-at-a-time strangler pattern, not a new question). Split by destination module, safest first: **4.4f-1 (backtesting's two commands) ✅ Done 2026-09-18** — found and removed one piece of pre-existing dead wiring on the way (`BacktestChartHostFactory` bound via the container when the View already constructs its own default; `BackTestPresenter` now constructs it directly, matching the view, and nothing resolves the type through DI any more) after moving the binding's import into `modules/backtesting/module.py` briefly broke `test_module_contribution_laziness.py` by pulling Qt into a headless run. **4.4f-2 (strategy's own state/arm-disarm) ✅ Done 2026-09-18** — `StrategyRegistry`+six strategies, `LiveStrategyFactory`/`LiveStrategySession`, `ArmStrategyCommand`/`DisarmStrategyCommand`, and `boot()`'s arm-from-config seeding all moved into `modules/strategy/composition/`, unchanged in shape; one test's fake container needed a real `DictConfig()` bound for the new `IConfig` resolve `boot()`'s seeding step added. **Re-split once 4.4f-3's classification read what actually remained**: the rest was never one owner — two bindings (`MarketDataVenue`, `IExchangeSessionFactory`/`MarketDataSessionFactory`) are `market_data`'s own leftover, four (`IEventPublisher`/`IConfigReader`/`ICommandDispatcher`/`ITaskManager`) are cross-cutting engine-adapter ports owned by no module — so the plan became **4.4f-3 (`market_data`'s leftover) ✅ Done 2026-09-18**, **4.4f-4 (`trading`'s infrastructure/commands/queries — order submission, last) ✅ Done 2026-09-19** — the largest slice, and the first that could not be a pure move: the legacy root's conditional `ITradingClient` bind (gated on `TradingVenue`, decided at registration time) cannot move into `register()` verbatim, because `register()` may not `resolve()` (`Docs/SDD/04_boot_and_configuration.md`'s own table) and deciding the conditional needs `TradingVenue`'s actual value — moved into `TradingModule.boot()` instead, alongside the `PositionRefreshService` scheduling that already lived there, preserving the exact unbound-when-disabled behaviour `tests/sanity/`'s own `_NOT_DISPATCHED` entry depends on. Also fixed a real path-resolution bug the move would otherwise have introduced silently (`PathUtils.get_relative_path(__file__, ...)` for `secrets.local.json` needed three more `..` segments from its new, deeper location) and added three individually-justified `allowlist_module_boundaries.txt` entries (9 → 12) for adapter-wiring imports the boundary scan had never seen before, because the exempt-by-filename legacy root always made them invisibly. **4.4f-5 (the shared core ports + indicator scripts, moving to `shell/composition_root.py` directly, + the file's own deletion) ✅ Done 2026-09-19** — `ITaskManager`/`IEventPublisher`/`IConfigReader`/`ICommandDispatcher` and the nine indicator-script registrations now bind directly inside `create_app()`, `_register_indicator_scripts()` moved verbatim; `binance_bot_module.py` itself deleted, along with its stale `COMPOSITION_ROOT_FILES` boundary-scan exemption. `tests/integration/test_app_integration.py`'s fixture now calls `create_app()` itself instead of hand-assembling a container, closing the drift risk its own docstring had flagged since PR 0.4a. Found and filed, not fixed (out of this slice's bounded scope): `BUG-131` — `test_indicator_script_conventions.py`'s `_SCRIPTS_DIR`/`_DOMAIN_DIR` have scanned a directory `EPIC-025` PR 1.6g deleted since 2026-09-16, making two of its checks silently vacuous; only `_MODULE_FILE` (in this slice's own scope) was repointed. **Phase 4 (`EPIC-025E`) is now complete — every PR from 4.1 through 4.4f-5 has landed, and `binance_bot_module.py` no longer exists.** See `TRACKING.md` for the per-PR log — this row is deliberately coarse, because a phase count here goes stale within a week |
