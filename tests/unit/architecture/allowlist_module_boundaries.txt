# Boundary violations AS FOUND, recorded as a ratchet (HLD §6.1).
# Read by tests/unit/architecture/test_module_boundaries.py.
#
# One line per (importing_module, imported_module) pair, no line numbers, so an
# unrelated edit never churns this file. The guard fails on any pair NOT listed
# here (a new violation) and on any pair listed here that no longer exists (the
# fix landed - delete the line in the same commit). This list only shrinks.
#
# Every entry names the phase that deletes it. An entry with no exit is not an
# entry, it is a permission - and this file grants none.
#
# History of the count: 10 as found (2026-09-13) -> 8 after PR 0.3 -> 41 after
# PR 0.4a -> 38 after PR 0.4a-2 -> 34 after PR 0.5 -> 28 after PR 1.1a -> 24
# after PR 1.1b -> 20 after PR 1.2 -> 80 after PR 1.3a -> 60 and 56 after PR
# 1.3b's two halves -> 46, 32, 29, 25, 23 after PR 1.3c-1..1.3c-5
# -> 42 after PR 2.1b -> 40 after PR 2.1c -> 36 after PR 2.1d
# -> 44 after PR 2.1e
# -> 37 after PR 2.1g
# -> 36 after PR 2.1c-2
# -> 35 after PR bug-127 (the metadata port is bound; the presenter's
#    adapter import goes with it)
# -> 29 after PR 3.1b
# -> 32 after PR 3.1c.
# -> 44 after PR 4.1a.
# -> 58 after PR 4.1b.
# -> 40 after PR 4.3m.
# -> 45 after PR 4.4a.
# -> 36 after PR 4.4b (45 - 10 stale retired + 1 new shell entry).
# -> 10 after PR 4.4c (36 - 30 stale retired + 4 new entries: 2 market_data.ui
#    reads under their new modules.trading.ui.dashboard address, 2 shell
#    entries for the two screens' module.py, same shape as PR 4.4b's one).
# -> 8 after PR 4.4d (10 - 5 stale retired + 3 new entries: 2 market_data.ui
#    reads under their new modules.backtesting.ui address, 1 shell entry for
#    module.py, same shape as PR 4.4b/4.4c's).
# -> 9 after PR 4.4e (8 - 0 stale retired + 1 new entry: the old monolithic
#    settings screen carried no allowlist entries of its own, so nothing
#    retires; splitting it adds one new cross-boundary read).
# -> 12 after PR 4.4f-4 (9 - 0 stale retired + 3 new entries: the legacy
#    composition root's own adapter-wiring imports, exposed for the first
#    time once the module that owns the binding does the wiring instead of
#    the exempt-by-filename strangler root -- see the entries' own comment).
# -> 8 after PR 5.2 (`EPIC-025F`; 12 - 4 stale retired + 0 new: all four
#    `shell.legacy_screens -> modules.*.module` entries retire together --
#    the four legacy screens left `AbstractScreenModule` for their own
#    `ScreenContribution`s, and `shell/legacy_screens.py` itself is deleted).
# -> 10 after PR BOT-019 (8 - 0 stale retired + 2 new: `market_tick_feed.py`
#    moved from `modules/trading/ui/` to `modules/market_data/ui/`, so its
#    two pre-existing `trading` consumers now read it via an allowlisted
#    cross-module line each, same shape as the two `symbol_options_
#    coordinator`/`sync_progress_feed` lines this same file already carries).
#
# THE LAST NUMBER ON THAT LINE IS CHECKED against the entries below, by
# `test_the_documented_count_is_the_real_count` in the guard. It stopped at 56
# for eleven pull requests while the file held 23, and the wrong figure was
# read out of `Tasks/epics/README.md` (which said 36 - a number this file has
# never held) and copied into six tracking rows and a decision record on
# 2026-09-16, each time as "unchanged at 36". A count in prose is a claim about
# state, and `.claude/ONBOARDING.md` §13 bans writing one without a
# way to check it; this is the way. Add or retire an entry, edit this line in
# the same commit -- and keep `N after PR <id>` on ONE line, because the guard
# reads the phrase and a `#` wrapped into the middle of it hides the figure.
# PR 2.1b found that out by breaking it and being told.
#
# The jump at 1.3a is 0.4a's again, and for the same reason: it moved the
# `trading` context into `modules/trading/`, so every legacy screen, CLI
# command and service that names one of its types became a visible, counted
# violation instead of an indistinguishable same-tree import. PR 1.3b
# publishes IOrderSubmission / ITradingSession / IAccountSnapshot and moves
# those consumers onto them, which takes this file to 13.
#
# Read the two directions separately, because they are not the same debt:
# SIX entries point OUT of the new tree (modules.trading -> legacy) and each
# names the one shared object or foreign concept behind it; the rest point IN
# (legacy -> modules.trading) and every one of them is a consumer waiting for
# a port. The direction that would be architecturally alarming is the first,
# and it is the small one -- and it has not moved since 1.3a.
#
# PR 1.3b's first half took the inbound count 61 -> 41 without touching a
# single consumer, by moving the types that CROSS the boundary into
# `contracts/`: `Order`, `LivePosition`, `EquitySample` and the four answer
# types (`ExecuteOrderResult`, `CancelOrderResult`, `OrderPreview`,
# `EnableTradingResult`, `EmergencyStopResult`). Their names are unchanged,
# deliberately -- see the note below.
#
# 0.5 is the first time an entry retired because a **consumer moved onto a
# published port** rather than because code moved into the module: four screens
# asked market_data for a sync by building its command, and now ask
# `IMarketDataSync` instead.
#
# The jump at 0.4a is not a regression: it moved
# ~60 files into modules/market_data/, and every legacy screen that dispatches
# one of its commands directly became a visible, counted violation instead of an
# indistinguishable same-tree import. Phase 1 replaces each dispatch with a port
# call, and this file returns to single digits. 0.4a-2 is the first such
# repayment: inverting who parses a CLI line let the two market_data commands
# move into the module, trading 5 dispatch entries for 2 handler-class ones.

# ---------------------------------------------------------------------------
# CLOSED by PR 1.3a. `application -> infrastructure` held five entries: the
# handlers EPIC-021 allowed to build FuturesTradingClient themselves, which
# HLD 7.3 measured with two independent tools and which is the violation this
# whole guard was written for. They are gone because the handlers and the
# client now live in the same module, so the import no longer crosses anything
# -- not because the direction was excused. `presentation -> infrastructure`
# closed the same way: the CLI payload mapper is
# `modules/trading/adapters/binance/` now, so those two entries moved into the
# 1.3b block below, re-keyed rather than retired.

# (PR 0.3 had already removed two entries from that second block:
# app_bootstrapper and settings_presenter both reached into
# `infrastructure.binance.binance_endpoints` for the venue resolvers, and that
# module is now `support/binance_gateway/contracts/binance_endpoints.py` —
# which any zone may import, so those imports are not violations at all.)

# ---------------------------------------------------------------------------
# -> modules.market_data.adapters: reaching past the contracts into a module's
# private adapters. There were two, and both were two lines of code rather than
# a design:
#
# * exchange_session_factory constructed PythonBinanceClient directly: RETIRED
#   by PR 1.3c-4, which did the split this note had promised since PR 0.4a.
#   MarketDataSessionFactory is market_data's own adapter now, so wrapping its
#   own client is an intra-module call. The rule the shared class existed to
#   keep -- ONE place may construct binance.client.Client -- moved down into
#   support/binance_gateway with the construction itself, so the guard still
#   asserts one call site by equality rather than growing to two.
# * ~~backtest_presenter falls back to InMemorySymbolMarketMetadataCache when
#   the container has no cache registered.~~ **CLOSED by BUG-127's fix.** This
#   note read "PR 0.4b rebuilds this screen and the fallback goes with it -- a
#   presenter that needs a stub is a presenter whose dependency should have
#   been injected", and the last clause was exactly right for a reason nobody
#   checked: the container had **no** cache registered, ever, so the fallback
#   was not a fallback but the only path, and the screen's exchange-rule check
#   answered "not verified" for every symbol from the day it shipped. The port
#   is bound now (`market_data/composition/adapter_bindings.py`), the presenter
#   resolves it plainly, and the import of another module's adapter is gone.
#   Retired by a **fix**, not by a move -- the third way an entry has left this
#   file, after 2.1b's (the file moved) and 0.5's (a consumer went onto a port).

# ---------------------------------------------------------------------------
# presentation -> modules.market_data.cli: RETIRED by PR 1.3c-5, and the route
# it took is worth keeping because two earlier attempts took the wrong one.
#
# `interactive_shell.py` hard-coded `{"sync": SyncCliHandler, ...}`, naming two
# classes inside the module: 2 entries, added by PR 0.4a-2. PR 1.1b tried to
# fix it by moving the shell into `shell/` and reverted, measured: the move
# traded these 2 entries for 2 new lines in
# `baseline_shell_legacy_imports.txt`, a shrink-only ratchet growing so another
# could shrink.
#
# What actually retired them is the inversion `IContributionRegistry` already
# made for panels: the module **declares** (`BoundedContextModule.declare_cli`)
# and the shell **collects** (`shell/cli_registry.py`). The shell names no
# module class, the module names no shell class, and adding a command touches
# only the module that owns it. Nothing about where `interactive_shell.py`
# lives had to change.
#
# `exchange-status` is still a hand-written entry in that file, and that is the
# honest remaining state: its handler formats through
# `exchange_status_formatter`, which needs `presentation.enum_labels`, so
# moving the handler into `modules/trading/cli/` would make a module import the
# legacy tree — refused outright, not allowlistable. It is a
# `presentation` -> `presentation` import, so it costs no entry here, and it
# goes when `EnumLabels` finds its home in `support/` (Phase 4 dissolves
# `ui/common`).

# ---------------------------------------------------------------------------
# presentation -> modules.market_data.application: the transitional dispatch
# surface (27 entries).
#
# What these are: a legacy screen or CLI command builds a market_data Command or
# Query object and hands it to the Engine's ICommandDispatcher. The import is
# only for the *type*, never for a handler - so the coupling is a data shape,
# which is why it was tolerable to leave standing for one phase rather than
# rewrite 20 screens in the same pull request that moved 60 files.
#
# Why they are not simply published in contracts/: publishing them would make
# this coupling permanent and correct-looking. It is neither. Phase 1 replaces
# each dispatch with a call on a narrow port (IHistoricalKlines, IMarketDataSync,
# IMarketStream, ISymbolCatalog, IRangeCoverage), and each replacement deletes
# its line here. Nothing new may be added to this block: a new caller uses a
# port, or there is a new port.
#
# Where they go, by phase:
#   Phase 4  - NINE of the eleven data_management.* lines: the two
#              `database.*` ones, `queries.get_database_gaps`,
#              `queries.get_database_status`, `queries.scan_all_databases`,
#              `queries.audit_database_integrity`, `database.clear_market_data`,
#              `database.prune_empty_shards` and the two bulk-sync ones.
#              NOT the other two: `kline_inspector -> get_historical_klines`
#              retires in PR 1.1 and `scan_coordinator -> list_available_symbols`
#              in PR 1.2, because HLD §3.4 lists `IHistoricalKlines` and
#              `ISymbolCatalog` as **published** ports — a screen using a
#              published port is not a violation, wherever the screen lives.
#              RE-KEYED from Phase 1 on 2026-09-14, when Phase 1
#              was cut into pull requests (epic README §3.3): "a port call
#              retires the entry wherever the file lives" is true mechanically,
#              and wrong here. HLD §3.4 marks database maintenance, gaps, scan,
#              audit and bulk sync **Internal**, and its selection rule is that
#              a port is public when it has a consumer in ANOTHER module. Theirs
#              is the Data Management screen, which the note below sends into
#              `modules/market_data/ui/` in Phase 4 — so publishing seven ports
#              to serve a consumer that is about to move inside the module would
#              publish internals to satisfy a counter. The screen move retires
#              them instead, which is the same phase either way.
#              The original reasoning for deferring that move, unchanged:
#              they were written down as "PR 0.4b, when
#              that screen is rebuilt", on the assumption that 0.4b moves the
#              screen into `modules/market_data/ui/` and turns these into
#              intra-module imports. It cannot: measured, the move needs 35 NEW
#              entries pointing the wrong way — `modules/market_data/ui/` ->
#              `presentation.ui.{kit,assets,common,components,state,qml}` —
#              because a module's `ui/` may import `support/ui_kit` and
#              `support/charting`, and neither exists before Phase 4. Trading 8
#              forward entries for 35 backward ones inverts the number this
#              epic actually watches. EPIC-025A §1.8 records the measurement.
#   PR 0.5   - DONE: the four `sync_market_data.command` lines, via
#              `IMarketDataSync` — the module publishes the capability, the
#              screens depend on the contract
#   PR 1.1a  - DONE: the six `get_historical_klines` lines, via
#              `IHistoricalKlines`
#   PR 1.2   - DONE: the two `list_available_symbols` lines via
#              `ISymbolCatalog`, and the two `get_backtest_range_coverage`
#              lines via `IRangeCoverage`. Both queries were then deleted:
#              once their consumers had moved, nothing in `src/` dispatched
#              either one, and a registration kept for a hypothetical
#              consumer is what PR 1.1a's cleanup established as dead weight
#   PR 1.1b  - DONE: the four `start/stop_live_stream` lines, via
#              `IMarketStream`. These two ports are what `EPIC-025A` §1 assigns
#              to Phase 1; 0.5 deliberately published one port rather than
#              three, so the mechanism is proved by a consumer that runs
#              rather than by three contracts written at once
#   PR 0.4a-2 - DONE: the sync/stream CLI lines left when those commands moved
#              into `modules/market_data/cli/`


# ===========================================================================
# PR 1.3a — the `trading` context moved into `modules/trading/`
# ===========================================================================
#
# What 1.3a did: `domain/trading`, `use_cases/trading` (less arm/disarm, which
# are `strategy`'s in Phase 2), `use_cases/queries` (all three were trading's),
# `application/ports` (all five were trading's), the four trading services and
# the trading half of `infrastructure/binance` are now one module. Trading's
# own value objects, order enums and events went into `contracts/` rather than
# `core/vo`, on HLD 2.4's rule measured the same way `SymbolMarketMetadata`
# was: a type reaches the Published Language on two consumers in two MODULES,
# and until `strategy` exists trading is the only module among its importers.
# That choice is why 26 would-be entries are not in this file at all: the
# legacy tree may import `modules.*.contracts` freely.
#
# What 1.3a did NOT do, deliberately: publish a port. The three ports HLD 3.4
# names could not be published first, which is worth writing down because the
# epic's own PR table had them in that order. `IOrderSubmission` needs
# `OrderPreview`, `ITradingSession` needs `EnableTradingResult`,
# `IAccountSnapshot` needs `ExchangeConnectionStatus` -- every one of those
# types was in the legacy tree, and `boundaries/rules.py` refuses the new tree
# any legacy import outside this file. `market_data` could publish
# `IMarketDataSync` at PR 0.5 only because PR 0.4a had already moved its code
# in. This pull request is trading's 0.4a.

# ---------------------------------------------------------------------------
# modules.trading -> legacy: THREE entries, and the whole of the new tree's
# debt to the old one. Each names the object or concept that keeps it standing.
#
# * exchange_session_factory (3): RETIRED by PR 1.3c-4. ONE instance used to
#   answer both this context's ITradingSessionFactory and market_data's
#   IExchangeSessionFactory (EPIC-024A registered it against the port precisely
#   so the handlers shared it instead of each building a throwaway), and this
#   note said for four pull requests that splitting it was a behaviour change
#   rather than a move. It was -- so the split came with its own pull request
#   and its own gate, and it is smaller than the note feared: the venue
#   plumbing (one Client construction site, the 30s timeout, the BUG-111 clock
#   sync) moved into support/binance_gateway, where both contexts already read
#   resolve_testnet_flag and ExchangeCredentials, and each context kept a
#   factory with only its own methods on it.
# * margin_risk_policy (1): sanctioned in writing. EPIC-025B 1.3 says
#   "`trading` keeps only the exchange's part of sizing -- lot/tick rounding
#   and TradingLimitPolicy -- and its import of MarginRiskPolicy stays on the
#   allowlist until Phase 2 (ADR D17)".
# * position_sizing (1) and signal_action (1): a sizing instruction and a
#   strategy's verdict. Both are `strategy`'s vocabulary, not trading's, so
#   promoting them now would publish another context's language from this one.
#   Phase 2 gives them an owner and these two entries leave with it.
#
# CLOSED by PR 2.1d (ADR D17) -- BOTH, and they were the pair this whole file
# was first written to catch: `trading` depended on `backtesting`, which no
# diagram in this repository ever drew. `position_sizing_bridge` moved into
# `modules/strategy/domain/policies/`, `PositionSizing` into `core/vo` (HLD
# 2.4's row for it), and the sizing rule out of `MarginRiskPolicy` into
# `strategy`'s `MarginSizingPolicy` behind `ISizingPolicy`. What stayed in
# `backtesting` is that class's other half -- leverage by direction,
# mark-to-market and realized PnL -- because those are a paper broker's books
# and moving them would have re-drawn the same wrong arrow one context over in
# Phase 3.


# ---------------------------------------------------------------------------
# WHY `Order` AND `LivePosition` KEEP THEIR NAMES (PR 1.3b)
#
# HLD 3.4 said "LivePosition NEVER leaves the module; PositionSnapshot is its
# flat DTO", and PR 1.3b does not do that. Measured before deciding: `Order`
# is a frozen dataclass with eleven fields and ZERO methods; `LivePosition` is
# frozen with eight scalar fields, a `LiquidationPrice` (a NewType over
# Decimal) and one pure derivation (`side`, the sign of `position_amt`).
# A `PositionSnapshot` would therefore be field-for-field identical, which is
# behaviour-free churn -- and that is the exact argument PR 1.2 used to KEEP
# the name `BacktestRangeCoverage` rather than rename a DTO that crosses the
# edge. Doing the opposite here would make the epic inconsistent with itself.
#
# So both moved into `contracts/` under their own names, HLD 3.4 is corrected
# to say so, and the 20 entries that named them are gone -- the legacy tree
# may import `modules.*.contracts` freely. What did NOT move is anything with
# behaviour: the policies, the handlers, the mutable session state.

# ---------------------------------------------------------------------------
# legacy -> modules.trading: SIXTY-ONE entries, every one a consumer waiting
# for a port, and every one retired by PR 1.3b.
#
# What these are: a legacy screen, CLI command or service names one of
# trading's types -- an ExecuteOrderCommand it dispatches, a `LivePosition` it
# renders, a TradingSessionState it reads three fields off. Same shape as the
# 27-entry market_data dispatch block above, and retired the same way: PR 1.3b
# publishes IOrderSubmission, ITradingSession and IAccountSnapshot, each
# consumer calls the port instead, and its line goes.
#
# Three of them are worth naming, because they are not dispatches and their
# fix is not just "call a port":
#
# * `-> modules.trading.application.trading_session_state`: RETIRED by PR
#   1.3c-1. It was 5 entries, three of them Presenters reading a MUTABLE,
#   lock-guarded application service directly. Measured, those Presenters read
#   exactly three fields -- `enabled`, `orders_sent_this_session`,
#   `known_open_symbols` -- so `ITradingSession.snapshot()` answers with a
#   frozen TradingSessionSnapshot of exactly those three (HLD 2.4), and the
#   shared mutable object no longer crosses the boundary at all. Both
#   Presenters now take one snapshot where they used to take two or three
#   separate reads, so a screen can no longer observe half of a change the
#   websocket thread is making.
# * `-> modules.trading.domain.live_position` (6) and `.domain.order` (7):
#   HLD 3.4 says LivePosition NEVER leaves the module and PositionSnapshot is
#   its flat DTO. These entries are the measurement of how far that is from
#   true today, and 1.3b's flattening is what closes them. They are the reason
#   `live_position.py` and `order.py` stayed in `domain/` while the order enums
#   moved to `contracts/`: publishing them as they are would have made the
#   wrong shape permanent and correct-looking.
# * `application.use_cases.trading.{arm,disarm}_strategy.handler -> ...
#   trading_session_state` (2): RETIRED by PR 1.3c-1, earlier than planned and
#   by a different route. This was `strategy` reading trading's session state --
#   legal by design once both are modules (HLD 3.4's worked example: arm reads
#   the enabled flag, a `strategy -> trading` call that resolves the cycle), a
#   violation only because `strategy` is not a module yet. The note here said
#   Phase 2 would retire it by moving the caller. In fact both handlers read
#   the same single field the Presenters did, so they took the port in the same
#   change: a legacy file may import `modules.*.contracts` freely, so calling
#   `ITradingSession.snapshot().enabled` needs no entry at all. Phase 2 still
#   moves the caller into `modules/strategy`; what it no longer has to do is
#   change the call.

# CLOSED by PR 2.1d: `order_preview_formatter -> order_quantity_rounding_policy`
# and `trade_once_cmd -> position_sizing_bridge`. The first is a retirement,
# not a move of its consumer: `OrderQuantityRoundingPolicy` and `NotionalCheck`
# are published as `trading/contracts/order_quantity_rounding_policy.py`,
# because `contracts/order_preview.py` already had a `notional_check` field
# typed by that enum -- a published DTO whose field type was not published --
# and because ADR D17 puts the exchange's lot filter on trading's side of the
# line, where `strategy`'s sizing bridge has to be able to reach it. The
# second only changed prefix: the bridge is `strategy`'s now, so the entry
# below replaces it and retires with PR 2.1g (`trade-once` becomes strategy's
# declared CLI command).
# CLOSED by PR 2.1g: the THREE `trading_limit_policy` entries, and none of
# them by moving a consumer. `TradingLimitViolation`, `TradingLimits`,
# `TradingLimitContext` and `TradingLimitCheck` are published as
# `trading/contracts/trading_limits.py`, on the measurement PR 2.1a and PR 2.1d
# both used: `ExecuteOrderResult` has carried all four since PR 1.3b -- a
# `TradingLimitViolation` inside its `blocked_by` union, the `limit_checks`
# tuple, the `limit_context` they were judged against -- so a consumer reading
# the answer had to import them out of `domain/`. The type crossed; the file
# had not. `TradingLimitPolicy` itself, the thing that *decides*, stays
# internal: publishing the answer is not publishing the judgement.
#
# The `trade_once_cmd` entry needed one more thing, and it is the better half
# of this change: that file did `container.resolve(TradingLimitPolicy)` to read
# `.limits` for its display. A command-line file resolving a domain policy for
# one attribute is what `ExecuteOrderResult.limits` now answers instead -- the
# same promise `limit_context` already made, that what gets shown is what
# decided.

# ---------------------------------------------------------------------------
# PR 2.1b -- `modules/strategy` arrives. 23 entries in, 4 retired out, and the
# jump is 0.4a's and 1.3a's for the third time: moving a context makes every
# import of it a counted violation instead of an indistinguishable same-tree
# one. Each line below names the pull request that deletes it, and the four
# that left did so because their file moved, not because a direction was
# excused -- `live_trading_coordinator` and the `SignalAction` bridge are
# inside this module now.
#
# The direction that would be architecturally alarming is OUT of the new tree,
# and it is two lines, both ADR D17's and both retiring together in PR 2.1d:
# the coordinator computes a quantity through `trading`'s position-sizing
# bridge and reads the legacy `PositionSizing` value object. D17 moves the
# bridge and `MarginRiskPolicy` here behind `strategy/contracts/ISizingPolicy`,
# which is what makes them intra-module.
#
# EPIC-025C section 3.4 carries the cut these exits refer to.

# CLOSED by PR 2.1d (ADR D17): the only outbound pair this module had, and
# the module now points nowhere but `core`, `support` and
# `trading/contracts`. `PositionSizing` went to `core/vo`; the bridge is
# `strategy`'s own domain and the coordinator calls it as one.
#
# ONE inbound entry arrived in exchange, and it is the shape 0.5 and 1.3b ran:
# `PaperExchange` sizes a paper fill by the same rule live trading uses (ADR
# D17's *"one number by construction"*), so it depends on `ISizingPolicy` --
# its constructor parameter is the contract, which a legacy file may import
# freely. What the entry records is the **default**: sixty construction sites
# in this repository pass no policy at all, so the class names the one
# implementation to build when nobody hands it one. `EPIC-025D` retires it by
# resolving the port from the container once `backtesting` is a module.

# CLOSED by PR 2.1c: the TWO `live_strategy_session` entries. Each Presenter
# read `.config`, and the Dev Board also read `.is_armed` through a SECOND
# acquisition of the session's lock. `IArmedStrategy.armed()` answers both in
# one `ArmedStrategySnapshot`, so the pair can no longer be observed
# disagreeing with itself, and neither screen names the session any more.
#
# CLOSED by PR 4.3m: the three `strategy_registry` entries this section used
# to carry (backtest/dashboard/trading Presenters passing `.available()` into
# their arming/overlay Coordinators). The measurement below is why an
# `IStrategyCatalog` returning bare keys could not have served them in 2.1c —
# every caller that read the keys also needed the strategy *classes* — and
# why the fix was not "publish that port sooner" but the wider redesign
# `DECISION_2026-09-17_strategy_ui_contributes_rather_than_being_imported.md`
# records: `IStrategyCatalog.options()`/`params_form()`/`validate_params()`
# and `IStrategyChartOverlay.overlay_for()` answer in data (labels, param
# forms, computed overlay lines) instead of handing out the registry, so a
# consumer never needs `BaseStrategy` at all. `trade_once_cmd`'s own registry
# use retired earlier, into `IStrategyEngineFactory` (Phase 3).
#
#   · both Presenters used to pass `available()` into `StrategyArmingCoordinator`
#     and `StrategyOverlayCoordinator`/`IndicatorCoordinator`, which called
#     `.get(key)` and constructed the strategy for `chart_line_colors()` /
#     `chart_line_widths()` — now `IStrategyChartOverlay.overlay_for()`;
#   · `backtest_presenter` passed it to `StrategyConfigCoordinator` and
#     `IndicatorCoordinator` for the same reason — now the same two ports.
# -> IStrategyEngineFactory (Phase 3, EPIC-025D). EPIC-025C section 1 item 2
# publishes this port *for* backtesting, so its consumers retire when that
# context moves -- not earlier, and saying "2.1c" here would be a date nobody
# can meet.

# CLOSED by PR 2.1e: all SEVEN, exactly as this note said they would be --
# the files became `modules/strategy/ui/` and stopped crossing anything. What
# arrived instead is the inbound direction, below.

# -> the two live screens move into a module's `ui/` (Phase 4, EPIC-025E), and
# the backtest screen into `modules/backtesting/ui/` (Phase 3, EPIC-025D).
#
# PR 2.1e moved eleven files -- `strategy_arming_coordinator`, `signal_feed`,
# `strategy_display`, `strategy_params/` and `strategy_overlay/` -- out of
# `presentation/ui/{common,components}` into this module's `ui/`, and extracted
# `StrategyCardViewModel` from the two live view models, which had been
# carrying **nineteen identical members each**. So every consumer that names
# one of these files is now a counted crossing. It is the 0.4a / 1.3a / 2.1b
# shape for the fourth time, and this one bought the number the epic is
# actually judged on: duplicated members across every UI pair **132 -> 115**,
# and the Phase 1 pair **59 -> 39**, its first fall since `PRO-004` measured it.
#
# The fifteen split by context, and that split is why no port is invented here:
#
#   * SEVEN are the two live surfaces (Trading, Dev Board). Their exit is the
#     screens themselves moving into `modules/*/ui` -- ADR D21's QML deletions
#     and Phase 4 -- not a contract, because a screen and the card it hangs
#     will then be in the same module.
#   * SIX were the **backtest** screen, a different bounded context reading
#     this one's parameter form, its chart overlays and its strategy-name
#     display.
#   * TWO were the view models themselves, retiring with their screens.
#
# CLOSED by PR 4.3m: all fifteen. `EPIC-025D`'s two choices for the SIX —
# share this card's view model, or read it through a published widget
# contract — turned out to be the wrong pair once the inbound direction was
# looked at again: neither shares data across a module boundary. The ADR
# (`DECISION_2026-09-17_strategy_ui_contributes_rather_than_being_imported.md`)
# instead deleted `modules/strategy/ui/strategy_params/`, `strategy_display.py`,
# `strategy_card_view_model.py`, `strategy_arming_coordinator.py` and
# `signal_feed.py` outright: the three pure-UI-kit widgets moved to
# `support/ui_kit/param_form/` (no strategy knowledge in them at all), the
# key→label lookup and the class-handling both moved onto
# `IStrategyCatalog`/`IStrategyChartOverlay`, and what stayed screen-specific
# (the card's own arm/disarm orchestration, its own signal feed) became each
# screen's own `coordinators/`/`signal_feed.py` — deliberately NOT shared,
# so Trading and Dev Board carry a second copy of orchestration glue with
# nothing behavioural left in it to duplicate wrongly. Nothing here crosses
# a module boundary any more, so there is nothing left to allowlist.

# CLOSED by PR 3.1b: the two backtest handlers and `paper_exchange` stopped
# reaching into this module. SIX lines, and each one was a consumer holding
# another context's internals rather than asking it a question:
#
#   * the handlers took `StrategyRegistry` in their constructors and handed it
#     back into `build_engine()` along with their own `IEventPublisher` --
#     three of `strategy`'s internals to get one engine. `IStrategyEngine`
#     behind `IStrategyEngineFactory` replaces all of it: name a key, get
#     something you can drive. The engine had to be published as an ABC rather
#     than the class, for PR 2.1c's reason (a published contract may not carry
#     `BaseStrategy`), and the port carries **two** of `StrategyEngine`'s three
#     public methods -- `run_batch()` has no caller outside this module in
#     `src/`, so publishing it would promise something nobody asked for.
#
#   * `paper_exchange` defaulted its `sizing_policy` to `MarginSizingPolicy()`,
#     imported straight from `domain/policies/`. This is the line ADR D17
#     scheduled for Phase 3 and the one this whole allowlist was written to
#     catch a second time. The default stays -- fifty-five inline construction
#     sites in its own test file pass no policy, which is `ONBOARDING` section 8
#     trap 5's shape -- but it now comes from `contracts/default_sizing_policy()`,
#     which returns that same one implementation. Backtest and live sizing are
#     still one number by construction, which is what D17 actually requires,
#     with no import across the boundary. The handlers also pass the resolved
#     port explicitly, so the container binding is live rather than the dead
#     wiring `BUG-120` was.
#
# The three `presentation.ui.screens.* -> strategy_registry` lines are NOT
# retired here and are a different debt: those are Presenters handing the
# registry to coordinators that construct a strategy for its chart lines, which
# PR 2.1c measured and which needs the screens to move (Phase 4).

# CLOSED by PR 2.1c-2: the tick handler is inside this module, so the one
# `legacy -> modules.strategy.application.services` entry that named the live
# session from an event handler is an intra-module read. Retired the 2.1b way
# (the file moved) rather than the 0.5 way (a consumer went onto a port), and
# there was nothing to publish here: the subscriber IS this context's, and the
# folder name `event_handlers/market_data/` was the only market-data thing
# about it. `StrategyModule.boot()` owns the subscription, on the raw bus --
# `subscribe(bridge)` was read first and is the wrong door twice over, which
# that method's docstring records.
#
# It also emptied `src/application/event_handlers/` entirely. What remains
# under `src/application/` is the four backtest use cases, which are Phase 3's.

# CLOSED by PR 2.1g: `trade-once` is this module's command now -- both files
# live in `modules/strategy/cli/`, so the four entries that named this module
# from `presentation/cli/` are intra-module reads. `main.py` imports
# `execute_trade_once` from the new path, which is exactly how
# `modules/market_data/cli/{sync,stream}_cmd.py` have worked since PR 0.4a-2;
# `declare_cli()` was read and is **not** the mechanism, because that registry
# is the *interactive shell*'s and `trade-once` has never been one of its
# commands. Recorded because the note this replaces said "declared CLI
# command", which would have sent the next reader to the wrong registry.

# ---------------------------------------------------------------------------
# PR 3.1c -- `modules/backtesting` arrives. THREE entries in, none retired,
# and three is the number worth explaining, because the measured estimate was
# five times that.
#
# `EPIC-025D` section 4 counted 33 imports reaching into the moving code from
# 17 files. Almost all of them read the *answers* a backtest produces --
# `BacktestResult`, `Trade`, `ExitReason`, `BacktestMetrics`, `CommissionType`,
# `Currency`, `BrokerSimulationConfig`, the two lifecycle events -- and those
# went into `contracts/` with the move, so they became legal imports rather
# than counted violations. PR 1.3b's first half did exactly this for `trading`
# and took its inbound count 61 -> 41 without touching a single consumer; this
# is the same trick, and here it absorbed 30 of 33.
#
# What is left is the one thing `contracts/` cannot absorb: the Backtest screen
# **builds this module's commands and dispatches them**. That is the
# transitional shape `EPIC-025` exists to retire -- a consumer assembling
# another context's command object for the Engine's dispatcher -- and HLD
# section 3.4 records its exit: *"if a CLI `backtest` command appears,
# `IBacktestRunner` is added then"*. It is `IMarketDataSync`'s arc exactly (PR
# 0.4a moved the code, PR 0.5 published the port and four screens stopped
# building the command), so these three retire with the port, at the latest
# when the screen itself moves in Phase 4.


# ---------------------------------------------------------------------------
# PR 4.1a -- `trading`'s six UI feeds come in, and TWELVE entries arrive for a
# move that retires none. That is the worst ratio in this epic, and it is the
# right trade for a measured reason: the entries are the *same* twelve imports
# that existed before the move, pointing at the same six files. What changed is
# only which side of the boundary the files sit on.
#
# `equity_chart_adapter`, `equity_feed`, `execute_order_block_reason`,
# `market_tick_feed`, `order_feed` and `order_fill_marker` normalise `trading`'s
# own published events onto Qt signals. HLD section 3.5 has assigned all six to
# `modules/trading` since the epic was cut, and PR 1.4b-2 measured why they
# could not go then -- the two legacy Presenters are their only consumers, and a
# legacy file may import a module only through `contracts/`. That is still true;
# it is why these twelve lines exist rather than nothing.
#
# What makes the move worth its twelve lines NOW rather than at Phase 4's end:
# the duplication ratchet. `tools/measure_duplicate_members.py` reports the
# Phase 1 pair (`dashboard` + `trading`) at **39 -> 34** on this move alone,
# its first fall since PR 2.1e, with the total across every pair unchanged at
# 113. The epic is judged on that number reaching 0, and each feed that leaves
# `presentation/ui/common/` for its owning module takes a handful of duplicated
# member names with it.
#
# Their exit is the whole point and it is two pull requests away, not five:
# `EPIC-025E` section 3.1 measured `screens/trading` as having exactly thirteen
# legacy imports -- these six plus seven into `components/order_book` -- and
# **nothing else, no QML at all**. So 4.1b (order_book, travelling with
# `data_management` by the user's decision of 2026-09-16) and then 4.1c
# (`screens/trading` itself into `modules/trading/ui/`) retire all twelve of
# these in one go, along with the six `dashboard` ones once that screen's four
# QML imports are gone in 4.3.


# ---------------------------------------------------------------------------
# PR 4.1b -- `order_book` and `live_order_book_coordinator` come in. FOURTEEN
# more entries, none retired, and the same argument as 4.1a's twelve: they are
# the imports that already existed, from the same two screens to the same
# files. All 26 retire together at 4.1c and 4.4, when the two screens
# themselves move into `modules/trading/ui/`.
#
# `EPIC-025E` section 3.1 is why these cannot wait for that: `screens/trading`
# has exactly thirteen legacy imports and **no QML at all**, so it is the one
# live screen whose move does not depend on ADR D21's deletions. 4.1a took six
# of the thirteen, these take the other seven, and 4.1c then retires all
# twenty-six of this module's lines in one pull request.
#
# The move itself waited for a de-duplication rather than for a dependency, and
# that was the user's call on 2026-09-16: moving `order_book` alone raised the
# duplication ratchet, because its two table models and Data Management's two
# were the same class written twice (section 3.5). `support/ui_kit/table_model.py`
# is that class now, so the move costs the ratchet nothing -- the Phase 1 pair
# goes 34 -> 32 and the total across every pair stays at 112.


# ---------------------------------------------------------------------------
# PR 4.4a -- `symbol_options_coordinator`, `sync_progress_feed` come in.
# `EPIC-025E` section 3.2/3.3 measured both as needing `modules/market_data/ui`
# as their only legal home (each already reads `modules.market_data.contracts.*`
# directly), while their consumers -- `backtest_presenter`, `dashboard_presenter`,
# `data_management_presenter` and the two screens' own signal/sync wiring --
# are still legacy. FIVE entries, none retired: they are the same imports that
# already existed, from `presentation.ui.common.*` to the same two classes,
# now crossing a module boundary because the classes moved rather than because
# a new coupling was introduced. `sync_progress_report` (the DTO) moved one
# step further, into `modules/market_data/contracts/`, so it needs no entry --
# a contracts import is legal from legacy unconditionally.
#
# These retire as their four legacy consumers move into their own modules:
# `backtest_presenter`/`signal_wiring` at 4.4d (`modules/backtesting/ui/`),
# `dashboard_presenter` at 4.4c (`modules/trading/ui/`), and
# `data_management_presenter` at 4.4b (`modules/market_data/ui/` -- at which
# point the import is same-module and needs no entry at all).


# ---------------------------------------------------------------------------
# PR 4.4b -- `screens/data_management` + `qml_property` move into
# `modules/market_data/ui/`. All ten of §3.15's predicted zero-cost entries
# retired in this same commit (nine `coordinators/* -> application/*` plus
# the one `sync_progress_feed` entry 4.4a left behind), since the presenter
# and its coordinators now live in the module they import from.
#
# ONE entry is new rather than retiring, and it is not `screens/data_management`
# debt at all: `shell/legacy_screens.py` still imports this screen's
# `AbstractScreenModule` (`DatabaseScreenModule`) the same way it always has --
# Trading and Dev Board keep the identical shape after PR 1.4's "become a
# surface" (`presentation/ui/screens/{trading,dashboard}/module.py` still
# subclass `AbstractScreenModule`, still wired from `legacy_screens.py`).
# `boundaries/rules.py::is_module_entry_point` only recognises the exact shape
# `modules.<name>.module` (three dotted parts) as the shell's legal entry
# point into a module, so `modules.market_data.ui.module` -- one level
# deeper, because a screen's registration class lives beside its
# Presenter/View rather than at the module's own root -- is not covered by
# that rule and needed a line here instead of a rule change: `EPIC-025F`
# (Phase 5) is what replaces `AbstractScreenModule`/`ScreenRegistry` with the
# Engine's `NavigationService`, not Phase 4, so widening `rules.py` now would
# legalise a shape the epic already plans to delete outright. Every other
# screen's `module.py` hits this identical wall the moment it moves in
# 4.4c/4.4d, so this entry's retirement (like the nine above it) is Phase 5's,
# not Phase 4's -- recorded here rather than left to surprise that phase.
#
# RETIRED, `EPIC-025F` PR 5.2 -- `DatabaseScreenModule` left
# `AbstractScreenModule` for a `ScreenContribution` (`database_screen()`,
# owned by `market_data` itself); `shell/legacy_screens.py` is deleted, so
# this pair no longer exists.


# ---------------------------------------------------------------------------
# PR 4.4c -- `screens/trading` (9 files) + `screens/dashboard` (16 files) +
# the three shared classes (`strategy_card_view_model.py`,
# `strategy_arming_coordinator.py`, `signal_feed.py`) move into
# `modules/trading/ui/` as two screen packages (`trading/`, `dashboard/`),
# unblocked by the port-inversion (ADR `DECISION_2026-09-17...md` §8) that
# landed the day before. All 30 of `EPIC-025E` §3.15's predicted zero-cost
# entries retired in this same commit (six `dashboard_presenter ->
# modules.trading.ui.*` feeds, six `trading_presenter -> modules.trading.ui.*`
# feeds, fourteen `order_book`-reading lines across both screens, two
# `manual_order_intent` lines, two `market_data.ui` lines that move to a new
# source below) -- same-module the instant both presenters and their
# `order_book` neighbours share an address.
#
# Two lines are new rather than retiring, same shape and reason as PR 4.4b's
# one: `dashboard_presenter` still reads `market_data.ui.symbol_options_coordinator`/
# `sync_progress_feed` (§3.3's open question, resolved to `market_data/ui/` in
# PR 4.4a) under its new `modules.trading.ui.dashboard` address -- retires
# when `data_management`/`backtest`'s own copies of this cross-module read are
# gone, i.e. never fully (the feed genuinely lives in a different module),
# unless a future per-module `SyncProgressFeed` variant (§3.15's seam) removes
# the direct read.
#
# `BOT-144` moved the first line's *construction* out of `dashboard_presenter`
# into its own `__init__` Builder (`logic/presenter_factory_core.py`, one of
# the four sibling modules `logic/presenter_factory.py` orchestrates --
# `build_core_presenter_state`) -- `symbol_options_coordinator`'s real import
# now lives there instead; `dashboard_presenter` itself keeps only a
# `TYPE_CHECKING`-guarded import for the class-level attribute annotation the
# same move required (never executed, so the boundary scanner does not see
# it). Retirement condition is otherwise unchanged from the paragraph above.
modules.trading.ui.dashboard.logic.presenter_factory_core -> modules.market_data.ui.symbol_options_coordinator
modules.trading.ui.dashboard.dashboard_presenter -> modules.market_data.ui.sync_progress_feed

# Two more are new for the same reason PR 4.4b's `shell.legacy_screens ->
# modules.market_data.ui.module` line was: `shell/legacy_screens.py` still
# imports `TradingScreenModule`/`DashboardScreenModule`, one level deeper than
# `is_module_entry_point`'s `modules.<name>.module` shape recognises, because
# each screen's `AbstractScreenModule` now lives beside its own Presenter/View
# under `modules/trading/ui/{trading,dashboard}/module.py` rather than at the
# module's own root. `EPIC-025F` (Phase 5) retires both when `NavigationService`
# replaces `AbstractScreenModule`/`ScreenRegistry`, not before.
#
# RETIRED, `EPIC-025F` PR 5.2 -- both screens left `AbstractScreenModule` for
# their own `ScreenContribution`s (`dashboard_screen()`/`trading_screen()`,
# owned by `trading` itself); `shell/legacy_screens.py` is deleted, so
# neither pair exists any more.


# ---------------------------------------------------------------------------
# PR 4.4d -- `screens/backtest` (79 files, the largest slice) +
# `base_event_logger.py` move into `modules/backtesting/ui/`, flattened to
# its root (no per-screen subdirectory needed, unlike 4.4c -- `backtesting`
# has only one screen, so there is no `module.py`/`preview.py` collision to
# avoid). All 5 of the zero-cost entries this screen carried retire in this
# same commit (3 same-module -> `modules.backtesting.application.*`, now
# same-address; the other 2 below move rather than retire).
#
# 2 lines are new rather than retiring, same shape as PR 4.4a/4.4c's:
# `backtest_presenter`/`signal_wiring` still read
# `market_data.ui.symbol_options_coordinator`/`sync_progress_feed` (§3.3's
# open question, resolved to `market_data/ui/` in PR 4.4a) under their new
# `modules.backtesting.ui` address.
modules.backtesting.ui.backtest_presenter -> modules.market_data.ui.symbol_options_coordinator
modules.backtesting.ui.signal_wiring -> modules.market_data.ui.sync_progress_feed

# 1 more is new for the same reason PR 4.4b/4.4c's shell entries were:
# `shell/legacy_screens.py` still imports `BacktestScreenModule`, one level
# deeper than `is_module_entry_point`'s `modules.<name>.module` shape
# recognises, because the registration class lives beside its own
# Presenter/View under `modules/backtesting/ui/module.py` rather than at the
# module's own root. `EPIC-025F` (Phase 5) retires it when `NavigationService`
# replaces `AbstractScreenModule`/`ScreenRegistry`, not before.
#
# RETIRED, `EPIC-025F` PR 5.2 -- the screen left `AbstractScreenModule` for
# its own `ScreenContribution` (`backtest_screen()`, owned by `backtesting`
# itself); `shell/legacy_screens.py` is deleted, so this pair no longer
# exists.

# ---------------------------------------------------------------------------
# PR 4.4e -- "Settings becomes a surface": the monolithic
# `presentation/ui/screens/settings/` splits into `modules/trading/ui/
# settings/` (credentials, connection check, order venue) and `modules/
# market_data/ui/settings/` (default symbols/interval/sync days, data
# venue). A design step, not a file move -- unlike PR 4.4a/c/d, the deleted
# monolith carried zero allowlist entries of its own (it read only within
# `presentation/`), so nothing here retires.
#
# 1 new entry: `TradingSettingsPresenter` renders the same
# `ExchangeConnectionStatus` the Trading screen's Check Connection button
# always has, through the one formatter both the CLI and the UI already
# shared (`format_exchange_connection_status`). That formatter still lives
# under `presentation/cli/` -- migrating trading's CLI formatting into
# `modules/trading/cli/`, the way `modules/market_data/cli/` already did in
# PR 0.4a-2, is real work of its own and out of this PR's bounded scope
# (`task-execution-rule.md`). It exits when that migration lands.
modules.trading.ui.settings.trading_settings_presenter -> presentation.cli.exchange_status_formatter

# PR 4.4f-4 -- trading's own infrastructure/commands/queries move out of
# `binance_bot_module.py` (`EPIC-025E_phase4_support_and_dissolve_common.md`
# §3.16). The legacy composition root always wired these three adapters; the
# boundary scan never saw it because that file is skipped by name
# (`tests/unit/architecture/boundaries/scan.py`) -- it *is* the composition
# root the strangler is replacing. Once `modules/trading/composition/
# adapter_bindings.py` does the same wiring, the scan sees it for the first
# time, and `_module_may_import`'s support-package branch only ever allowed
# a `contracts/` package, a computation library, or a module's own `ui/`
# reaching a UI support zone -- none of which fits "a composition root wires
# a port to its concrete adapter", the one thing every composition file in
# this epic exists to do.
#
# `InMemoryFuturesSymbolMetadataCache` implements `IFuturesSymbolMetadataCache`,
# a port `modules/trading/contracts/` owns, but the class itself sits under
# the generic `infrastructure/persistence/` rather than `modules/trading/
# adapters/persistence/` -- pre-existing debt from before the module split,
# not something this PR introduced. Relocating it touches nine other files
# (tests under `tests/testnet/`, `tests/integration/infrastructure/binance/`,
# `tests/unit/infrastructure/persistence/`, two probe scripts) that read
# nothing about *this* move -- real work of its own, out of this PR's
# bounded scope (`task-execution-rule.md`). It exits when that relocation
# lands.
modules.trading.composition.adapter_bindings -> infrastructure.persistence.futures_symbol_metadata_cache

# `EnvFirstCredentialsProvider`/`SecretsFileSource` are `support/
# binance_gateway`'s own concrete adapters behind `IExchangeCredentialsProvider`
# (that package's own `contracts/`) -- the same shape `FuturesSessionFactory`/
# `MarketDataSessionFactory` already have as each module's *own* adapter, but
# credentials resolution has exactly one consumer (`trading`) and no module-
# owned adapter class of its own to hide behind. `trading` is `support/
# binance_gateway`'s only consumer of these two files today (`grep` across
# `src/`), so there is no second module this entry's absence would protect.
# It exits if/when `support/binance_gateway` grows its own composition-time
# factory for this port, the way each module's own session factory already
# hides its adapter's construction -- until then, the one module that
# consumes it constructs it directly, exactly as the legacy composition root
# always did.
modules.trading.composition.adapter_bindings -> support.binance_gateway.adapters.env_first_credentials_provider
modules.trading.composition.adapter_bindings -> support.binance_gateway.adapters.secrets_file_source

# `BOT-019` moved `market_tick_feed.py` from `modules/trading/ui/` to
# `modules/market_data/ui/`: `MarketTickEvent` is `market_data`'s own
# contract, and a Feed normalizing one module's own event belongs in that
# module's `ui/` -- the exact call `EPIC-025` PR 4.4a already made for
# `symbol_options_coordinator`/`sync_progress_feed`, two lines above this
# same file's own precedent for it. `trading`'s two pre-existing consumers
# now read it from its new address, same shape as those two lines. Exits
# when/if a per-module `MarketTickFeed` variant removes the direct read --
# not planned, since the whole point of one Feed is that a second copy is
# the defect this exists to prevent.
modules.trading.ui.dashboard.dashboard_presenter -> modules.market_data.ui.market_tick_feed
modules.trading.ui.trading.trading_presenter -> modules.market_data.ui.market_tick_feed
