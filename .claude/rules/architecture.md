---
paths:
  - "src/**/*.py"
---

Open [`.agents/rules/architecture-rule.md`](../../.agents/rules/architecture-rule.md) — layers, explicit contracts (ABC by default), one abstraction per file, event placement, seams. This pointer copies no rule text.
