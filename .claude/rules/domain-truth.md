---
paths:
  - "src/domain/**/*.py"
  - "src/application/**/*.py"
---

**Open [`.agents/rules/domain-truth-rule.md`](../../.agents/rules/domain-truth-rule.md) now** — it
is short, and it is the rule guarding real money: truthful data, no collapsed trading semantics, a
UI that promises only what the engine delivers.

`architecture-rule.md` also governs this layer. This pointer copies no rule text.
