---
paths:
  - "src/**/*.py"
  - "scripts/**/*.py"
---

**Open [`.agents/rules/code-quality-rule.md`](../../.agents/rules/code-quality-rule.md) now** — it
is short, and it governs the file you are reading.

This pointer copies no rule text, by the rule in `CLAUDE.md`. `ls .agents/rules/` is the full
index; that file is rarely the only one that applies.
