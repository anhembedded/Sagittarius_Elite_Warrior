"""`StatCardRow.qml` + `StatCardRowVM` — the dynamic row of primary
performance stat cards (`BackTestViewModel.primaryStatCards`).

This file exists only so `tests/` resolves to a unique package path
(`qml-rule.md` §1 — one widget, one directory), same as every other
standalone widget package in `qml/`.
"""
